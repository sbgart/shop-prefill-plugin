## Настройки витрины в админке (спека по реализации)

Источник истины по дефолтам: `lib/config/storefront.settings.php`.

UI сейчас состоит из вкладок:

- `General` («Общие») — основная вкладка (витрина и предзаполнение): `templates/actions/settings/blocks/tabs/General.html`, `templates/actions/settings/blocks/tabs/Prefill.html`
- `Design` (`templates/actions/settings/blocks/tabs/Design.html`)

Ниже — **каждая настройка**: где показываем, **каким полем** и что означает.

У каждой опции: **Название** — человекочитаемая подпись; в скобках — ключ строки в `locale/*/LC_MESSAGES/shop_prefill.po` (`msgid`), если он уже заведён. Если ключа нет или перевод в `ru_RU` пустой, название сформулировано по смыслу (при появлении ключа в локализации лучше синхронизировать текст). **Описание** — в одну короткую фразу: что даёт включение или заполнение поля для магазина; детали реализации — в разделах концепта и в коде, не дублируем здесь.

### Zen Mode: общий и пер-инстансный шаблон сводки

В чекауте Shop-Script у каждого **способа доставки** и **способа оплаты** есть свой идентификатор инстанса (несколько подключений одного плагина — разные ID). Для свёрнутой строки группы в Zen Mode задаётся **один общий** Smarty-шаблон на группу (`summary_template`). Дополнительно для отдельных инстансов хранится запись в `custom_templates`: персональный шаблон применяется **только если** у записи включён флаг **«шаблон активен»** (`active`), иначе для этого инстанса всегда используется общий `summary_template` (см. UI ниже).

**Формат хранения (целевой, под UI):** для каждого ID инстанса — объект `{ "active": bool, "template": string }` (Smarty). Сейчас в коде для доставки может фигурировать устаревший вариант «значение = одна строка шаблона» — при внедрении свитчера нужно унифицировать чтение/запись: учитывать `active`, при `false` игнорировать `template`.

- **Доставка** — ключ в `zen.groups.delivery.custom_templates`: строка, которую возвращает `shopPrefillCheckoutState::getShippingInstanceId()`. Это **ID записи способа доставки в `shop_plugin`** (то же значение, что первая часть `variant_id` до точки, формат ключей тарифов в `shopHelper::getShippingMethods()`). **Сверка с ядром Shop-Script (checkout 2.0):** в `shopCheckoutShippingStep::process()` в `data['shipping']['selected_variant']` кладётся элемент из списка тарифов с полем `variant_id` вида `{id_плагина}.{id_тарифа}`; отдельного поля `id` у тарифа ядро не добавляет. В `shopCheckoutDetailsStep` при успешном расчёте выставляется `data['shipping']['id']` = первая часть `variant_id` (переменная `$shop_plugin_id` в коде шага). В `vars` для шаблона шаг shipping отдаёт **`selected_variant_id`**, а не `vars.shipping.selected_variant`. Порядок в геттере: `data.shipping.id` → опционально `data.shipping.selected_variant.id` (кастом плагинов) → первая часть `variant_id` из `getSelectedVariant()` → первая часть `vars.shipping.selected_variant_id`. В норме совпадающие источники дают **одну** строку; метод не сравнивает их попарно.
- **Оплата** — тот же контракт: ID из `getPaymentId()` (`data.payment.id`). В конфиге и в `getSummaryTemplate()` для группы `payment` пер-инстансная логика пока не подключена — добавить по аналогии с доставкой.

#### UI: пер-инстансные шаблоны сводки (доставка и оплата)

**Где:** вкладка Design → блок «Дзен режим» → блоки оформления «Доставка» и «Оплата» (отдельные подсекции или два последовательных списка). Контекст — **текущая витрина** настроек плагина.

**Состав списка:** для витрины подтягиваются **активные** способы доставки и **активные** способы оплаты (инстансы, доступные в магазине для этой витрины). Для каждого инстанса — одна строка настроек.

**Строка инстанса (слева направо):**

1. **Switcher** — «шаблон для этого инстанса активен». В выключенном состоянии персональный шаблон на фронте **не** используется (`active = false` в сохранённых данных).
2. Справа от свитчера — **название инстанса** (как в бекенде Shop-Script: человекочитаемое имя способа доставки/оплаты).

**Под строкой:**

- Свитчер **выкл.** — статичный текст: _используется общий шаблон_ (имеется в виду групповой `summary_template` для доставки или оплаты). Ссылки на редактирование нет.
- Свитчер **вкл.** — ссылка **«Редактировать шаблон»**. Поле с текстом шаблона на самой странице списка **не** показываем — только в модальном окне.

**Модальное окно «Редактировать шаблон»:**

- **Основная зона (слева или на всю ширину без колонки переменных):** редактор содержимого шаблона. **Сейчас** — одно многострочное текстовое поле (textarea). **Позже** — замена на JS-редактор (подсветка/подсказки Smarty и т.д.) без смены смысла поля.
- **Справа в модальном окне:** список **доступных переменных** для этой группы (доставка или оплата) — те же плейсхолдеры, что подставляются в Smarty при рендере сводки. Клик по переменной **вставляет** её в редактор (в позицию курсора или в конец — уточнить при реализации).
- Кнопки модального окна: сохранить (писать в `template` и не сбрасывать `active`), отмена.

**Итог по данным:** в сохранённом JSON/структуре настроек для каждого инстанса хранятся `active` и `template`; пустой `template` при включённом `active` — решить при реализации (валидация или fallback на общий шаблон).

### Общие

Заголовок блока переключателей `prefill.sections.*` в UI: **Группы предзаполнения** (`setting.prefill.sections.label`).

- `active`
  - **Название**: Статус для витрины (`setting.active`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Основные настройки"
  - **Описание**: Включает работу плагина на выбранной витрине.
  - **Дефолт**: `true`
  - **Статус UI**: уже есть

- `prefill.my_delivery_variants`
  - **Название**: Мои варианты доставки
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Основные настройки"
  - **Описание**: Включает выбор вариантов доставки на основе истории заказов покупателя.
  - **Дефолт**: `true`
  - **Статус UI**: уже есть

- `prefill.debug_panel`
  - **Название**: Панель отладки (`setting.prefill.debug_panel`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Основные настройки"
  - **Описание**: Показывает на витрине панель отладки плагина. Работает при включённом режиме отладки Webasyst.
  - **Дефолт**: `false` (чтобы при часто включённом debug у администратора панель не светилась покупателям)
  - **Статус UI**: уже есть

- `prefill.on_entry`
  - **Название**: Предзаполнение при входе на страницу оформления (ключа в locale нет)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Основные настройки"
  - **Описание**: При включении предзаполнение выполняется уже при первом заходе на любую страницу витрины. Если опция выключена, срабатывание — при открытии корзины. Полезно, если цена в корзине зависит от способа доставки.
  - **Дефолт**: `true`
  - **Статус UI**: нет (нужно добавить)

- `prefill.sections.customer`
  - **Название**: Покупатель (авторизация) (`setting.prefill.sections.customer`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Блоки оформления"
  - **Описание**: Разрешает предзаполнение блока «Покупатель (авторизация)».
  - **Дефолт**: `true`
  - **Статус UI**: нет (нужно добавить)

- `prefill.sections.delivery`
  - **Название**: Доставка (регион, способ, адрес) (`setting.prefill.sections.delivery`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Блоки оформления"
  - **Описание**: Разрешает предзаполнение блока «Доставка (регион, способ, адрес)».
  - **Дефолт**: `true`
  - **Статус UI**: нет (нужно добавить)

- `prefill.sections.payment`
  - **Название**: Оплата (`setting.prefill.sections.payment`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Блоки оформления"
  - **Описание**: Разрешает предзаполнение блока «Оплата».
  - **Дефолт**: `true`
  - **Статус UI**: нет (нужно добавить)

- `prefill.sections.confirm`
  - **Название**: Подтверждение (`setting.prefill.sections.confirm`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Блоки оформления"
  - **Описание**: Разрешает предзаполнение для блока «Подтверждение» (только комментарий к заказу; поле "Условия предоставления услуг" — плагин не заполняет).
  - **Дефолт**: `true`
  - **Статус UI**: нет (нужно добавить)

- `prefill.integration.cityselect`
  - **Название**: Интеграция с плагином "CitySelect" (`setting.prefill.integration.cityselect` — в `ru_RU` перевод пустой)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Интеграции"
  - **Описание**: Планируемая настройка. Сейчас интеграция не подключена в логике плагина, переключатель ни на что не влияет.
  - **Дефолт**: `true`
  - **Статус UI**: не реализуем (оставлено на будущее)

- `prefill.integration.dp`
  - **Название**: Интеграция с плагином "Информация о доставке и оплате от BodeSite" (ключа в locale нет)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Интеграции"
  - **Описание**: Планируемая настройка. Сейчас интеграция не подключена в логике плагина, переключатель ни на что не влияет.
  - **Дефолт**: `true`
  - **Статус UI**: не реализуем (оставлено на будущее)

- `prefill.remember_me.active`
  - **Название**: Продлевать авторизацию на витрине (ключа в locale нет)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие (подгруппа «Продление cookie авторизации» — не путать с гостевым `consent.remember_data`)
  - **Описание**: Для покупателей, которые **авторизовались**. При посещении витрины магазина плагин продлевает срок, на который они остаются авторизованными в Webasyst.
  - **Дефолт**: `true`
  - **Статус UI**: нет

- `prefill.remember_me.expires`
  - **Название**: Срок жизни cookie авторизации, дней (ключа в locale нет)
  - **Формат**: Number (int, days)
  - **Вкладка**: Общие (та же подгруппа; показывать если `active = true`)
  - **Описание**: На сколько **дней вперёд** продлевается вход при каждом посещении витрины авторизованным покупателем.
  - **Дефолт**: `90`
  - **Статус UI**: нет

- `prefill.guest.consent_required`
  - **Название**: Требовать согласие гостя на сохранение данных для предзаполнения (ключа в locale нет)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Общие -> подгруппа "Основные настройки"
  - **Описание**: Требует от гостя явного согласия перед сохранением данных для предзаполнения. При снятии флажка данные стираются.
  - **Дефолт**: `true`
  - **Статус UI**: нет

- `prefill.my_delivery_variants_button_classes`
  - **Название**: Дополнительные CSS-классы для кнопки «Мои варианты» (ключа в locale нет)
  - **Формат**: Text (string)
  - **Вкладка**: Общие -> подгруппа "Основные настройки"
  - **Описание**: Добавляет CSS-классы к кнопкам «Мои варианты» для подгонки под вёрстку темы дизайна.
  - **Дефолт**: `''`
  - **Статус UI**: нет

### Design

Вкладка: **Дизайн** (`tab.design`).

- `styles.accent_color`
  - **Название**: Акцентный цвет оформления (ключа в locale нет)
  - **Формат**: Color (hex string) / Text с валидацией hex
  - **Вкладка**: Design
  - **Описание**: Задаёт акцентный цвет элементов плагина на витрине для подгонки под тему дизайна.
  - **Дефолт**: `#000`
  - **Статус UI**: нет

### Дзен режим

Вкладка: **Дзен-режим** (`tab.zen`). В locale также есть **Группы секций** (`zen.groups.title`) — при появлении блока групп в админке.

- `zen.active`
  - **Название**: Включить Дзен-режим (`zen.setting.active`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Дзен режим -> подгруппа "Основные настройки"
  - **Описание**: Дзен-режим скрывает поля ввода и выводит краткую сводку посекционно, при условии что данные указаны в полном объёме. При наличии незаполненных обязательных полей или ошибок в заполнении, секция выводится в полном виде.
  - **Дефолт**: `true`
  - **Статус UI**: нет

- `zen.icon_display`
  - **Название**: Иконки в свёрнутых секциях (`zen.icon_display`)
  - **Формат**: Select - я сменил, надо поменять в UI.
  - **Варианты**:
    - `plugin` — «Логотипы способов доставки и оплаты» (`zen.icon_display.plugin`)
    - `default` — «Дефолтные иконки групп» (`zen.icon_display.default`)
    - `none` — «Без иконок» (`zen.icon_display.none`); в коде: `shopPrefillPluginZenMode::getIconDisplayMode()` → `renderCollapseBlock()` не заполняет `icon_url`
  - **Подсказка под полем**: `zen.icon_display.hint`
  - **Вкладка**: Дзен режим -> подгруппа "Основные настройки"
  - **Описание**: Выбирает набор иконок для свёрнутых секций.
  - **Дефолт**: `plugin`
  - **Статус UI**: уже есть

- `zen.icon_size`
  - **Название**: Размер иконок секций (`zen.icon_size`)
  - **Формат**: Radio (`small` / `medium` / `large`)
  - **Варианты**: «Маленький» / «Средний» / «Большой» — `zen.icon_size.small`, `zen.icon_size.medium`, `zen.icon_size.large`; подсказка: `zen.icon_size.hint`
  - **Вкладка**: Дзен режим -> подгруппа "Основные настройки"; показывать если `zen.icon_display != none`
  - **Описание**: Задаёт размер иконок в сводках блоков.
  - **Дефолт**: `medium`
  - **Статус UI**: уже есть (условно)

- `zen.toggle_button_classes`
  - **Название**: Дополнительные CSS-классы кнопки сворачивания секций (ключа в locale нет)
  - **Формат**: Text (string)
  - **Вкладка**: Дзен режим -> подгруппа "Основные настройки"
  - **Описание**: Добавляет CSS-классы к кнопке сворачивания/разворачивания блоков для подгонки под тему дизайна.
  - **Дефолт**: `''`
  - **Статус UI**: нет

#### Блоки оформления

- `zen.groups.customer.enabled`
  - **Название**: Сворачивать секцию «Покупатель» (`zen.setting.group_enabled` + контекст `zen.group.customer`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Сворачивает блок «Покупатель» и выводит краткую сводку при условии правильного заполнения блока.
  - **Дефолт**: `true`
  - **Статус UI**: нет

- `zen.groups.customer.hide_auth_header`
  - **Название**: Скрыть отображение ФИО и авторизацию в заголовке блока "Покупатель" (ключа в locale нет)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: ФИО покупателя дублируется в сводке блока дзен режима. Данная опция применима только к авторизованным пользователем. 
  - **Дефолт**: `true`
  - **Статус UI**: нет

- `zen.groups.customer.icon`
  - **Название**: Иконка блока «Покупатель» (ключа в locale нет; группа: `zen.group.customer`)
  - **Формат**: Text (string, URL)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Задаёт свою иконку для свёрнутого блока «Покупатель».
  - **Дефолт**: `''`
  - **Статус UI**: нет

- `zen.groups.customer.summary_template`
  - **Название**: Шаблон сводки: Покупатель (`zen.template.customer`)
  - **Формат**: Textarea (Smarty-template string)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Задаёт текст краткой сводки в свёрнутом блоке «Покупатель».
  - **Дефолт**: `{if $company}{$company} • {/if}{$firstname} {$lastname} • {$phone}`
  - **Статус UI**: нет

- `zen.groups.delivery.enabled`
  - **Название**: Сворачивать блок «Доставка» (`zen.setting.group_enabled` + `zen.group.delivery`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Сворачивает блок Доставка и выводит краткую сводку при условии правильного заполнения блока.
  - **Дефолт**: `true`
  - **Статус UI**: нет

- `zen.groups.delivery.icon_source`
  - **Название**: Иконка секции «Доставка» (`zen.icon_source.delivery`); варианты: `zen.icon_source.plugin_logos`, `zen.icon_source.default_icons`
  - **Формат**: Select - сменил с radio
  - **Варианты**:
    - `plugin` — «Логотипы плагинов» (`zen.icon_source.plugin_logos`), подсказка `zen.icon_source.plugin_logos.hint`
    - `default` — Стандартная иконка» (`zen.icon_source.default_icons`), подсказка `zen.icon_source.default_icons.hint`
  - **Без иконок вообще**: отдельного значения здесь нет — выключается глобально через `zen.icon_display` = `none` (см. выше).
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Выбирает источник иконки для свёрнутого блока «Доставка».
  - **Дефолт**: `plugin`
  - **Статус UI**: уже есть

- `zen.groups.delivery.icon`
  - **Название**: Кастомная иконка группы «Доставка» (ключа в locale нет; группа: `zen.group.delivery`)
  - **Формат**: Text (string, см. `zen.groups.customer.icon`)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Задаёт свою иконку для свёрнутого блока «Доставка».
  - **Дефолт**: `''`
  - **Статус UI**: нет

- `zen.groups.delivery.summary_template`
  - **Название**: Шаблон сводки: Доставка (`zen.template.delivery`)
  - **Формат**: Textarea (Smarty-template string)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Задаёт текст краткой сводки в свёрнутом блоке «Доставка».
  - **Дефолт**: `<strong>{$delivery_plugin}</strong><br />{$shipping_name} • {$shipping_rate}`
  - **Статус UI**: нет

- Пер-инстансные шаблоны сводки — **расположение UI** (`zen.groups.delivery.custom_templates`, `zen.groups.payment.custom_templates`)
  - **Название**: Персональные шаблоны сводки по способам доставки / оплаты (ключа в locale нет; блок UI, см. начало документа)
  - **Формат**: — (не отдельный ключ; навигация по экрану)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления" → под общим `summary_template` в подсекциях **«Доставка»** и **«Оплата»**; контекст — **текущая витрина**
  - **Описание**: Позволяет задать отдельный текст сводки для отдельных способов доставки и оплаты.
  - **Дефолт**: —
  - **Статус UI**: нет (по той же спеке)

- `zen.groups.delivery.custom_templates`
  - **Название**: то же, что блок «Персональные шаблоны сводки» для доставки (см. выше)
  - **Формат**: структура по ID инстанса доставки → `{ active: bool, template: string }` (сериализация при сохранении на усмотрение бекенда). Поведение списка и модального окна — см. **«UI: пер-инстансные шаблоны сводки»**.
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления" → подсекция «Доставка»; см. блок **«Пер-инстансные шаблоны сводки — расположение UI»** выше
  - **Описание**: Задаёт свой шаблон сводки для выбранных способов доставки вместо общего.
  - **Дефолт**: `{}`
  - **Статус UI**: по спеке выше (не свёрнутая textarea JSON, а список инстансов и модальное окно)

- `zen.groups.payment.enabled`
  - **Название**: Сворачивать группу «Оплата» (`zen.setting.group_enabled` + `zen.group.payment`)
  - **Формат**: Switcher (boolean)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Позволяет сворачивать блок «Оплата» в дзен-режиме.
  - **Дефолт**: `true`
  - **Статус UI**: нет

- `zen.groups.payment.icon_source`
  - **Название**: Иконка секции «Оплата» (`zen.icon_source.payment`); варианты: `zen.icon_source.plugin_logos`, `zen.icon_source.default_icons`
  - **Формат**: Radio
  - **Варианты**:
    - `plugin` — «Логотипы плагинов» (`zen.icon_source.plugin_logos`), подсказка `zen.icon_source.plugin_logos.hint`
    - `default` — «Дефолтные иконки» (`zen.icon_source.default_icons`), подсказка `zen.icon_source.default_icons.hint`
  - **Без иконок вообще**: отдельного значения здесь нет — выключается глобально через `zen.icon_display` = `none` (см. выше).
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Выбирает источник иконки для свёрнутого блока «Оплата».
  - **Дефолт**: `plugin`
  - **Статус UI**: уже есть

- `zen.groups.payment.icon`
  - **Название**: Кастомная иконка группы «Оплата» (ключа в locale нет; группа: `zen.group.payment`)
  - **Формат**: Text (string, см. `zen.groups.customer.icon`)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Задаёт свою иконку для свёрнутого блока «Оплата».
  - **Дефолт**: `''`
  - **Статус UI**: нет

- `zen.groups.payment.summary_template`
  - **Название**: Шаблон сводки: Оплата (`zen.template.payment`)
  - **Формат**: Textarea (Smarty-template string)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления"
  - **Описание**: Задаёт текст краткой сводки в свёрнутом блоке «Оплата».
  - **Дефолт**: `<strong>{$payment_name}</strong><br />{$payment_description}`
  - **Статус UI**: нет

- `zen.groups.payment.custom_templates` (планируется)
  - **Название**: Персональные шаблоны сводки по способам оплаты (ключа в locale нет)
  - **Формат**: как у доставки — ID инстанса оплаты → `{ active: bool, template: string }`; UI — тот же паттерн (свитчер, название, подпись, ссылка, модальное окно с переменными)
  - **Вкладка**: Дзен режим -> подгруппа "Блоки оформления" → подсекция «Оплата»; размещение списка и модального окна — см. блок **«Пер-инстансные шаблоны сводки — расположение UI»** выше (тот же экран, зеркально под `zen.groups.payment.summary_template`)
  - **Описание**: Задаёт свой шаблон сводки для выбранных способов оплаты вместо общего.
  - **Дефолт**: `{}`
  - **Статус UI**: нет (после бекенда — по той же спеке, что доставка)

### Технические (в UI не показываем)

- `update_time`
  - **Название**: Время последнего изменения настроек (ключа в locale нет)
  - **Формат**: int (timestamp)
  - **Описание**: Служебно: время последнего сохранения настроек.

- `updated_by`
  - **Название**: Контакт, изменивший настройки (ключа в locale нет)
  - **Формат**: int (contact_id)
  - **Описание**: Служебно: кто сохранил настройки.
