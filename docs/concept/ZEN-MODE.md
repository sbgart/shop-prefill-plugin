# Концепция: Zen Mode (Дзен-режим корзины)

> **Нейминг:** Используем **"Дзен-режим"** в UI и **"zen"** в коде.
> 
> Почему "Дзен"? Это не просто "сделали меньше" — это про:
> - ☮️ **Спокойствие** — не путаем пользователя полями
> - 🎯 **Фокус** — только важное (кнопка "Оформить")
> - ⚡ **Скорость** — не нужно скроллить через 20 полей

---

## Проблема

В корзине Shop-Script много полей для заполнения. Когда поля уже предзаполнены плагином Prefill, показывать их все пользователю избыточно — это создаёт визуальный шум и может "напугать" пользователя обилием форм.

**Цель:** Свернуть секции с предзаполненными данными, показывая только краткую сводку информации.

---

## Решения принятые

| Вопрос                    | Решение                                                                     |
| ------------------------- | --------------------------------------------------------------------------- |
| Для кого сворачивать?     | Для **всех** — и авторизованных, и гостей                                   |
| Как разворачивать?        | Кнопка/ссылка **"Изменить"** через хук в конце каждой секции                |
| Кнопка "Развернуть всё"?  | Пока не нужна в UI, но **функционал нужен** (для разворота при ошибках)     |
| Когда сворачивать?        | Если в чекауте **нет ошибок валидации** — сворачиваем все включенные секции |
| Кастомные поля (details)? | Уже реализовано в основном prefill — переиспользуем                         |

---

## Хуки для секций ✅

Все секции поддерживают хуки `checkout_render_{section_id}`:

| Секция        | CSS-класс                  | Хук                          | Сворачивать? |
| ------------- | -------------------------- | ---------------------------- | ------------ |
| Покупатель    | `wa-step-auth-section`     | `checkout_render_auth` ✅     | ✅ Да         |
| Регион        | `wa-step-region-section`   | `checkout_render_region` ✅   | ✅ Да         |
| Доставка      | `wa-step-shipping-section` | `checkout_render_shipping` ✅ | ✅ Да         |
| Дополнительно | `wa-step-details-section`  | `checkout_render_details` ✅  | ✅ Да         |
| Оплата        | `wa-step-payment-section`  | `checkout_render_payment` ✅  | ✅ Да         |
| Подтверждение | `wa-step-confirm-section`  | `checkout_render_confirm` ✅  | ❌ Нет*       |

> *Секция "Подтверждение" содержит итоговую стоимость, галочку условий и кнопку "Оформить заказ" — её **не нужно сворачивать**.

**Вывод:** Можно использовать хуки для добавления кнопки "Изменить" в каждую секцию.

---

## Механизм сворачивания (CSS)

### Структура секции (упрощённо)

```html
<section class="wa-step-section wa-step-auth-section">
    <header class="wa-section-header">...</header>      <!-- Остаётся видимым -->
    <div class="wa-section-body">                       <!-- Скрывается -->
        <form>...</form>
    </div>
    <div class="wa-plugin-hook prefill-zen-expand">     <!-- Хук с кнопкой — виден всегда -->
        <a href="#">Изменить данные покупателя ▼</a>
    </div>
</section>
```

### Подход через CSS-классы

```css
/* Дзен-режим (свёрнутая секция) */
.wa-step-section.prefill-zen .wa-section-body {
    display: none !important;
}

/* Развёрнутая секция */
.wa-step-section.prefill-zen-expanded .wa-section-body {
    display: block !important;
}

/* Блок сводки — показывается только в дзен-режиме */
.prefill-zen-summary {
    display: none;
}
.wa-step-section.prefill-zen .prefill-zen-summary {
    display: block;
}
```

### JavaScript логика

```javascript
// Инициализация — добавляем класс prefill-zen к секциям
// Клик на "Изменить" — меняем prefill-zen на prefill-zen-expanded
// При ошибках валидации — разворачиваем ВСЕ секции (убираем prefill-zen)
```

### Управление состоянием

Используем **CSS-классы** (`prefill-zen` / `prefill-zen-expanded`) на секцию.

---

## Сводка данных по секции

### Шаблонизация

Администратор может настроить шаблон для каждой секции:

```smarty
{* Секция auth *}
{$firstname} {$lastname}{if $phone} • {$phone}{/if}{if $email} • {$email}{/if}

{* Секция shipping *}
{$shipping_name}
{$address.street}{if $address.building}, д. {$address.building}{/if}
```

### Минимальное оформление

- Иконка секции (опционально)
- Текст сводки (шрифт, размер)
- Ссылка "Изменить" с иконкой стрелки

---

## Настройки плагина

### Новая вкладка "Дзен-режим"

Добавить вкладку в настройки плагина:

```
📋 Предзаполнение    |    🧘 Дзен-режим
```

### Структура настроек

```php
'zen' => [
    'active' => ['value' => false, 'filter' => FILTER_VALIDATE_BOOLEAN],
    
    'sections' => [
        'auth' => [
            'enabled' => ['value' => true, 'filter' => FILTER_VALIDATE_BOOLEAN],
            'expand_text' => ['value' => 'Изменить данные покупателя'],
            'summary_template' => ['value' => '{firstname} {lastname} • {phone} • {email}'],
        ],
        'region' => [
            'enabled' => ['value' => true, 'filter' => FILTER_VALIDATE_BOOLEAN],
            'expand_text' => ['value' => 'Изменить регион'],
            'summary_template' => ['value' => '{city}, {region}'],
        ],
        'shipping' => [
            'enabled' => ['value' => true, 'filter' => FILTER_VALIDATE_BOOLEAN],
            'expand_text' => ['value' => 'Изменить доставку'],
            'summary_template' => ['value' => '{shipping_name}\n{address}'],
        ],
        'details' => [
            'enabled' => ['value' => false, 'filter' => FILTER_VALIDATE_BOOLEAN],
            'expand_text' => ['value' => 'Изменить дополнительные данные'],
            'summary_template' => ['value' => 'Заполнено'],
        ],
        'payment' => [
            'enabled' => ['value' => false, 'filter' => FILTER_VALIDATE_BOOLEAN],
            'expand_text' => ['value' => 'Изменить способ оплаты'],
            'summary_template' => ['value' => '{payment_name}'],
        ],
        // confirm — НЕ сворачивается (содержит итого и кнопку оформления)
    ],
],
```

### UI настроек (примерный макет)

```
┌─────────────────────────────────────────────────────────────────┐
│ ☑ Включить Дзен-режим                                           │
│                                                                  │
│ Скрывает заполненные поля, показывая только краткую сводку.     │
│ Пользователь видит чистую форму без визуального шума.           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│ Секции:                                                          │
│ ┌─────────────┬─────────────────────────────────────────────────┐
│ │ ☑ Покупатель │ Текст ссылки: [Изменить данные покупателя    ] │
│ │              │ Шаблон:       [{firstname} {lastname} • {phone}] │
│ ├─────────────┼─────────────────────────────────────────────────┤
│ │ ☑ Регион     │ Текст ссылки: [Изменить регион                ] │
│ │              │ Шаблон:       [{city}, {region}               ] │
│ ├─────────────┼─────────────────────────────────────────────────┤
│ │ ☑ Доставка   │ Текст ссылки: [Изменить доставку              ] │
│ │              │ Шаблон:       [{shipping_name}                ] │
│ └─────────────┴─────────────────────────────────────────────────┘
│                                                                  │
│ Доступные переменные:                                            │
│ {firstname}, {lastname}, {phone}, {email}, {city}, {region}...   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Обработка ошибок валидации

### Логика

1. Пользователь жмёт "Оформить заказ"
2. Если есть ошибки валидации:
   - **Разворачиваем ВСЕ секции** (убираем `prefill-zen` класс)
   - Показываем ошибки (стандартное поведение Shop-Script)
3. Пользователь исправляет ошибки
4. При следующем посещении страницы — снова сворачиваем (если ошибок нет)

### Реализация

```javascript
// Подписка на событие ошибки
$(document).on('checkout.validation.error', function() {
    $('.wa-step-section.prefill-zen')
        .removeClass('prefill-zen')
        .addClass('prefill-zen-expanded');
});
```

---

## Переменные для шаблонов

> **Примечание:** Переменные извлекаются из данных чекаута Shop-Script. Кастомные поля доступны через синтаксис `{custom.field_id}`.

### Секция auth (Покупатель)

| Переменная            | Описание                       | Пример           |
| --------------------- | ------------------------------ | ---------------- |
| `{firstname}`         | Имя                            | Иван             |
| `{lastname}`          | Фамилия                        | Петров           |
| `{middlename}`        | Отчество                       | Сергеевич        |
| `{phone}`             | Телефон                        | +7 999 123-45-67 |
| `{email}`             | Email                          | ivan@example.com |
| `{company}`           | Название компании              | ООО "Пример"     |
| `{company_inn}`       | ИНН компании                   | 1234567890       |
| `{company_tax}`       | Форма налогообложения          | УСН              |
| `{is_company}`        | Тип: компания или физлицо      | true/false       |
| `{contact_id}`        | ID контакта (если авторизован) | 123              |
| `{service_agreement}` | Согласие с условиями           | true/false       |
| `{custom.*}`          | Кастомные поля auth            | —                |

### Секция region (Регион)

| Переменная      | Описание                       | Пример          |
| --------------- | ------------------------------ | --------------- |
| `{country}`     | Название страны                | Россия          |
| `{country_id}`  | ID страны                      | rus             |
| `{region}`      | Название региона/области       | Московская обл. |
| `{region_id}`   | ID региона                     | 77              |
| `{city}`        | Название города                | Москва          |
| `{city_id}`     | ID города (если справочник)    | 1234            |
| `{zip}`         | Почтовый индекс                | 123456          |
| `{location_id}` | ID локации (минимальный режим) | 5               |

### Секция shipping (Доставка)

| Переменная              | Описание                          | Пример                 |
| ----------------------- | --------------------------------- | ---------------------- |
| `{shipping_name}`       | Название способа доставки         | СДЭК Курьер            |
| `{shipping_type}`       | Тип доставки                      | todoor / pickup / post |
| `{shipping_rate}`       | Стоимость доставки                | 350 ₽                  |
| `{shipping_rate_free}`  | Бесплатная ли доставка            | true/false             |
| `{shipping_date}`       | Дата доставки                     | 25 янв.                |
| `{shipping_service}`    | Название службы доставки          | СДЭК                   |
| `{shipping_variant_id}` | ID выбранного варианта            | cdek_courier_123       |
| `{possible_address}`    | Выбранный ПВЗ/адрес (если pickup) | ПВЗ на ул. Ленина      |

### Секция details (Детали доставки)

| Переменная       | Описание                       | Пример                |
| ---------------- | ------------------------------ | --------------------- |
| `{street}`       | Улица                          | ул. Ленина            |
| `{building}`     | Номер дома                     | 15                    |
| `{apartment}`    | Квартира/офис                  | 42                    |
| `{address_full}` | Полный адрес (собранный)       | ул. Ленина 15, кв. 42 |
| `{zip}`          | Индекс (если в details)        | 123456                |
| `{est_delivery}` | Ориентировочное время доставки | 25 янв.               |
| `{storage_days}` | Срок хранения (для ПВЗ)        | 7 дней                |
| `{custom.*}`     | Кастомные поля доставки        | —                     |
| `{plugin.*}`     | Поля плагинов доставки         | —                     |

### Секция payment (Оплата)

| Переменная              | Описание                      | Пример               |
| ----------------------- | ----------------------------- | -------------------- |
| `{payment_name}`        | Название способа оплаты       | Банковская карта     |
| `{payment_id}`          | ID способа оплаты             | 5                    |
| `{payment_description}` | Описание способа оплаты       | Оплата картой онлайн |
| `{custom.*}`            | Кастомные поля плагина оплаты | —                    |

> **Кастомные поля плагинов оплаты:** Например, для плагина `invoicejur` доступны `{custom.cust_inn}`, `{custom.cust_company}` и др.

### Общие переменные (доступны во всех секциях)

| Переменная      | Описание                   | Пример  |
| --------------- | -------------------------- | ------- |
| `{order_total}` | Итоговая сумма заказа      | 5 350 ₽ |
| `{subtotal}`    | Сумма товаров без доставки | 5 000 ₽ |
| `{discount}`    | Скидка                     | 200 ₽   |
| `{items_count}` | Количество товаров         | 3       |

### Синтаксис шаблонов

```
Базовый:      {firstname} {lastname}
С фильтром:   {phone|default:"не указан"}
Условие:      {if phone}{phone}{else}телефон не указан{/if}
Кастомные:    {custom.my_field_id}
```

> **TODO:** Решить какой уровень сложности шаблонов поддерживать:
> - **Простой:** Только `{variable}` и `{variable|default:"..."}` 
> - **Продвинутый:** Условия `{if}...{/if}`, циклы (для кастомных полей)

---

## План реализации

### Фаза 1: MVP

1. [ ] Добавить настройки `zen` в `storefront.settings.php`
2. [ ] Добавить CSS для сворачивания (`.prefill-zen`)
3. [ ] Добавить кнопку "Изменить" через хуки `checkout_render_*`
4. [ ] JavaScript: переключение классов при клике
5. [ ] Простая сводка без шаблонов (захардкодить)

### Фаза 2: Настраиваемые шаблоны

1. [ ] UI для настройки текста ссылок
2. [ ] UI для настройки шаблонов сводки
3. [ ] Парсер переменных `{variable}`
4. [ ] Документация по доступным переменным

### Фаза 3: Полировка

1. [ ] Интеграция с валидацией (авто-разворот при ошибках)
2. [ ] Анимации сворачивания/разворачивания
3. [ ] Мобильная адаптация
4. [ ] Иконки для секций

---

## Связанные документы

- [CONCEPT.md](CONCEPT.md) — основной концепт плагина
- [VALIDATION.md](VALIDATION.md) — валидация полей

---

## Открытые вопросы

1. **Иконки секций** — использовать emoji, SVG или текст?
2. **Анимация** — CSS transition или JS animation?
3. **SessionStorage** — сохранять ли состояние свёрнутости между обновлениями страницы?
4. **Превью в админке** — показывать ли админу как будет выглядеть сводка?
