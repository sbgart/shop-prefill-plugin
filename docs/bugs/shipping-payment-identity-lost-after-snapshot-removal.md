# Ошибка на шаге `auth` уничтожает выбор доставки и оплаты, а Zen сначала врёт, что всё на месте

**Статус:** ✅ Исправлено 24.08.2026. Правки B и C реализованы, прогнаны в браузере (полный цикл «сломал → починил», перезагрузка, смена региона, гард сворачивания), автотесты зелёные — 166 проверок. При реализации найдена четвёртая ошибка плана — критерий «промолчала» пришлось уточнить, разбор в «Что выяснилось при реализации»
**Приоритет:** 🔴 был блокером релиза — любая жёсткая ошибка шага `auth` стоила покупателю выбора доставки и оплаты, без единого сообщения о потере
**Сложность фикса:** 🔧 средний — `SectionChecker`, `SessionStorageProvider`, `CheckoutHooks`, `ZenModeToggle`, правила Z5/P9
**Связано:** [баг zen-collapse](zen-collapse-on-upstream-checkout-error.md) (сценарии 1/1a/2 переоткрыты), [план снятия снапшота](../plans/snapshot-removal-and-html-ownership.md) (регрессия появилась здесь), [план эхо-кэша payment](../plans/payment-section-echo-cache.md) (механизм обобщается), [issue-86](../codereview/issue-86-delivery-group-source-completeness.md)
**Правила:** [RULES.md](../concept/RULES.md) — Z1, Z2, Z3, Z5, P1, P2, P9, B2a

## Симптом

Группа «Доставка» заполнена и свёрнута (курьер, вариант, адрес), способ оплаты выбран. Покупатель делает **одну** опечатку в email — убирает собачку — и тут же её исправляет. После исправления:

- ни один тип доставки не подсвечен, блока «Варианты доставки» нет;
- способ оплаты не выбран, секция оплаты скрыта целиком;
- адрес пуст.

Всё это надо выбирать заново. **Полная перезагрузка `/order/` не помогает** — данных нет в сессии.

Zen до этого успевает соврать: на запросе с ошибкой группа остаётся **свёрнутой** с полной сводкой из кэша, хотя выбора под ней уже нет на странице. Нажатие «Изменить» разворачивает группу в пустоту.

## Замеры 24.08.2026 (`/order/`, контакт user:1, заказ не создавался)

### Обычный поток: сломал → починил (один сломанный запрос)

| Что мерили | Базовое | После 1 сломанного `update()` | После починки email |
|---|---|---|---|
| `shipping[type_id]` | `todoor` | поля нет вовсе | `""` |
| `shipping[variant_id]` | `33.courier` | поля нет вовсе | поля нет |
| `.wa-type-wrapper.is-active` | 1 (`todoor`) | 0 | **0** |
| `#js-delivery-variants-section` | есть | нет | **нет** |
| выбранный способ оплаты | `20` | нет | **нет** |
| `details[shipping_address][street]` | заполнена | поля нет | **поля нет** |
| `#wa-step-shipping-section` | `block` | `none` | `block` |
| `#wa-step-payment-section` | `block` | `none` | **`none`** |

**Одного сломанного запроса достаточно.** Полная перезагрузка `/order/` после этого: `type_id: ""`, вариантов нет, оплата не выбрана.

Утверждение этапа 3 [плана снятия снапшота](../plans/snapshot-removal-and-html-ownership.md) («Самовывоз остаётся выбранным до и после ошибки») этим замером **опровергнуто**; вероятнее всего, тогда починка проверялась перезагрузкой страницы, а не тем же `update()`.

### Лог того же прогона

```
Payment echo cache updated with confirmed choice   ← запрос со сломанным email
Payment echo cache cleared: customer left payment section empty   ← ЗАПРОС С ПОЧИНКОЙ
```

Эхо-кэш оплаты уничтожается ровно на том запросе, где он был нужен.

### Захват `POST /order/calculate/`

```
error_step_id: "auth"
shipping.html: 1254 символа, inputCount: 0, selectCount: 0, buttonCount: 0, hasDisplayNone: true
```

## Когда это срабатывает — не только опечатка в email

Потерю вызывает **любая** жёсткая ошибка шага `auth`: она кладётся в `$errors`, даёт `can_continue = false`, и [`shopCheckoutStep::processAll()`](wa-apps/shop/lib/classes/checkout2/shopCheckoutStep.class.php#L288) после этого **не вызывает `process()` ни у одного следующего шага** — только `prepare()`, который рендерит пустой шаблон.

| # | Ошибка | Источник | Насколько реально |
|---|---|---|---|
| 1 | Невалидный email (`waEmailValidator`) | [строка 209](wa-apps/shop/lib/classes/checkout2/shopCheckoutAuthStep.class.php#L209) | ✅ **измерено**, опечатка |
| 2 | Email или телефон **принадлежат другому покупателю** (режим `order_without_auth = create_contact`) | `getForbiddenAddress()`, [строка 226](wa-apps/shop/lib/classes/checkout2/shopCheckoutAuthStep.class.php#L226) | ✅ **измерено** 24.08.2026, `id: forbidden_error`; 🔥 рутина для гостя, который уже заказывал |
| 3 | Email или телефон принадлежат **сотруднику бэкенда** | `getAdminErrorFields()`, [строка 221](wa-apps/shop/lib/classes/checkout2/shopCheckoutAuthStep.class.php#L221) | ✅ **измерено** 24.08.2026, `id: admin_error`; владелец магазина заказывает у себя |
| 4 | Email или телефон **забанены** | `getBannedErrorFields()`, [строка 219](wa-apps/shop/lib/classes/checkout2/shopCheckoutAuthStep.class.php#L219) | ✅ **измерено** 24.08.2026, `id: banned_error`; редко |
| 5 | **Товар кончился** (`cart_invalid`) | `getCartErrors()`, [строка 245](wa-apps/shop/lib/classes/checkout2/shopCheckoutAuthStep.class.php#L245) | ✅ **измерено** 24.08.2026 (товар снят с публикации); 🔥 обычное дело; к `auth` отношения не имеет, но отдаётся его шагом |
| 6 | Тип покупателя не выбран / вход как компания при выключенном режиме компаний | [строки 43, 50, 142](wa-apps/shop/lib/classes/checkout2/shopCheckoutAuthStep.class.php#L43) | ✅ **измерено** 24.08.2026 (ранний `return`, отдельная ветка кода); конфигурационный |

Все шесть пунктов измерены 24.08.2026 в браузере (контакты `PrefillTestBuyer`/`PrefillTestOwner`/`PrefillTestBanned`, удалены после прогона) — детали в «Прогон остальных ошибок шага `auth`» ниже. Механизм у всех один, кроме пункта 6 — там отдельная ветка кода (см. там же).

**Граница, которая радует:** пустое обязательное поле сюда **не попадает** — оно кладётся в `delayed_errors` ([строка 134](wa-apps/shop/lib/classes/checkout2/shopCheckoutAuthStep.class.php#L134)), а те `can_continue` не трогают.

## Полная опись: что теряет группа доставки

Проверено построчно 24.08.2026 — от этой таблицы зависит состав эха.

| Данные | Теряется | Кто возвращает |
|---|---|---|
| `region.country/region/city/zip` | нет | region-шаг делает работу в `prepare()`, а тот вызывается всегда. Замерено: 6 инпутов целы |
| `region.possible_address.*` | **да** | никто: скрытые инпуты живут в форме **shipping** ([shipping.html:83-87](wa-apps/shop/templates/actions/frontend/order/form/shipping.html#L83)), хотя пишут в `order.region`. **Принято как известная потеря** — ниша (подсказки адресов у части плагинов доставки), последствие косметическое: дропдаун вернётся в «Укажите адрес» |
| `shipping.type_id` | да, но не нужен | ядро выводит тип из варианта: [ShippingStep:251](wa-apps/shop/lib/classes/checkout2/shopCheckoutShippingStep.class.php#L251) `$selected_type_id = $type['id']`; вход `type_id` читается только при пустом `variant_id` ([:227](wa-apps/shop/lib/classes/checkout2/shopCheckoutShippingStep.class.php#L227)) |
| `shipping.variant_id` | **да** | **эхо** |
| `details.shipping_address.*` | из POST — да | ядро, `details_address` ([DetailsStep:26](wa-apps/shop/lib/classes/checkout2/shopCheckoutDetailsStep.class.php#L26)), аддитивно: `array_filter($input) + $stored`. Подставляется не только в форму, но и в расчёт — [:134](wa-apps/shop/lib/classes/checkout2/shopCheckoutDetailsStep.class.php#L134) `$base_values = $prepare_result['stored_address'] + $base_values` |
| `details.custom.*` — дата и интервал доставки, поля плагина | **да** | **эхо**. Значения берутся только из POST ([DetailsStep:168](wa-apps/shop/lib/classes/checkout2/shopCheckoutDetailsStep.class.php#L168)) и нигде не хранятся; поля с `affects-rate` обязательны, без них тариф не пересчитается |
| `confirm.comment` | нет | замерено (текстарея с «1» пережила) |
| `payment.id` + `custom` | **да** | существующее эхо P9 — после починки критерия (правка B1) |

## Два дефекта, а не один

| | Что именно | Кто виноват | Чем лечится | Приоритет |
|---|---|---|---|---|
| **D1** | Свёрнутый блок обещает выбор, которого нет на странице; «Изменить» ведёт в пустую секцию; гард «Свернуть» слеп к серверным ошибкам | плагин (Z2, Z3) | **Правка C** | 🟠 |
| **D2** | Выбор доставки и оплаты стирается безвозвратно | ядро; плагин до 23.08.2026 это маскировал снапшотом | **Правка B** | 🔴 |

С правкой B сводка перестаёт быть ложью — выбор действительно цел, — и D1 сжимается до «кнопка не работает, пока держится чужая ошибка» плюс отсутствие обратной связи. Поэтому B обязательна, C желательна.

### D2 — регрессия против пред-релизного поведения, а не «как в стоке»

Формально это поведение ядра, и B2 («не бороться с ядром») мог бы закрыть вопрос. Но до 23.08.2026 плагин потерю **чинил** — снапшотом, и собственный замер плана снятия снапшота ([этап 0, п.4](../plans/snapshot-removal-and-html-ownership.md)) это фиксирует: *«Снапшот включается только на следующем запросе... там наблюдались оба `restored from snapshot`»*. Коммит `0ba2315` снял снапшот, обосновав это недостижимостью сценария issue-65 — верно само по себе, но снапшот попутно закрывал **другую** дыру, и связь никто не сверил.

## Почему не спас существующий эхо-кэш payment (P9)

Расхождение в протоколе ядра: **одно и то же «просто перерисуй меня» кодируется двумя значениями**.

| Ветка `getData()` | `shipping` | `details` | `payment` |
|---|---|---|---|
| `disabled` | `'only'` | `'only'` | `'only'` |
| `options.clean` | `'only'` | `'only'` | `'only'` |
| обычный перерендер (`that.reload`) | `'only'` ([1871](wa-apps/shop/js/frontend/order/form.js#L1871)) | `'only'` ([2429](wa-apps/shop/js/frontend/order/form.js#L2429)) | **`1`** ([2660](wa-apps/shop/js/frontend/order/form.js#L2660)) |

`isSectionMechanicallyClean()` проверяет только `=== 'only'`, поэтому для `payment` при коротком замыкании условие ложно → код падает в ветку «покупатель сам оставил пустым» и **стирает** эхо. Это и есть `Payment echo cache cleared` в логе.

## План правки

### Правка B (🔴). Критерий «секция промолчала» + эхо группы доставки

**B1. Починить критерий.** Не список принимаемых значений, а то, что заложено в названии метода: секция не прислала **ни одного собственного поля**.

```php
private const SECTION_INPUT_FIELDS = [
    'auth'     => ['mode', 'user_id', 'data', 'service_agreement'],
    'region'   => ['country', 'region', 'city', 'city_id', 'zip', 'location_id'],
    'shipping' => ['type_id', 'variant_id'],
    'details'  => ['shipping_address', 'custom'],
    'payment'  => ['id', 'custom'],
    'confirm'  => ['comment', 'terms'],
];

public function isSectionMechanicallyClean(string $section_id, array $checkout_params): bool
{
    $section    = $checkout_params['order'][$section_id] ?? null;
    $own_fields = self::SECTION_INPUT_FIELDS[$section_id] ?? null;

    if (!is_array($section) || $own_fields === null) {
        return false;
    }

    // Важно наличие ключа, а не его значение: пустой скрытый инпут — тоже разговор
    return empty(array_intersect_key($section, array_flip($own_fields)));
}
```

Протокол-независимо: переживёт и третье значение `html`, если ядро его заведёт. **Проверять «все ключи, кроме `html`» нельзя** — разбор в «Что выяснилось при реализации».

Побочно: гард `if (!empty($id)) return null;` внутри `syncPaymentEcho()` становится структурно недостижимым (если своих полей нет, то и `id` взяться неоткуда) — оставлен утверждением.

**B2. Эхо группы доставки** — `shop/prefill_delivery_echo`, по образцу существующего payment-эха. Хранит три вещи:

```php
[
    'variant_id' => '33.courier',
    'custom'     => ['desired_delivery' => '...'],          // order.details.custom
    'region'     => ['country' => 'rus', 'region' => '54',  // отпечаток адреса
                     'city' => 'Новосибирск', 'zip' => '630000'],
]
```

Отпечаток региона — **обязательная часть, а не украшение**: без него эхо не отличит починку email от смены города (разбор в «Что перепроверено», п. 1).

```php
public function syncDeliveryEcho(): array
{
    $checkout_params = $this->getCheckoutParams();
    $checker         = $this->getSectionChecker();
    $variant_id      = $checkout_params['order']['shipping']['variant_id'] ?? null;

    if ($checker->isSectionMechanicallyClean('shipping', $checkout_params)) {
        $echo = $this->getDeliveryEcho();
        if ($echo === null) {
            return [];
        }

        // Вариант доставки осмыслен только для адреса, под который выбран: список
        // вариантов, тариф и срок ядро считает от региона. Сменился адрес — выбор
        // недействителен, группа обязана развернуться (Z2, B2a).
        if ($echo['region'] !== $this->getRegionFingerprint($checkout_params)) {
            $this->clearDeliveryEcho();
            shopPrefillPluginLog::debug('Delivery echo dropped: region changed');
            return [];
        }

        $restored = ['shipping' => ['variant_id' => $echo['variant_id']]];

        // custom пишем, только если details тоже промолчала: иначе перезапишем
        // то, что покупатель прислал в этом же запросе (P1)
        if (!empty($echo['custom']) && $checker->isSectionMechanicallyClean('details', $checkout_params)) {
            $restored['details'] = ['custom' => $echo['custom']];
        }

        $this->setCheckoutParams(
            shopPrefillPluginHelper::deepMergeArrays($checkout_params, ['order' => $restored])
        );
        shopPrefillPluginLog::debug('Delivery section restored from echo cache', [
            'variant_id' => $echo['variant_id'],
        ]);

        return $restored;
    }

    // Секция говорила сама за себя — это её настоящее состояние
    if (!empty($variant_id)) {
        $this->saveDeliveryEcho([
            'variant_id' => $variant_id,
            'custom'     => $checkout_params['order']['details']['custom'] ?? [],
            'region'     => $this->getRegionFingerprint($checkout_params),
        ]);
    } elseif ($this->getDeliveryEcho() !== null) {
        // Покупатель сменил тип и ещё не выбрал вариант — прежний выбор больше не его
        $this->clearDeliveryEcho();
        shopPrefillPluginLog::debug('Delivery echo cleared: customer left shipping section empty');
    }

    return [];
}
```

Отпечаток — четыре скаляра из `order.region`, сравнение после `trim()`. `region` **никогда** не приходит секцией `clean` (проверено, см. «Что перепроверено», п. 4), так что сравнивать всегда есть с чем.

**B3. Два письма, а не одно.** Записи в `shop/checkout` **недостаточно** — `calculateAction()` строит `$input` из POST, сессию не читает. Восстановленное надо ещё и влить в живой `$data['input']`, как это уже делает payment-эхо ([CheckoutHooks:89-95](wa-apps/shop/plugins/prefill/lib/classes/hooks/shopPrefillPluginCheckoutHooks.class.php#L89)):

```php
// handleCheckoutBeforeAuth(), рядом с syncPaymentEcho()
foreach ($this->session_storage->syncDeliveryEcho() as $section => $values) {
    if (isset($params['data']['input'])) {
        $params['data']['input'][$section] = shopPrefillPluginHelper::deepMergeArrays(
            $params['data']['input'][$section] ?? [],
            $values
        );
    }
}
```

Каждое письмо закрывает свой путь, и оба нужны:

| Путь | Откуда берётся `$input` | Что срабатывает |
|---|---|---|
| `POST /order/calculate/` | `waRequest::post()` ([:17](wa-apps/shop/lib/actions/frontend/order/shopFrontendOrder.actions.php#L17)) | инъекция в `$data['input']` |
| Загрузка `/order/` | сессия ([`formVars():429`](wa-apps/shop/lib/classes/checkout2/shopCheckoutViewHelper.class.php#L429)) | запись в сессию |
| `use_session_input` после `fast_render` | сессия ([:22](wa-apps/shop/lib/actions/frontend/order/shopFrontendOrder.actions.php#L22)) | запись в сессию |

Побочно это чинит и «перезагрузка не помогает»: на загрузке страницы `$input` берётся из сессии, эхо успевает её долить.

**B4. Сброс.** `DELIVERY_ECHO_KEY` чистится там же, где сегодня `clearPaymentEcho()`: создание заказа, `clear_form`, отзыв согласия. Чистить только при непустом эхо (как в существующем коде) — лишняя запись в сессию поднимает `Set-Cookie: PHPSESSID` (P5, регресс issue-53).

### Правка C (🟠). Гард сворачивания должен знать про серверные ошибки

Сегодня [`ZenModeToggle.collapseGroup()`](wa-apps/shop/plugins/prefill/js/modules/ZenModeToggle.js#L92) спрашивает только клиентскую валидацию своей группы:

```js
var hasErrors = this.validateSections(form, sections);   // только JS-валидаторы, только своя группа
```

Две дыры:

- **Серверные ошибки невидимы.** `ivan@test.123` проходит JS-регулярку и падает на `waEmailValidator`. Гард ошибок не видит → сворачивает **поверх живого сообщения об ошибке** (оно внутри формы, наш CSS его прячет) → `form.update()` → сервер отвечает ошибкой → Z1 разворачивает обратно. Покупатель видит мигание без объяснения. То же со всеми шестью ошибками из таблицы выше.
- **Про чужую группу не сказано ничего.** Ошибка в «Покупателе», покупатель сворачивает «Доставку» — гард смотрит только секции доставки, там чисто, сворачивает молча.

Правка минимальная: плагин уже знает обе вещи серверно (`getErrorStepId()` + `getRegularErrors()` в [`shopPrefillCheckoutState`](wa-apps/shop/plugins/prefill/lib/classes/checkout/shopPrefillCheckoutState.class.php#L623), `getRegularErrors()` уже отфильтровывает маркер `fast_render`). Достаточно прокинуть в JS-инициализатор флаг «оформление сейчас заблокировано» + id группы с ошибкой, и в `collapseGroup()` показать **существующий** диалог (`showValidationErrorDialog`) с честной причиной вместо молчаливого сворачивания. Цена: один флаг в генерируемом инициализаторе, одна ветка в JS, одна строка локали.

**Направление «Изменить» — пересмотрено 24.08.2026 после прогона.** Планировалось не трогать: данные целы (правка B), покупатель увидит пустую секцию, после починки всё вернётся. Вживую это оказалось ровно той паникой «данные пропали», которой мы избегаем: клик по «Изменить» при активной ошибке разворачивает группу, где ядро не отрисовало **ни типов доставки, ни адресных полей** — вместо своей заполненной доставки покупатель видит пустоту. Плюс кука `expanded` залипает до конца сессии из-за действия, которое ничего не дало.

Поэтому гард поставлен на **оба** направления, одним и тем же флагом. Атрибут нужен в двух местах: на кнопке и на самой карточке-обёртке (`CollapseBlock.html` вешает `js-prefill-zen-toggle` на весь блок, клик по сводке тоже разворачивает).

### Правка D. Документация

- **Z5** — убрать «устойчивость установлена замером, а не выведена из кода»: выведена. Записать правду: `variant_id` и `payment.id` короткое замыкание **не переживают** (ядро пересоздаёт JS-контроллер секции на каждом рендере, [form.js:3739](wa-apps/shop/js/frontend/order/form.js#L3739)); устойчивость обеспечивает эхо, а не природа полей.
- **Z2** — дополнить: «данных нет» и «данные не спрашивали» — разные ответы, и различает их только состав POST секции.
- **P9** — расширить на группу доставки, исправить описание протокола (`'only'` **и** `1`), добавить условие отпечатка региона как обязательное для доставки (у оплаты его нет намеренно — там совместимость проверяет ядро на `/order/create/`).
- [zen-collapse](zen-collapse-on-upstream-checkout-error.md) — сценарии 1/1a/2 переоткрыть со ссылкой сюда.
- [план снятия снапшота](../plans/snapshot-removal-and-html-ownership.md) — пометка стоит.

## Как эхо соотносится со снапшотом

Да, по духу это снапшот — теневая копия, переживающая замену сессии. Отличия, которые делают его допустимым там, где снапшот был отвергнут:

| | Снапшот (снят 23.08.2026) | Эхо |
|---|---|---|
| Что хранит | весь `shop/checkout`, шесть секций | три ключа: `variant_id` + `custom` + отпечаток региона; отдельно `payment.id` + `custom` |
| Когда пишет | на каждом запросе | только когда секция говорила сама за себя |
| Когда восстанавливает | когда секция «не занята покупателем» (`canPrefillSection`) | только когда секция прислала **единственный** ключ `html` |
| Самоинвалидация | нет, жил до конца сессии | да: сменился регион → выброшено; секция сама сказала «пусто» → выброшено |
| Конфликт с P2 | был: возвращал стёртое покупателем | нет — см. ниже |

**Почему конфликта с P2 больше нет.** Секция, которая отрисовалась, всегда шлёт хотя бы одно своё поле: скрытый `shipping[type_id]` живёт в блоке типов и сериализуется даже пустым. Значит «покупатель очистил выбор» приезжает как `{type_id:'', html:'only'}` — два ключа, не «промолчала», эхо не восстанавливает, а очищается. Пустой POST из одного `html` бывает **только** когда секции на странице нет. То есть критерий — не «признак пустоты», а «признак отсутствия секции», и это разные вещи; снапшот их путал, эхо нет.

Проверено и на смене типа доставки: `Shipping.update()` шлёт свою секцию не-`clean` → `{type_id:'pickup', variant_id:'', html}` → секция говорила → эхо **очищается**, прежний вариант не воскресает.

## Что выяснилось при реализации 24.08.2026 — четвёртая ошибка плана

Первая реализация критерия (`array_keys($section) === ['html']`) в браузере **не сработала**: эхо доставки стиралось на каждом запросе, в логе `Delivery echo cleared: customer left shipping section empty`. Диагностический дамп секции показал причину:

```json
"shipping": {"service_agreement": "1", "html": "only"}
```

Секция `shipping` приходит **не пустой** даже когда её на странице нет. Поле `shipping[service_agreement]` рендерит **форма секции `region`** — [region.html:476-484](wa-apps/shop/templates/actions/frontend/order/form/region.html#L476):

```smarty
<input type="hidden" name="shipping[service_agreement]" value="0">
<input class="wa-checkbox" type="checkbox" name="shipping[service_agreement]" value="1" ...>
```

А `region` короткое замыкание переживает — он делает работу в `prepare()`. Значит в любом магазине с включённым согласием на оказание услуг `order.shipping` **никогда** не приходит «только с `html`», и проверка по составу всех ключей всегда даёт ложное «секция говорила».

Это не тема и не сторонний плагин — это ядро, воспроизводится на любой инсталляции с `service_agreement` в настройках чекаута.

**Вывод:** проверять надо не «нет ключей, кроме `html`», а «нет ни одного **собственного** поля секции». Список собственных полей снят с шаблонов и шагов ядра (см. `SECTION_INPUT_FIELDS` в B1); чужие поля в пространстве имён секции он игнорирует по построению. Тезис «отрисованная секция всегда шлёт своё поле» при этом остаётся верным — ошибка была в том, что «своё» я подменил на «любое».

Закреплено регресс-проверкой в `SectionCheckerOwnershipVsDataTest` (блок 13).

## Что перепроверено 24.08.2026 (и три ошибки первых редакций)

**1. Гард по `error_step_id` невозможен в принципе — ошибка редакций 1 и 2.** Восстанавливать надо на запросе-**починке**, где ошибки уже нет; смена региона — тоже запрос без ошибки. Оба шлют `shipping` как `{html:'only'}` байт в байт:

| Запрос | POST для `shipping` | `error_step_id` | Что нужно |
|---|---|---|---|
| сломал email | полный | `auth` | сохранить |
| **починил email** | `{html:'only'}` | нет | **восстановить** |
| сменил регион | `{html:'only'}` | нет | **не восстанавливать** |

Дискриминатор — отпечаток региона, не ошибка.

**2. Записи только в сессию недостаточно — ошибка редакции 3.** `calculateAction():17` — `$input = waRequest::post()`. Сессия в обработку `calculate` не участвует. Нужны оба письма (B3).

**3. Одного `variant_id` мало — ошибка редакции 3.** `details.custom` (дата и интервал доставки) не восстанавливается ничем: значения берутся только из POST, `details_address` покрывает исключительно подмассив `shipping_address`.

**4. `region` никогда не приходит секцией `clean`.** Проверены все вызовы `scope.update()`: сокращённый список секций есть ровно в двух местах — `Shipping.prototype.update` ([:2010](wa-apps/shop/js/frontend/order/form.js#L2010), исключает `payment`) и `onRegionChange` ([:3601](wa-apps/shop/js/frontend/order/form.js#L3601), исключает `shipping` и `payment`). Остальные (`Auth`, `Region`, `Details`, `Payment`, `Confirm`) зовут `scope.update()` без опций → все секции сериализуются полностью. Значит отпечаток всегда есть с чем сравнить, ложных срабатываний «регион пуст» не будет.

**5. Ядро выводит тип из варианта — подтверждено у источника,** а не по ссылке на issue-86: [ShippingStep:251](wa-apps/shop/lib/classes/checkout2/shopCheckoutShippingStep.class.php#L251). Там же видно самокоррекцию: `is_selected` считается сопоставлением с заново рассчитанным для текущего адреса списком, не нашлось → вариант не выбран. То есть даже при промахе отпечатка ядро уронит вариант, которого нет в новом городе; отпечаток нужен для случая, когда id совпадает в обоих городах.

**6. Адрес восстанавливается в расчёт, а не только в форму:** [DetailsStep:134](wa-apps/shop/lib/classes/checkout2/shopCheckoutDetailsStep.class.php#L134).

**7. Порядок с предзаполнением безопасен.** На запросе с молчащей секцией `order.shipping = {html:'only'}` — ключ `html` присутствует, значит `canPrefillSection('shipping')` считает секцию занятой и `applyPrefill()` её не трогает. Эхо и предзаполнение не конкурируют; эхо вызывается после, как и payment-эхо.

## Отвергнутые варианты

**1. Вернуть общий снапшот.** Отвергнут вместе со снятием снапшота 23.08.2026 и по той же причине: слепое восстановление по признаку пустоты не отличает «ядро замкнуло конвейер» от «покупатель очистил». Эхо на критерии «секция не прислала ни одного своего поля» это различает — разбор выше.

**2. Правка ядра (`form.js`, привести `payment[html]` к `'only'`).** Нарушает B1 — плагин ставится на чужие инсталляции. Плюс не чинит `shipping`: там теряется не значение протокола, а сами скрытые поля.

**3. Клиентская починка: JS запоминает выбор в `localStorage`.** Гонки с перерисовкой секций, зависимость критичной для оформления функциональности от JS, третий источник состояния вне сессии.

**4. Детектор недоверия к рендеру, разворачивающий группу при чужой ошибке (правка A прошлых редакций).** Отброшен как лишний: с эхом сессия перестаёт врать, и **существующая** проверка `isGroupMinimumFilled()` сама даёт оба нужных поведения — выбор был → свёрнуто с правдивой сводкой; выбора не было → эхо пусто → развёрнуто, и пустая секция там уместна. Детектор был протезом под испорченную сессию. Замер показал и второе: при коротком замыкании ядро прячет `#wa-step-shipping-section` целиком, поэтому развёрнутая группа показывает **не** «незаполненные поля», а пустоту — цели «покупатель видит, что где-то не заполнено» разворот не достигает. Эту цель закрывает правка C.

**5. Только правка C, без B.** Сообщение вместо потери — но потеря остаётся.

**6. Покрыть эхом `region.possible_address`.** Отклонено: ниша, последствие косметическое (дропдаун подсказок вернётся в исходное состояние), а объём эха заметно вырастет.

## Как воспроизвести

```js
var i = document.querySelector('input[name="auth[data][email]"]');
var good = i.value;
i.type = 'text'; i.value = 'broken.address.without.at'; i.classList.remove('wa-error');
window.waOrder.form.update({});          // один сломанный запрос
// подождать ответ
i.type = 'email'; i.value = good;
window.waOrder.form.update({});          // починка — и выбор уже потерян
```

Проверять после второго шага: `document.querySelectorAll('.wa-type-wrapper.is-active').length` (должно быть 1, будет 0), наличие `#js-delivery-variants-section`, `.js-method-field:checked`. Следов в БД не остаётся, заказ не создаётся.

## Что реализовано 24.08.2026

| Правка | Файл | Что сделано |
|---|---|---|
| B1 | `shopPrefillPluginSectionChecker` | `SECTION_INPUT_FIELDS` + переписанный `isSectionMechanicallyClean()` |
| B1 | там же | Docblock `GROUP_MINIMUM_SECTIONS` приведён к правде: устойчивость даёт эхо, а не природа полей |
| B2 | `shopPrefillPluginSessionStorageProvider` | `DELIVERY_ECHO_KEY`, `getRegionFingerprint()`, `syncDeliveryEcho()`, `get/save/clearDeliveryEcho()` |
| B3 | `shopPrefillPluginCheckoutHooks` | `applyEchoToInput()` — общая проводка обоих эхо в живой `$data['input']` |
| B4 | `OrderHooks`, `FrontendConsent`, `FrontendClearStorage`, `resetAndRefill()` | сброс эха доставки вместе с остальным состоянием |
| C | `shopPrefillCheckoutState` | `STEP_TO_GROUP` + `getBlockingGroup()` (шаг `confirm` исключён) |
| C | `ZenMode`, `CollapseBlock.html`, `ToggleButton.html` | `data-blocked-by` на обеих кнопках и на карточке-обёртке |
| C | `ZenModeToggle.js` | гард в `collapseGroup()` **и** `expandGroup()`, `showCheckoutBlockedDialog()`, общий `showNoticeDialog()` |
| C | локали ru/en | `zen.blocked.title`, `zen.blocked.message`; в JS передаются названия групп |
| — | `SectionCheckerOwnershipVsDataTest` | блок 13 переписан, добавлен регресс-щит на `service_agreement`. 166 проверок |

Прогон в браузере 24.08.2026 (контакт user:1, заказ не создавался):

| Сценарий | Результат |
|---|---|
| Сломал email → починил | ✅ тип, вариант, метка варианта, оплата и адрес на месте. Лог: `Delivery section restored from echo cache`, `Payment section restored from echo cache` |
| Полная перезагрузка `/order/` после этого | ✅ состояние держится (`formVars()` читает сессию) |
| Запрос с активной ошибкой email | ✅ `data-blocked-by="customer"` на кнопках «Свернуть» обеих групп |
| Клик «Свернуть» на группе доставки при ошибке в «Покупателе» | ✅ диалог «Сначала нужно исправить ошибку» → «В разделе «Покупатель» есть ошибка…» |
| Смена города (Ишим → Казань) | ✅ `Delivery echo dropped: region changed`, доставка не выбрана, `prefill-zen-styles-delivery` отсутствует — группа развёрнута |

Отдельный прогон **со свёрнутыми группами** — исходный сценарий жалобы (первый прогон шёл на развёрнутых, что было пробелом):

| Сценарий | Результат |
|---|---|
| Свернул «Доставку» и «Оплату», сломал email | ✅ обе остаются свёрнутыми, сводка полная: «Бесплатная доставка курьером • Бесплатно, Ишим, Дзержинского/улица Брестская пр-кт» |
| Ещё три `update()` подряд без починки | ✅ не деградирует. В логе на каждом: `Delivery section restored from echo cache` + `Zen summary rendered from cache`. **Ни одного `expanded: nothing to summarize yet`** — исходный симптом не воспроизводится |
| Починка email | ✅ группы остались свёрнутыми, сводки актуальны, в сессии `variant_id` и `payment.id` |
| «Изменить» на свёрнутой группе без ошибок | ✅ разворачивается в полностью заполненную форму: типы, варианты, `todoor` подсвечен, адрес на месте |
| «Изменить» при активной ошибке email | ⚠️ **до доработки** разворачивал в пустоту → гард распространён на разворот, теперь диалог |
| Клик по телу свёрнутой карточки при ошибке | ✅ тоже перехвачен (обёртка несёт свой `data-blocked-by`) |

## Прогон остальных ошибок шага `auth` — 24.08.2026

Строка 1 таблицы «Когда это срабатывает» была измерена опечаткой в email при первой реализации; строки 2–6 оставались выведены из кода. По запросу — отдельный прогон каждой на живом стенде, все под контактом `PrefillTestBuyer` (person, зарегистрирован через «Создать аккаунт покупателя», авторизован на витрине), с базовым состоянием `variant: 33.courier`, `payment: 16`, адрес заполнен.

Подготовка: два вспомогательных контакта — `PrefillTestOwner` (пароль есть → не «buyer», для п. 2) и `PrefillTestBanned` (`is_user = -1` напрямую в БД, для п. 4). Email админа (`artem.shamberger@gmail.com`, `is_user = 1`) — для п. 3. Все три контакта удалены безвозвратно по завершении прогона.

| # | Сценарий | Триггер | `errors[0].id` | Итог |
|---|---|---|---|---|
| 2 | `auth[data][email]` = email контакта `PrefillTestOwner` | `order_without_auth = create_contact` (текущая настройка магазина, без правки) | `forbidden_error` | ✅ короткое замыкание, `variant`/`payment`/адрес восстановлены после починки email |
| 3 | `auth[data][email]` = email админа | — | `admin_error` | ✅ то же |
| 4 | `auth[data][email]` = email `PrefillTestBanned` | — | `banned_error` | ✅ то же |
| 5 | `UPDATE shop_product SET status = 0` на товаре в корзине, затем `update({})` | — | `cart_invalid`, `section: cart` | ✅ то же после `status = 1` |
| 6 | Настройка «Кто может оформлять заказы» временно переключена на «Организации» (был «Все») | Авторизован как физлицо | — (ранний `return` до `getConfirmationErrors()`, `fields: null`) | ✅ сообщение «Вы авторизованы как физическое лицо, но заказ могут оформить только организации»; `variant`/`payment` пережили и смену настройки, и попутную смену авторизованного контакта (бэкенд-логин в соседней вкладке переключил сессию витрины на admin — «одна сессия» подтвердилась ещё раз) |

Во всех пяти случаях — тот же короткий цикл, что и в замере с email-опечаткой: сразу после ошибки `#wa-step-shipping-section`/`#wa-step-payment-section` уходят в `none`, лог пишет `Delivery section restored from echo cache` + `Payment section restored from echo cache` на запросе-починке, `variant_id`/`payment.id`/адрес возвращаются без деградации при нескольких `update()` подряд.

Пункт 6 подтверждает, что эхо не привязано к конкретному типу ошибки и не привязано к тому, что «сломанная» и «чинящая» секция — один и тот же покупатель: критерий `isSectionMechanicallyClean()` смотрит только на состав POST, а не на причину короткого замыкания или личность автора запроса.

## Проверять после фикса

| # | Сценарий | Ожидание | Правило | Правка | Итог 24.08.2026 |
|---|---|---|---|---|---|
| 1 | Сломал email → починил (обычный поток) | Доставка, вариант, адрес и оплата **на месте** | — | B | ✅ |
| 2 | То же + перезагрузка `/order/` | Всё на месте (эхо доливает сессию до `formVars`) | — | B3 | ✅ |
| 3 | Сломанный email, два-четыре `update()` подряд | Не деградирует, эхо не самоуничтожается | Z5 | B | ✅ на свёрнутых группах |
| 4 | Товар в корзине кончился (`cart_invalid`) | То же, что 1 | — | B | ✅ (товар снят с публикации вместо обнуления остатка — на этой инсталляции нет SKU с учётом остатков) |
| 5 | Покупатель вводит email существующего покупателя (`create_contact`) | То же, что 1 | — | B | ✅ авторизованным контактом, не гостем (`getForbiddenAddress()` требует `isAuth()`) |
| 6 | **Смена региона**, доставка была выбрана | Доставка **разворачивается**, старый вариант не подставляется; в логе `Delivery echo dropped: region changed` | Z2, B2a | B2 (негативная) | ✅ Ишим → Казань |
| 7 | Смена типа доставки (Курьер → Самовывоз) | Прежний вариант не воскресает; оплата ведёт себя по P9 | P2, P9 | B (негативная) | ⬜ |
| 8 | Первая отрисовка `/order/` (`fast_render`) | Группа свёрнута, сводка из кэша, **скачка вёрстки нет** | R4 | — (негативная) | ⬜ |
| 9 | Покупатель сам сменил тип и не выбрал вариант, затем чужая ошибка | Группа **развёрнута** — эхо очищено, восстанавливать нечего | Z2 | B2 | ⬜ |
| 10 | Свернуть группу с серверной ошибкой (`ivan@test.123`) | Диалог с причиной, мигания нет | Z1 | C | ⬜ |
| 11 | Свернуть чужую группу при заблокированном оформлении | Диалог сообщает, где проблема | Z1 | C | ✅ |
| 11a | **Развернуть** («Изменить») при заблокированном оформлении | Диалог, а не разворот в пустоту | Z1, Z3 | C | ✅ после доработки |
| 11b | Клик по телу свёрнутой карточки при заблокированном оформлении | То же — обёртка тоже несёт флаг | Z1 | C | ✅ |
| 11c | Свёрнутая группа при ошибке email | Остаётся свёрнутой, сводка полная и правдивая | Z2, R4 | B | ✅ |
| 12 | Пустое обязательное поле в `auth` | Ничего не теряется (`delayed_errors`, не короткое замыкание) | — | граница | ⬜ |
| 13 | Гость, чистая банка кук (curl) | Эхо не поднимает сессию и куку на пустом месте | P5 | B4 | ⬜ |
| 14 | Заказ оформлен | Оба эхо-ключа сброшены вместе с остальным состоянием | — | B4 | ⬜ |
| 15 | Магазин с `details.custom` (дата/интервал доставки) | Дата и интервал переживают сценарий 1 | — | B2 | ⬜ плагинов доставки с такими полями на этой инсталляции нет |

Сценарии 6, 7, 8 — негативные: их нельзя сломать этим фиксом, ради 6 и введён отпечаток региона.
