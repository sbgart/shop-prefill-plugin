# Тесты

Проверки привязаны к инвариантам из [concept/RULES.md](../concept/RULES.md) — в скобках у каждой стоит правило, которое она защищает.

**Этот файл — журнал: что уже проверено, когда и с какими граблями.** Что и в каком объёме проверять перед релизом — в [TEST-PLAN.md](TEST-PLAN.md): реестр функциональных областей, матрица конфигураций стенда, критерии выпуска и шаблон отчёта.

## Автотесты

Обычные PHP-скрипты без зависимостей от Webasyst. Ненулевой код возврата = провал.

```bash
cd wa-apps/shop/plugins/prefill
for t in tests/*Test.php; do php "$t" || echo "ПРОВАЛ: $t"; done
```

| Файл | Что закрывает |
|---|---|
| `tests/SectionCheckerOwnershipVsDataTest.php` | S1, S2, P1, P2, N2, Z2, Z5 — разделение «принадлежит покупателю» и «есть данные», минимум группы для дзен-режима; 166 проверок: структурный инвариант «списки различаются ровно на `html`», дословная сверка списка владения (его расширение до всей секции — фикс [issue-65](../codereview/issue-65-prefill-overrides-current-input.md), тогда же переписываются блоки 7-8), короткое замыкание и самовывоз без адресных полей. Блок 9: минимум `delivery` — `shipping.variant_id`, а не `type_id` (тип без варианта — открытая вкладка, не выбор); включает случай `variant_id = '37.0'` (самовывоз, `rate_id` = `'0'`). Блок 13: `isSectionMechanicallyClean()` — признак «секция не прислала своих полей» для эхо-кэшей (P9), с регресс-щитом на `shipping[service_agreement]` — поле чужой формы в пространстве имён секции |
| `tests/FillParamsShippingVariantIdTest.php` | Разбор `variant_id` на `shipping_id` + `rate_id` |
| `tests/FillParamsSameDeliveryOptionTest.php` | Схлопывание одинаковых вариантов доставки |
| `tests/CheckoutPageDetectorTest.php` | A1 — ассеты только там, где рендерится форма заказа: матрица маршрутов витрины, приоритет сработавшего checkout-хука над маршрутом, лень чтения маршрута ([issue-64](../codereview/issue-64-assets-loaded-on-every-page.md)) |
| `tests/FillParamsCollectionLimitTest.php` | Настройка «сколько вариантов показывать»: клампинг диапазона 1–10 и фоллбэк на умолчание для мусора и нуля ([issue-68](../codereview/issue-68-params-choice-collection-n-plus-1.md)) |
| `tests/ShippingCustomDetailsOwnershipGateTest.php` | P1 — кросс-секционная запись `shipping`→`details.custom` спрашивает владение `details` отдельным гейтом, а не только `canPrefillSection('shipping')`; независимость гейтов и то, что `applyDeliveryAddress()` (явный выбор, без чекера) пишет всегда ([issue-60](../codereview/issue-60-cross-section-write-details-custom.md)) |
| `tests/CheckoutStateFastRenderTest.php` | `isFastRender()` (сентинел `fast_render` не путается с реальной недоступностью варианта) и `isStepSkipped()` — B2b, различает «шаг не считался» (`vars.<шаг> === []`) от «шаг выключен настройками» (`disabled`) и от «шаг отработал, но пуст» (`methods: []`, ловушка D1); регресс-щит на первую редакцию фикса, где критерий был `empty($vars.payment.methods)` ([баг пропажи карточки оплаты](../bugs/payment-zen-card-vanishes-on-shortcircuit.md)) |
| `tests/ZenGroupCarrierTest.php` | B2b, Z3 — `shopPrefillPluginZenMode::GROUP_CARD_SECTION`: структурный замок (носитель объявлен для каждой группы и входит в её же `GROUP_SECTIONS`), носитель `delivery` дословно `details` (щит от «упрощения» до `end(GROUP_SECTIONS[...])`); `generateGroupStyles()`/`generateSectionRevealStyles()` — счёт правил, показывающее правило только для секции-носителя (щит от раскрытия `region`/`shipping` вместе с заголовком «Регион») |
| `tests/GeoSyncDecisionTest.php` | G1, G1a, P1, P2 — таблица решений гео-синхронизации: вакуум занимаем, первую встречу перезаписываем один раз, расхождение с нашей записью = действие покупателя (отступаем); подпись состояния без индекса и без учёта регистра, перенос слепка через массив (кука и сессия). 18 проверок |
| `tests/GeoIntegrationsAvailabilityTest.php` | Тумблер интеграции без установленного плагина не действует: доступность плагина перекрывает хранимое значение, `hasAnyCityProvider()` не считает `dp` поставщиком города, `sanitize()` гасит `true` отсутствующего плагина и не трогает частичные сохранения без ветки `prefill`. 15 проверок |

Тест обязан уметь падать. Проверяется мутацией: внести регресс, убедиться что тест краснеет, откатить.

### Гость не создаёт следов (P5, P8)

Одна команда, годится и для CI:

```bash
curl -sk -o /dev/null -D - https://wa-dev.loc/ | grep -i '^set-cookie'
```

Ожидание: только `landing` от ядра. Появились `prefill_guest_token` или `prefill_consent` — регресс маркера или куки гостя.
Проверено 19.08.2026: ✅ только `landing`.
**С 26.08.2026 на этой инсталляции добавился `PHPSESSID` — его ставит плагин «SEO-регионы», а не мы** (проверено его выключением, см. блок про интеграцию ниже). Наш критерий сузился до `prefill_*`.

### Ассеты не висят на каталоге (A1)

Тоже одной командой и тоже годится для CI:

```bash
test $(curl -sk https://wa-dev.loc/ | grep -c 'plugins/prefill') -eq 0 && echo OK || echo REGRESS
```

Ожидание: на главной и в карточке товара — ноль ссылок на `plugins/prefill`; на `/order/` они есть.
Проверено 20.08.2026: ✅ 0 на каталоге (было 11), 14 на `/order/` с товаром в корзине.

## Браузерные проверки

Доступы, адреса витрины и грабли — в `CLAUDE.md` в корне проекта. Общий прогон предзаполнения — [issue-63-browser-test-runbook.md](issue-63-browser-test-runbook.md).

### Ассеты на чекауте живые — статус: ✅ пройдено 20.08.2026

Девять кликовых сценариев с результатами — в разделе «Браузерный прогон»
[issue-64](../codereview/issue-64-assets-loaded-on-every-page.md). Ключевой — №7: гость при
выключенном `prefill.guest.enabled` разворачивает свёрнутую группу кнопкой «Изменить» (Z3);
до фикса на этой странице JS не подключался вовсе.

Настройки, которые трогались и возвращены в исходное: `styles.custom_css`,
`prefill.guest.enabled`, `prefill.debug_panel` глобальной витрины `*`.

### Диалог читается в обеих темах (A2) — статус: ✅ пройдено 20.08.2026

Кнопка «Да, очистить» в диалоге подтверждения. Тему в браузере переключать необязательно —
достаточно подменить `color-scheme` у корня и снять computed style **следующим кадром**
(в том же тике `background-color` ещё старый):

```js
document.documentElement.style.colorScheme = 'light';
await new Promise(r => setTimeout(r, 500));
const s = getComputedStyle(document.querySelector('.prefill-dialog__confirm-btn'));
[s.color, s.backgroundColor];   // цвета обязаны различаться
```

Тёмная: `rgb(0,0,0)` на `rgb(255,255,255)`. Светлая: `rgb(255,255,255)` на `rgb(201,49,49)`.

### Разделение ownership / data — статус: ✅ пройдено 19.08.2026

Правка затрагивает `canPrefillSection()` и восстановление из снапшота, поэтому проверялись обе стороны: не вернулось ли стёртое и не пропало ли восстановление.

| # | Сценарий | Ожидание | Правило | Итог |
|---|---|---|---|---|
| 1 | Стереть комментарий к заказу | Поле пустое после `update` и после полной перезагрузки | P2 | ✅ |
| 2 | Стереть улицу в «Адресе доставки» | То же | P2 | ✅ |
| 3 | Стереть email, оставив телефон | Email пуст, телефон на месте, секция не перезаполнилась | P1, P2 | ✅ |
| 4 | Стереть город в секции «Регион» | Поле пустое | P2 | ⚠️ город возвращается — **ядром**, не плагином, см. ниже |
| 5 | Загрузка `/order/` при живой сессии | Все секции предзаполнены как раньше | P3 | ✅ |
| 6 | Сессия перезаписана обеднённым POST | Снапшот вернул улицу и доставку | N1, N3 | ✅ |
| 7 | Лог `wa-log/prefill.plugin.log` | `skipped: belongs to customer` вместо прежних `already filled` | S1 | ✅ |

Во всех сценариях лог показывал `skipped: belongs to customer` — предзаполнение к правленным секциям не притрагивалось.

**Про сценарий 4.** Город очищается в поле, но на следующем `calculate` ядро подставляет свой. Проверено прямым POST на `/order/calculate/` с пустым `region[city]` мимо плагина — ядро вернуло город из настроек локации магазина. Это `shopCheckoutRegionStep`, плагин здесь ни при чём: в логе того же запроса `Section 'region' skipped: belongs to customer`. Для региона правило P2 действует в форме «не подменяем введённое», а не «можно очистить».

**Осторожно при воспроизведении.** `calculateAction()` пишет `$session_checkout['order'] = $input` **целиком**. Ручной POST с урезанным телом стирает из сессии всё, чего в нём нет. Так и подтвердилось правило N1 — но состояние чекаута после такого придётся восстанавливать руками.

### Дзен-режим — статус: ✅ пройдено 19.08.2026 (кроме сценария 5)

Восемь сценариев с привязкой к правилам и результатами — в разделе «Проверять после фикса» [бага zen-collapse](../bugs/zen-collapse-on-upstream-checkout-error.md). Итог: 1, 1a, 2, 4, 4a, 4b, 6, 8 — ✅; 3 и 7 — 🟡 (разобраны там же, правку не требуют); 5 (`shipping.used = false`) — ⬜, нужен другой конфиг чекаута.

Замер, из которого вырос «минимум на группу» вместо «все секции»: при самовывозе с выключенными адресными полями в сессии `details = {"html":"only"}` — данных нет, хотя доставка выбрана.

**Гостевые сценарии — только curl с пустой банкой кук.** Выход из бэкенда обнуляет авторизацию, но не сессию чекаута, а файлы PHP-сессий из песочницы не видны. Рецепт стенда и id-шники типов доставки — в том же баг-репорте. Заодно это единственный способ увидеть `Set-Cookie`: браузерные инструменты заголовки не отдают.

Настройки, которые трогались и возвращены в исходное: `debug_panel`, адресные поля чекаута, `prefill.guest.enabled` (глобальная витрина `*`).

### Эхо группы доставки и гард сворачивания — статус: 🟡 частично 24.08.2026

Сценарии с привязкой к правилам — в разделе «Проверять после фикса» [бага потери выбора](../bugs/shipping-payment-identity-lost-after-snapshot-removal.md). Прогнаны 1, 2, 3, 4, 5, 6, 11, 11a-11c — все ✅. Не прогнаны 7-10, 12-15; из них важнее прочих **7** (смена типа доставки) и **8** (`fast_render`) — обе негативные, ломать их фикс не должен.

Отдельно прогнаны все шесть веток жёсткой ошибки шага `auth` из раздела «Когда это срабатывает» того же бага (не только опечатка в email, но и чужой email/телефон покупателя, email/телефон админа, забаненный контакт, кончившийся товар, несовпадение типа покупателя) — все дают одинаковый короткий цикл «секция пустеет → эхо восстанавливает после починки», независимо от типа ошибки и даже от того, кто именно авторизован в момент починки.

Первый прогон шёл на **развёрнутых** группах и потому проверял только восстановление данных; отдельный прогон со свёрнутыми (исходный сценарий жалобы) вскрыл остаток: «Изменить» при активной ошибке разворачивал группу в пустоту. Гард после этого распространён на оба направления. Урок: сценарии дзен-режима гонять именно в свёрнутом состоянии, иначе половина логики не выполняется.

Сценарий 15 (магазин с `details.custom` — датой и интервалом доставки) на этой инсталляции проверить нечем: ни один установленный плагин доставки таких полей не отдаёт. Ветка кода покрыта только чтением.

Автотесты: `SectionCheckerOwnershipVsDataTest` — 166 проверок, включая регресс-щит на `shipping[service_agreement]` (поле чужой формы в пространстве имён секции, из-за которого первая реализация критерия не работала).

### Видимость свёрнутой карточки при коротком замыкании (B2b) — статус: ✅ пройдено 25.08.2026

Сценарии и результаты — в разделе «Проверено после фикса» [бага пропажи карточки оплаты](../bugs/payment-zen-card-vanishes-on-shortcircuit.md). Прогнаны 1-4 — все ✅: обычная загрузка с первого кадра, репро «сломать email со свёрнутыми доставкой и оплатой» (обе карточки не пропадают), развёрнутая оплата при короткой замыкании (секция остаётся скрытой, без одинокой кнопки — осознанное отличие от снятого JS-костыля `forceDetailSectionVisible()`), обычное сворачивание доставки без костыля.

`config.payment.used = false` и `shipping.used = false` живьём не гонялись — требуют менять настройки чекаута на общем дев-стенде, закрыты автотестом (`CheckoutStateFastRenderTest`, регресс-щит на `empty()`-версию критерия).

**Побочная находка, тоже исправлена и проверена:** иконка группы (`icon_source: plugin`) читала логотип способа доставки/оплаты напрямую из `$state`, мимо кэша сводки — при коротком замыкании падала на дефолтный SVG, хотя текст сводки в тот же момент уже восстанавливался из кэша. Проверено на сторфронте `wa-dev.loc/*` с временно включённым `icon_source: plugin` для `payment` (лого `cash.png`) — при разбитом email лого остаётся, настройка возвращена в `default` после проверки.

PHP-лог (`/Applications/ServBay/logs/php/7.4/errors.log`) и лог плагина (`wa-log/prefill.plugin.log`) за время прогона чисты — сверено по приросту строк и mtime, а не по метке времени (та отстаёт на ~7 часов).

### Экранирование данных покупателя (Z7) — статус: ✅ пройдено 20.08.2026

Полезная нагрузка: имя `<img src=x onerror="window.__xss=1">`, фамилия `O'Reilly & "Co" <b>bold</b>`, улица `<b>Тверская</b> & <i>1</i>`. Проверялось на `/order/` при свёрнутых блоках; критерий не «выглядит нормально», а `childElementCount` сводки и `window.__xss`.

| # | Сценарий | Ожидание | Правило | Итог |
|---|---|---|---|---|
| 1 | Нагрузка в имени/фамилии, блок «Покупатель» свёрнут | Теги видны как текст, `childElementCount = 0`, `__xss = 0` | Z7 | ✅ |
| 2 | Нагрузка в улице, блок «Доставка» свёрнут | Адрес — текст; разметка самого шаблона (`div.wa-header`, `strong`, `br`) на месте | Z7 | ✅ |
| 3 | `shipping_rate` в том же блоке | Остаётся HTML: `span.prefill-zen-price-free` («Бесплатно» зелёным) | Z7 | ✅ |
| 4 | `payment_description` в блоке «Оплата» | Остаётся HTML — паритет с `order/form/payment.html` ядра | Z7 | ✅ |
| 5 | Нет двойного экранирования | В тексте сводки нет `&lt;` и `&amp;`; `&` в фамилии показан как `&` | Z7 | ✅ |
| 6 | Диалог «Мои варианты» с HTML в адресе прошлого заказа | Теги как текст, в карточке нет `img`/`b`, `__xss = 0` | Z7 | ✅ |
| 7 | Редактор шаблонов: подсказка HTML-поля | В тултипе `{$shipping_rate}` есть «Содержит HTML и выводится без экранирования» | Z7 | ✅ |
| 8 | Предпросмотр шаблона в админке | Совпадает с витриной, без артефактов экранирования | Z7 | ✅ |

**Грабли стенда.** Блок не свернётся, пока висит кука `prefill_zen_<группа>=expanded` (Z4) — снимать её перед проверкой, иначе примешь «нет сводки» за поломку. Кука не HttpOnly, снимается из консоли.

Данные, которые трогались и возвращены в исходное: `shipping_address.street` у заказа #85 (временно содержал нагрузку).

### Интеграция с плагинами выбора города (G1, G1a) — статус: ✅ пройдено 26.08.2026

Покупатель `user:1`, город последнего заказа подменялся в `shop_order_params` заказа 92. Оба плагина установлены: CitySelect 2.0.4, «SEO-регионы» 3.2.10. Города «SEO-регионов»: Москва (дефолт) и Новосибирск на `wa-dev.loc/*`, Казань на `wa-dev.loc/shop-1/*`.

Стенд, приём с `<title>` для чтения текущего города «SEO-регионов» и способ сброса гео-состояния — в [city-sync-regions.md](../todo/city-sync-regions.md#стенд).

| # | Сценарий | Ожидание | Правило | Итог |
|---|---|---|---|---|
| 1 | Вакуум, город истории есть на текущей витрине | Город подставлен обоим плагинам, окна «Ваш город?» нет, `shop_cityselect.detect()` из разметки исчез | G1 | ✅ |
| 2 | Три страницы подряд (главная, товар, `/order/`) | Ровно одна подстановка на плагин за сессию — иначе выброшенное эхо отняло бы выбранную доставку | P9 | ✅ |
| 3 | `/order/` после подстановки | Город в шапке и в форме совпадают: `Новосибирск / 54 / rus / 630000` | — | ✅ |
| 4 | Покупатель сам сменил город штатным механизмом плагина | `Geo sync backs off: customer changed the city`, его выбор переживает все последующие загрузки | P1, G1 | ✅ |
| 5 | Город есть, нашей записи нет (первая встреча) | Однократная перезапись | G1a | ✅ |
| 6 | Город истории привязан к другой витрине (Казань → `shop-1/*`) | В «SEO-регионы» не пишем, редиректа нет, витрина осталась со своим городом | B2a | ✅ |
| 7 | Города истории нет в списке «SEO-регионов» (Омск) | Не вмешиваемся: `getCityByLocation()` вернул `null` | B2a | ✅ |
| 8 | Тумблер выключен | Ноль гео-активности в логе, оба плагина работают стоково | — | ✅ |
| 9 | Плагин выключен в `plugins.php`, а `integration.regions` в базе `true` | Интеграция мертва, тумблер исчез из формы, хранимое `true` погашено в `0` при первом сохранении | — | ✅ |
| 10 | Аноним | Ни куки, ни гео-строки в логе — выход на гарде опознания, до базы и до сессии | P5 | ✅ |
| 11 | Для города истории настроен переход на другую витрину (CitySelect, правило «регион 54 → `shop-1`») | Не вмешиваемся: куки нет, `shop_cityselect.detect()` остался в разметке, плагин работает стоково | B2a | ✅ |
| 12 | Тот же покупатель выбирает этот город руками | Переход отрабатывает штатно: `wa-dev.loc/` → `wa-dev.loc/shop-1/`, город доехал их пикселем переноса кук | G2 | ✅ |
| 13 | Он же уже на витрине назначения | Подстановка снова работает: `detectRedirect()` гасит правило, когда цель совпадает с текущим маршрутом — петли нет | B2a | ✅ |

**PHPSESSID анониму ставим не мы.** С установленными «SEO-регионами» `curl` с пустой банкой кук получает `PHPSESSID` на первой же странице каталога — это их плагин, проверено выключением: `regions => false` → в ответе только `landing`, `regions => true` → снова `PHPSESSID`. Про это важно помнить, когда прогоняешь проверку «Гость не создаёт следов» выше: на инсталляции с «SEO-регионами» её критерий «только `landing`» больше не выполняется по чужой вине, и смотреть надо на отсутствие `prefill_*` кук и гео-строк в логе.

**Грабли стенда.** Кука `prefill_geo` — HttpOnly, из консоли не стирается, а сессия «SEO-регионов» переживает выход из бэкенда: между сценариями состояние сбрасывать через `POST /prefill/consent` с `action=clear`. Собственного фронтового геттера текущего города у «SEO-регионов» нет — читать его через их же оптимизатор мета-тегов, иначе проверяешь свой лог своим логом.

Данные, которые трогались и возвращены в исходное: `shipping_address.{city,region,zip}` у заказа #92 (временно Казань и Омск), `meta_title` в `shop_regions_settings` (зонд, удалён), правило перехода `region = 54 → wa-dev.loc/shop-1/*` в `shop_cityselect__region` (удалено). Вернуть правило для повторной проверки переходов:

```sql
INSERT INTO shop_cityselect__region (region, city, redirect) VALUES (54, '', 'wa-dev.loc/shop-1/*');
```

### ФО-01. Активация и область действия (A-01…A-07) — статус: ✅ пройдено 28.08.2026

Первый релизный прогон (R), см. [отчёт](runs/1.0.0-2026-08-28.md). Стенд K1, дальше K5/K7/K8.

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| A-01 | Плагин и витрина активны, `user:1` со свежей сессией (сброс через `/prefill/clear-storage`) | В логе появилась `Loading fill params for authorized user` — загрузчик реально сходил за источником; форма предзаполнена с первого кадра | ✅ |
| A-02 | `active=false` глобально | 0 новых строк в логе плагина на загрузку `/order/`, 0 ссылок на ассеты плагина | ✅ |
| A-03 | Витрина неактивна (`storefront][active]=false`), глобальная активна | Чекаут работает по глобальным настройкам — Zen-сворачивание и «Мои варианты» на месте | ✅ |
| A-04 | Витрина неактивна И глобальная неактивна, витрина-сосед (`shop-1`) активна независимо | `wa-dev.loc/order/` — чекаут стоковый; `wa-dev.loc/shop-1/order/` в то же самое время работает штатно | ✅ |
| A-05 | `accent_color` различается у `shop-1` (`#00ff00`) и глобальной (`#c93131`) | Сгенерированные CSS-файлы разных витрин содержат каждый своё значение, витрины не путаются | ✅ |
| A-06 | `shop-1.wa-dev.loc/*` (поддомен) | Загружается штатно, отдельный `storefront_code`, баннер согласия на куки показан; глубокий регресс не гонялся — уже покрыт прогоном интеграции с городами 27.08.2026 | ✅ (лёгкий смоук) |
| A-07 | Заказ создан из бэкенда при живой сессии витрины | В `shop_order_params` нет `prefill_guest_*`; лог плагина не вырос ни на строку — `isStorefrontRequest()` гасит хук | ✅ |

Побочные находки (обе — факт без разбора, см. файлы):

- [`storefront_code = '-'`](../bugs/settings-storefront-code-dash-row.md) — незнакомая псевдо-витрина в `shop_prefill_settings`
- [Мастер-тумблер «active» — визуал расходится с реальным состоянием](../bugs/settings-active-toggle-visual-state-desync.md) — обнаружено при A-02/A-03, из-за этого временно потерялось состояние `active` на `wa-dev.loc/*`, исправлено вручную SQL

**Грабли, подтверждённые в этом прогоне.** У `shop_prefill_settings` нет уникального индекса (осознанно, issue-57) — `INSERT ... ON DUPLICATE KEY UPDATE` тихо создаёт дубль строки вместо обновления. Runbook-эндпоинты `/prefill/clear-storage` и `force-prefill` гейтятся на `isDebug()` — с выключенным глобальным дебагом (issue-87) ими пользоваться нельзя, поэтому основной прогон L3 идёт с debug **on**, а прод-бандл (debug off) проверяется отдельным прицельным проходом по Zen-сценариям (ФО-06) и J-08.

Данные, которые трогались и возвращены в исходное: `shop_prefill_settings` (`active`/`accent_color` у `*`, `wa-dev.loc/*`, `shop-1`), тестовый заказ #107 (создан и удалён), `wa-config/config.php: debug` (временно off для пересборки бандлов, затем on).

### ФО-02. Предзаполнение: источники и ленивость (B-01…B-12) — статус: ✅ пройдено 05.09.2026

Прогон R, `user:1`, K1. Полный цикл через настоящий чекаут (не подмену сессии вручную).

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| B-01 | Свежая сессия (`/prefill/clear-storage`), `/order/` | `Loading fill params for authorized user` → ровно 1 `shop_customer` PK + 1 `shop_order_params` + 1 `shop_order` (`mysql.general_log`); форма предзаполнена с первого кадра | ✅ |
| B-02 | Та же сессия, 3× клик «+» количества | **0** обращений к `shop_order_params`/`shop_order`/`shop_customer` за все три `calculate` | ✅ |
| B-03, B-04 | Контакт без заказов / без `shop_customer` | Не переиспроизводилось живьём в этом прогоне — логика покрыта `FillParams*`-автотестами, отдельного контакта под эти два кейса на стенде не заводили | ⬜ (см. причину) |
| B-05 | Логин → логаут → логин | `Loading fill params for authorized user` сработал заново после повторного логина — маркер прежней личности не переиспользован | ✅ |
| B-06 | Оформлен заказ (#108, курьер + нал.), продолжение в той же сессии | Заказ прошёл с первого клика. `shop_customer.last_order_id` → 108. Следующая загрузка `/order/`: `Loading fill params` с `Context: {"order_id": 108}` — маркер сброшен, источник — новый заказ | ✅ |
| B-07 | Каталог не трогает БД заказов | Дублирует L2.2 (curl, `docs/tests/TEST-PLAN.md` §2) — 0 ссылок на ассеты и, соответственно, 0 обращений | ✅ (через L2) |
| B-08 | `sections.customer=false`, свежий логин | Лог: `Section 'auth' skipped: group disabled`. **Методическая ловушка**: имя/фамилия/телефон/email в форме всё равно видны заполненными — это НЕ наш плагин, а нативное поведение ядра (показывает собственные поля авторизованного `wa_contact`), что подтверждается тем же самым выводом при `active=false` глобально ещё в ФО-01/A-02. Судить по логу, не по виду формы | ✅ |
| B-09 | `sections.delivery=false` | Лог: `Section 'region'/'shipping'/'details' skipped: group disabled` — вся группа доставки корректно отключена одним флагом | ✅ |
| B-10 | `sections.payment=off`, свежая сессия | Оплата не предзаполняется (список раскрыт, ничего не выбрано) — покупатель выбирает T-Касса вручную → смена региона (Новосибирск → Москва, механический сброс секции) → **выбор пережил пересчёт**: эхо-кэш P9 работает независимо от флага предзаполнения секции | ✅ |
| B-11 | `sections.confirm=false` | Лог: `Section 'confirm' skipped: group disabled` | ✅ |
| B-12 | Все четыре флага (`customer/delivery/payment/confirm`) выключены разом | 12 строк `group disabled`, и — важно — **0** строк `Loading fill params`/`loaded from`: загрузчик не вызывается вовсе, если предзаполнять нечего (не просто «данные не показаны», а реальная экономия обращения к БД) | ✅ |

Побочно: тестовый заказ #107 (создан из бэкенда, A-07) и #108 (этот блок) удалены после проверки; `shop_customer.last_order_id`/`number_of_orders` контакта 1 возвращены к 106/65. Четыре флага `sections.*` возвращены в `true` после B-08…B-12 (05.09.2026).

**Рецидив стороннего инцидента (не баг плагина, см. ФО-06 ниже).** При подготовке B-08 05.09.2026 обнаружено, что `wa_contact` id=1 (admin) снова откатился на `firstname/lastname = 'Гость'/'Ревьюшный'`, а `wa_contact_emails.sort=0` — снова на `review-guest-2608@example.com` (тот самый инцидент, зафиксированный и исправленный другой сессией 28.08.2026 в разделе ФО-06 — «root cause самого механизма... не разбирался»). Значит, тот же механизм сработал повторно уже **после** фикса, независимо от него. Восстановлено вручную тем же способом (имя/фамилия → `Admin`/`Admin`, `sort=0` → реальный email). Причина рецидива не расследовалась — фиксируется как факт, повышает приоритет добраться до root cause.

### ФО-03. Приоритет ввода покупателя (C-07) — статус: 🟡 частично 28.08.2026

Прогон R, `user:1`, K1. Закрыт единственный незакрытый хвост области — **C-07, регресс issue-65 в свежей сессии**, висевший непрогнанным с 18.08.2026 (issue-63-browser-test-runbook.md, п.4.5).

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| C-07 | Свежая сессия (`clear-storage`) → сразу вручную вписать email, отличный от истории, до всякого повторного захода | Значение пережило и AJAX `calculate` (спровоцированный сменой количества товара — обычный blur/клик мимо поля **не** шлёт `calculate` для текстовых полей, только явные триггеры вроде смены количества/региона), и полную перезагрузку `/order/`: `c07-tracked@example.com` осталось после `navigate` | ✅ |
| C-01…C-06 | Стирание комментария/улицы/email/города, лог `skipped: belongs to customer` | Не переигрывалось в этом заходе — тот же код-путь, что и C-07 (единый гейт ownership), плюс уже пройдено 19.08.2026 (раздел «Разделение ownership / data» выше); отдельного регресса не ожидается | ⬜ (см. причину) |

**Методическая находка (не баг, для памяти).** В этой версии чекаута `blur`/клик мимо текстового поля не порождает `calculate` — сеть подтверждает 0 запросов. Для проверок P1/P2 нужен настоящий триггер пересчёта (смена количества, региона, типа доставки), иначе тест ничего не проверяет и по ошибке можно принять «не успел отправиться» за «перезаписалось».

### ФО-04. Гость и согласие (D-01…D-12) — статус: ✅ пройдено 05.09.2026

Прогон R, K2 (`consent_required=on`). Гостевой путь целиком через настоящий чекаут — logout из бэкенда даёт чистого гостя (форма пустая, `Вход` в шапке, поле `prefill[consent]` отрендерено как «Запомнить мои данные для следующих заказов»).

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| D-01 | Чистая банка кук (curl), каталог + `/order/` с товаром в корзине | Куки — только `landing`+`PHPSESSID` (второй ставят «SEO-регионы», не мы); к заказам — 0 реальных запросов (только `DESCRIBE`, это метаданные под debug) | ✅ |
| D-02 | Гость оформляет первый заказ с галочкой согласия (заказ #109) | `shop_order_params`: `prefill_guest_f14cebf5…`, `CHAR_LENGTH(name)=62`, `value='1'` — точное совпадение с форматом из P6 | ✅ |
| D-03 | Тот же гость (та же кука) возвращается, новый заход на `/order/` | Лог: `Fill params loaded from guest order` (ровно 1 обращение); `EXPLAIN` по реальному имени — `type: ref`, `key: name`, `filtered: 100.00`; форма и галочка согласия предзаполнены | ✅ |
| D-04 | Случайный корректный 64-hex токен гостя в куке (не привязанный ни к одному заказу) | Уже закрыт сценарием N-03 (ФО-14, 05.09.2026, `openssl rand -hex 32`) — тот же код-путь, безопасный промах | ✅ (см. N-03) |
| D-05 | Кука `prefill_guest_token`: `abc`, пустая строка, 65×`a`, 64×`z` (не-hex) | `getToken()` вернул `null` во всех 4 случаях (регэксп `TOKEN_PATTERN` требует ровно 64 `[a-f0-9]`); лог — `Prefill was evaluated but no params were filled (empty final_params)`, HTTP 200×4, PHP-лог не вырос | ✅ |
| D-06 | `consent_required=on`, чекбокс не тронут, гость оформляет заказ (пункт выдачи, наличные) | Заказ создан (id 110), но в `shop_order_params` нет ни одной строки `prefill_guest_%` — токен не выдан | ✅ |
| D-07 | `consent_required=off` **у глобальной витрины `*`** (не у `wa-dev.loc` — см. ниже), гость оформляет заказ без единого клика по чекбоксу (чекбокса на форме нет вовсе) | Заказ создан (id 111); `shop_order_params` содержит `prefill_guest_1878e935…`, `CHAR_LENGTH(name)=62`; лог — `[INFO] Guest token created` | ✅ |
| D-09 | Отзыв согласия (`/prefill/consent`, `action=revoke`) | Строка `prefill_guest_%` для заказа #109 удалена из `shop_order_params`; повторный лог — `Guest consent check: no consent`. Данные на странице после этого остаются видны, но это отдельный механизм — `skipped: belongs to customer` (P1, уже принадлежит текущей сессии), не повторный гостевой lookup | ✅ |
| D-11 | Тот же гость (токен уже отозван) напрямую дёргает `params-choice`/`apply-delivery` | Ссылки «Мои варианты» в DOM нет; оба эндпоинта — 403 | ✅ |
| D-08 | Чекбокс «Запомнить мои данные…»: поставить → снять | Лог: `[INFO] User granted prefill consent` при постановке, `[INFO] User revoked prefill consent` при снятии; текст чекбокса — из `.po` (`msgstr`), не хардкод (см. также M-03/ФО-13) | ✅ |
| D-10 | `guest.enabled=off` (глобально, `*`), гость оформляет заказ | `shop_order_params` для нового заказа (id 115) — без `prefill_guest_%`; лог — `[DEBUG] Skipping saveGuestLink: guest prefill is disabled`; при этом на `/order/` в том же состоянии CSS/JS плагина подключены (`link[href*=plugins/prefill]`, `script[src*=plugins/prefill]`) и разметка Zen-режима присутствует (`prefill-zen-*` классы) — ассеты и дзен не зависят от гостевого модуля | ✅ |
| D-12 | Второй гостевой заказ на тот же токен (переиграно после рестарта Мака) | `consent_required=off` у `*` держали на оба заказа подряд без промежуточного возврата: заказы 113 и 114 получили в `shop_order_params` **одинаковое** `name=prefill_guest_1878e935…`; свежий заход на `/order/` после этого — лог `Fill params loaded from guest order`, `order_id: 114` (больший, не 113 и не более старый 111) | ✅ |

**Побочное наблюдение (D-08, не баг).** Отзыв согласия (`revokeConsent()`) дополнительно роняет
выбор города у обеих интеграций гео-синхронизации — лог `Cityselect city dropped on consent
revoke` / `Regions city dropped on consent revoke`. Похоже на намеренную связь («забыть гостя» =
забыть всё, включая город), отдельно не проверялось.

**Важная методическая находка (05.09.2026, не баг сценария, но меняет технику прогона).** Витрина
`wa-dev.loc/*` не имеет ни одной собственной строки в `shop_prefill_settings` — она базы никогда не
настраивалась индивидуально и все её эффективные настройки (включая `consent_required`,
`enabled`) на 100% берутся из глобальной витрины `*` (фоллбэк `getEffectiveStorefront()` из-за
`active=false` по умолчанию для ненастроенной витрины). Есть отдельный, мёртвый код
`wa-dev.loc/shop/*` (легаси, оставшийся от `old_url` в `routing.php` после переноса приложения
`shop` на корень домена) с ~80 настроенными строками — они не читаются ни одним живым запросом.
Практическое следствие: **любая настройка «только для этой витрины» в тестах на `wa-dev.loc`
должна меняться у глобальной `*` (или создавать новую строку под правильным кодом
`d2EtZGV2LmxvYy8q`), а не у `wa-dev.loc/shop/*`.** Подробности и связь с уже известным багом
мастер-тумблера — [storefront-settings-orphaned-by-url-promotion.md](../bugs/storefront-settings-orphaned-by-url-promotion.md).

Тестовые данные: заказы #109, #110, #111, #112, #113, #114 удалены (мягко); контакты **50** (`A07Backend`, из ФО-01/A-07), **51** (`D02Guest Testov`) и **52** (`D06Guest`, гостевой контакт из D-06/D-07/D-12) оставлены на стенде — в интерфейсе Shop-Script у карточки покупателя нет кнопки удаления, чистка отложена на отдельный проход (по прецеденту «Хвосты issue-51» в TODO.md). Настройки, тронутые в этом заходе: `shop_prefill_settings` id=64 (`consent_required` у мёртвого кода `wa-dev.loc/shop/*`) выключалась и возвращена в `1`; id=97 (`consent_required` у `*`) выключалась трижды (D-07, первая и переигранная попытки D-12) и каждый раз возвращена в `true`; id=12 (`active` у мёртвого кода) была найдена в `0` (не по документированному состоянию), выставлена в `1` — эффекта на живой сайт не имеет (код мёртв), оставлено для согласованности с историческим намерением фикса от 28.08. Между первой (неудачной) и переигранной попыткой D-12 был перезапуск Мака пользователем — сверено: `wa-config/config.php: debug` пережил рестарт корректно (`false`), БД и логи не пострадали.

### ФО-05. «Мои варианты» (E-01…E-09) — статус: 🟡 частично 05.09.2026

Прогон R, `user:1` (65 заказов в истории), K1.

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| E-01 | Открыть диалог | 10 карточек (`.prefill-params__card`), текущий вариант — класс `is-active` на первой карточке (курьер) | ✅ |
| E-02 | Кликнуть по другому варианту (СДЭК ПВЗ) | Тип доставки в форме реально переключился на «Самовывоз» | ✅ |
| E-03 | `my_delivery_variants_limit` = 1 / 10 | Ровно 1 / ровно 10 карточек | ✅ |
| E-04 | `limit` = 99 (мусор, выше диапазона) / 0 (мусор, ниже диапазона) | 99 → кламп до 10; 0 → фоллбэк на умолчание (5) — оба без ошибок в UI | ✅ |
| E-05 (05.09) | `my_delivery_variants=false` (глобально, `*`) | Текста/ссылки «Мои варианты» на странице нет (`document.body.innerText` не содержит); код `FrontendParamsChoice::execute()` явно бросает `waRightsException('Access denied')` при пустом `my_delivery_variants` — эндпоинт закрыт структурно, не только UI | ✅ |
| E-06 (05.09) | `my_delivery_variants_button_classes = "e2e-test-btn-class"` | `className` ссылки — `wa-tooltip bottom prefill-params-choice-link e2e-test-btn-class`, кастомный класс применился | ✅ |
| E-07 (05.09, частично) | Побочный эффект применения варианта на секции `auth`/`payment`/`comment` (ранее не проверялось) | Email/имя/телефон покупателя и комментарий не изменились после применения другого варианта доставки из диалога; способ оплаты (`payment=16`) тоже не тронут | 🟡 (эта часть ✅; `is_minimal_mode`-вёрстка, легаси UI 1.3, темы кроме default — по-прежнему не проверялись) |
| E-08 | Вариант из истории недоступен на текущем адресе | Не прогонялось — тестовые данные истории (65 заказов) не содержат заведомо недоступного адреса/типа для текущей конфигурации служб доставки на этом стенде, воспроизвести без правки данных не удалось за отведённое время | ⬜ |
| E-09 | XSS-нагрузка в адресе прошлого заказа | Уже закрыт сценарием 6 раздела «Экранирование данных покупателя» (20.08.2026, выше) | ✅ (см. выше) |

**Грабли этого блока.** Клик по «Мои варианты» после того как диалог уже открыт — если бить в ту же точку экрана дважды подряд, второй клик закрывает диалог (клик мимо карточек, по фону). Дал ложный «0 карточек» на первой попытке E-04 — перепроверил открытие/закрытие явно перед тем, как считать это багом. Координаты ссылки «Мои варианты», полученные через `getBoundingClientRect()`, могут не совпадать с реальным положением на скриншоте после AJAX-перерисовки секции — надёжнее кликать по свежим координатам из самого последнего скриншота, а не переиспользовать более ранние.

### ФО-06. Дзен-режим (F-01..F-30 кроме F-26) — статус: 🟡 частично 05.09.2026

Прогон R, K1. Проверялось через `curl` с чистой банкой кук (`admin`/`123`, реальный логин через
`POST /webasyst/`) — сворачивание рендерится сервером в `buildCollapseBlock()`/`renderCollapseBlock()`,
поэтому `curl` даёт тот же HTML, что и браузер, без накопленных за сессию кук `prefill_zen_*`.
Заодно выполнен обещанный отдельный проход с выключенным `wa-config/config.php: debug` (issue-87,
J-08) — см. ниже.

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| J-08 (issue-87) | `debug=false`, свежая сессия, `GET /order/` | Подключены именно прод-бандлы: `prefill.frontend.min.js` (200, 13370 байт) и `frontend.min.css` (200, 5086 байт); `PrefillFrontendController` определён, `SyntaxError` в консоли нет | ✅ |
| F-01 | Полностью заполненный чекаут (`user:1`, 65 заказов, все поля профиля непустые), свежая сессия | `delivery`/`payment` свёрнуты с первого кадра корректно. `customer` — **не сворачивается**, хотя данные заполнены. Воспроизведено дважды на независимых сессиях | 🔴 (см. bugs/) |
| F-01 (05.09, уточнение механизма) | Ещё одна полностью независимая свежая сессия (`curl`, новый логин `admin`/`123`, новый cookie jar) + повторный `GET /order/` в той же сессии | Воспроизведено идентично: `customer` не сворачивается ни на первом, ни на втором запросе. По логу — на первом запросе `Zen group 'customer' expanded: nothing to summarize yet` (сессия чекаута ещё не прогрета), а `syncCollapseCookieState()` тут же проставляет куку `prefill_zen_customer=expanded`, которая на всех последующих запросах той же сессии безусловно держит группу развёрнутой через кука-ветку `shouldCollapseGroup()` — механизм не отличает «покупатель сам развернул» от «сервер временно не смог свернуть». В уже тёплой сессии (данные в сессии чекаута давно записаны) ручная очистка только этой куки даёт корректное сворачивание — сама логика проверки данных для `customer` не сломана, ломается именно кука-защёлка на холодном старте | 🔴 (уточнено, см. bugs/) |
| F-05 | `shipping[type_id]=post` без `shipping[variant_id]` (тип выбран, вариант — нет), свежая сессия | Лог: `Zen group 'delivery' expanded: nothing to summarize yet` — группа `delivery` не сворачивается, пока не выбран именно вариант (не только тип), как требует правило Z5a | ✅ |
| F-06 | Самовывоз (ПВЗ СДЭК выбран, `shipping.variant_id` заполнен), адресные поля `details.shipping_address` неприменимы для этого типа доставки | Код: `SectionChecker::GROUP_MINIMUM_SECTIONS['delivery'] = ['shipping']` — `details` в минимум группы вообще не входит (комментарий в коде: `details.shipping_address.street` намеренно исключён, «как признак выбора он ложный»). Живьём: группа `delivery` свёрнута корректной сводкой (`СДЭК ПВЗ Новосибирск, Вокзальная магистраль... Доставим 11–12 сентября`) при самовывозе, где customer-адрес не заполнялся — сворачивание идёт по минимуму группы (`shipping`), а не по наличию `details` | ✅ (структурно + живьём) |
| F-11, F-12 | Короткое замыкание (`error_step_id=auth`) при свёрнутой/развёрнутой «Оплате» | Уже закрыто и проверено в браузере **до старта этого прогона** — [payment-zen-card-vanishes-on-shortcircuit.md](../bugs/payment-zen-card-vanishes-on-shortcircuit.md), таблица «Проверено после фикса» (25.08.2026, до фикса именно это и было багом): #2 — обе карточки (доставка/оплата) видимы весь момент ошибки; #3 — развёрнутая оплата остаётся скрытой без одинокой кнопки. Не переигрывалось заново в этом прогоне — переиспользована существующая проверка | ✅ (см. bugs/payment-zen-card-vanishes-on-shortcircuit.md) |
| F-13, F-14 | Шаг доставки/оплаты выключен настройками чекаута (`config.shipping.used=false` / `config.payment.used=false`) | Там же (тот же файл бага): живьём не гонялось («требуют менять настройки общего дев-стенда»), закрыто автотестом `tests/CheckoutStateFastRenderTest.php` — `isStepSkipped()` возвращает `false` для `['disabled' => true]` (шаг не путается с «ядро осознанно ничего не предлагает»), мутация на `empty()`-вариант теста ловит | ✅ (по автотесту, не живьём) |
| F-17 | `zen.groups.delivery.enabled=false` (`storefront_code='*'`, id=109), остальные группы включены | `buildZenModeGroupBlock()`: при `!isGroupEnabled($group)` возвращает `''` **до** вызова `buildCollapseBlock()` — карточки для группы вообще нет. Живьём: на странице ровно 2 `.prefill-zen-collapse-block` вместо 3 (только customer/payment), «Доставка» рендерится обычной развёрнутой формой ядра (тип/регион/вариант), «Покупатель» и «Оплата» продолжают штатно сворачиваться. Настройка возвращена в `true`, кэш очищен, повторно проверено — снова 3 блока | ✅ |
| F-03 | Развёрнутая группа переживает перезагрузку страницы (кука `prefill_zen_<group>=expanded`, сессионная) | Уже многократно и непреднамеренно подтверждено в этом же заходе как побочный эффект: после клика «Почта» без выбора варианта (F-05) `delivery` осталась в развёрнутом виде на **двух** последующих независимых полных перезагрузках `/order/`, хотя тип впоследствии был возвращён на «Самовывоз» с полностью заполненным вариантом — кука `prefill_zen_delivery=expanded` держала группу развёрнутой, пока не была снята вручную. Это и есть механизм F-03 (кука переживает reload) — тот же самый механизм, который для `customer` превращает единичный сбой в постоянный (см. bugs/zen-customer-group-never-collapses-f01.md) | ✅ |
| F-21 | `icon_source=plugin` + короткое замыкание (`error_step_id=auth`) | Уже закрыто и проверено в браузере 25.08.2026 («Побочная находка» в [payment-zen-card-vanishes-on-shortcircuit.md](../bugs/payment-zen-card-vanishes-on-shortcircuit.md)) — до фикса логотип откатывался на дефолтный SVG при коротком замыкании (иконка читала `$state` напрямую, минуя кэш сводки); после фикса `resolveSummaryData()` вызывается один раз и данные идут и в текст, и в иконку — на сторфронте с `icon_source: plugin` (лого `cash.png`) логотип остаётся на месте при разбитом email. Не переигрывалось заново в этом прогоне | ✅ (см. bugs/payment-zen-card-vanishes-on-shortcircuit.md) |
| F-27, F-28 | `shipping_rate`/`payment_description`/`delivery_*_html` остаются HTML; двойного экранирования нет | Код: `ZenData::getAvailableFields()` помечает `is_html=true` у `shipping_rate`, `payment_description`, `delivery_description`, `delivery_schedule`, `delivery_photos_html`, `service_agreement_hint` — с комментарием «HTML собирает сам плагин (уже экранировано)» либо «контент администратора, ядро выводит сырым». `ZenSummaryEscaper::escape()` — единственная точка вызова `htmlspecialchars()`, пропускает поля из этого списка и применяется **один раз** (Smarty `escape_html=false`, второго прохода нет структурно). Живое подтверждение обычных (не-html) полей — уже в N-01 (ФО-14): XSS-полезная нагрузка в имени/фамилии экранирована без сырых тегов | ✅ (структурно + живое подтверждение non-html веток через N-01) |
| F-08 | Сломать email при **свёрнутых** `delivery`/`payment` (`user:1`, Самовывоз+ПВЗ NSK2, Наличные) | `payment` остаётся свёрнутым с верной сводкой (не разворачивается в пустоту) — соответствует ожиданию. `delivery`-карточка (несёт её `details`-секция) вообще **не показывается** на экране, пока держится ошибка: `renderCollapseBlock()` при `!$is_collapsed` осознанно **не** вызывает `generateSectionRevealStyles()` (строка 590-592, комментарий: «показывать одинокую кнопку «Свернуть» в пустой секции незачем») — секция остаётся `display:none`, как и решило ядро. Формально это удовлетворяет F-08 сильнее, чем ожидалось: группа не просто «не разворачивается в пустоту» — она вообще не пытается стать видимой в пустом виде | ✅ (не показывает пустоту, но иначе, чем можно было ожидать) |
| F-09 | Починить email (реальный ввод в браузере + `Tab`, не синтетический POST) | **`payment`** — сводка «Наличные» верно возвращается, ничего не терялось. **`delivery` — РЕГРЕССИЯ**: тип доставки не восстановлен («Типы доставки» без подсветки, `Варианты доставки` нет), полная перезагрузка `/order/` не помогает — нужно выбирать самовывоз+ПВЗ заново. В логе `Delivery echo cleared: customer left shipping section empty` — тот же класс ложного срабатывания эха, что раньше (до фикса 24.08) ломал `payment`. Подробности и разбор — [shipping-payment-identity-lost-after-snapshot-removal.md](../bugs/shipping-payment-identity-lost-after-snapshot-removal.md), раздел «Регресс сценариев 1/2» | 🔴 (регресс, для `delivery`; `payment` ✅) |
| F-10 | Шесть веток жёсткой ошибки шага `auth` | Переиграна только ветка 1 (невалидный email) — см. F-09 выше, она же демонстрирует регресс. Остальные пять веток (чужой email/телефон, email админа, забаненный контакт, кончившийся товар, несовпадение типа покупателя) не переигрывались в этом заходе — учитывая находку F-09, вероятно воспроизводят тот же регресс (общий механизм по коду), но не измерено | 🔴 (частично, по 1 из 6 веток) |
| F-24 | `custom_templates[instance_id]` для конкретного способа оплаты (маркерный текст `active=true`+`template` на все 6 существующих payment-инстансов `*`, id 16/17/18/20/22/23) | Живьём: сводка группы `payment` заменилась на `F24-CUSTOM-TEMPLATE-MARKER` — сработал именно инстанс текущего выбранного способа («Наличные»), `delivery`/`customer` не затронуты. Код (`getSummaryTemplate()`): `resolveCustomTemplate()` возвращает кастомный шаблон только если для конкретного `instance_id` есть `active=true`+непустой `template`, иначе — общий `summary_template` группы. Настройки возвращены (`active=0`, `template=''` на всех шести), кэш очищен, повторно проверено — снова «Наличные Оплата наличными при получении» | ✅ |
| F-30 | Кнопка «Да, очистить» (`.prefill-dialog__confirm-btn`) в диалоге согласия — светлая/тёмная схема | Код (`css/frontend.css`/`frontend.min.css`): `background-color: var(--prefill-accent-color)`, `color: var(--prefill-accent-contrast)`; `--prefill-accent-contrast: light-dark(#ffffff, #000000)` — переключается вместе со схемой через нативный CSS `light-dark()`, `@supports not` даёт статичный фоллбэк для старых браузеров. На текущей витрине живые значения акцента: `--prefill-accent-color-light:#c93131` (светлая — красный фон/белый текст), `--prefill-accent-color-dark:#fff` (тёмная — белый фон/чёрный текст) — фон и текст гарантированно различаются в обеих схемах структурно (`light-dark()` всегда даёт разные значения для двух аргументов). Визуально в реальном переключении ОС light/dark не перепроверялось в этом заходе | ✅ (структурно, живое переключение схемы не перепроверялось) |
| F-02 | Кука `prefill_zen_<group>=expanded` форсирует разворот | Подтверждено: с кукой `delivery` рендерится развёрнутым (`data-action="expand"` пропадает) вместо обычного авто-сворачивания | ✅ |
| F-07 | Сломать email (`auth[data][email]=not-an-email`) через `/order/calculate/` | `errors: auth[data][email]`; `delivery` и `payment` получают `data-blocked-by="customer"` и форсированно разворачиваются (`data-action="collapse"`), хотя сами по себе исходно были свёрнуты — блокировка в обе стороны работает | ✅ |
| F-16 (K9) | `shop_prefill_settings`: `storefront_code` витрины, `groups=["zen"]`, `name=active`, `value=0` (28.08) | Эффекта нет: `delivery`/`payment` по-прежнему сворачиваются, `zenmode.css` по-прежнему подключается — **ложное срабатывание**: витрина `wa-dev.loc/*` не имела своего `active=true`, поэтому фоллбэчила на `*` и override не мог сработать (см. storefront-settings-orphaned-by-url-promotion.md) | ⚠️→✅ переиграно 05.09 |
| F-16 (переиграно, 05.09) | `shop_prefill_settings` id=102 (`storefront_code='*'`, `groups=["zen"]`, `name=active`, `value=false`) | `zenmode.css` не подключён, `[class*=prefill-zen]` — 0 элементов; после возврата в `true` — снова подключён, 12 элементов | ✅ |
| F-04 (05.09, по логам всего прогона) | Данных в группе нет → группа не сворачивается | `grep` по `wa-log/prefill.plugin.log` за весь прогон: `Zen group 'customer/delivery/payment' expanded: nothing to summarize yet` встречается многократно (гостевые визиты без данных, D-04/D-05 и др.) — во всех случаях группа остаётся развёрнутой | ✅ (по накопленным логам) |
| F-15 (05.09) | Секция `confirm` не сворачивается никогда | В `wa-log/prefill.plugin.log` за весь прогон **нет ни одной** строки `Zen group 'confirm' ...`; в коде (`lib/classes/zenmode/`, `CheckoutHooks`) секция `confirm` вообще не участвует в списке сворачиваемых групп — структурная гарантия, не поведенческое совпадение | ✅ |
| F-18 | `zen.groups.customer.hide_auth_header` вкл/выкл (`storefront_code='*'`, id=105), `user:1` авторизован, `GET /order/` живой браузер | **Вкл:** `.wa-contact-name`/`.wa-logout-link` в DOM присутствуют, `getComputedStyle().display === 'none'`. **Выкл:** оба `display: inline`/`inline-block` — видны. Дополнительно проверен файл переменных (`wa-data/.../variables_*.css`): при `false` правила `.wa-step-auth-section .wa-section-header .wa-contact-name/.wa-logout-link{display:none!important}` в файле **нет вообще** (не просто переопределено — не эмитится), при `true` — правило появляется. Настройка возвращена в исходное `true`, кэш очищен, повторно проверено | ✅ |
| F-19 | `zen.icon_display`: `default` / `plugin` / `none` (глобальная настройка, `storefront_code='*'`, id=76) | Полный `grep -rn "icon_display"` по `lib/`+`js/` — значение читается только в двух местах и оба раза сравнивается **исключительно** с `'none'` (`getIconDisplayMode()`/`FrontendHooks::initializeFrontendAssets()`). Различия между `'default'` и `'plugin'` в коде нет вообще — обе ветки ведут себя одинаково (просто «показать иконку»); реальный ТИП иконки (стандартная/логотип плагина/своя/аватар) выбирает отдельная per-group настройка `icon_source` на вкладках «Покупатель»/«Доставка»/«Оплата», не зависящая от глобального `icon_display`. UI и подсказки при этом обещают админу, что именно глобальный переключатель выбирает между «Логотипы способов доставки и оплаты» и «Стандартные иконки блоков» — это неверно | 🔴 (найден новый баг, см. bugs/) |
| F-20 | `zen.icon_size`: `small`/`medium`/`large` | Код (`getIconSizeDimensions()`) — чистая таблица без побочных эффектов: `small`→2.5rem×1.5rem, `medium`→3.5rem×2rem, `large`→4.5rem×2.5rem. Живой файл переменных при текущем значении `medium` содержит ровно `--prefill-zen-icon-width: 3.5rem; --prefill-zen-icon-height: 2rem` — совпадает | ✅ (структурно + 1 живое значение) |
| F-22 (05.09, доп. заход) | `zen.groups.customer.icon_source`: `avatar` / `custom` (`storefront_code='*'`, id=106 + `icon`, id=107) | `avatar`: иконка свёрнутого блока «Покупатель» — `<img src="/wa-content/img/userpic.svg">`, т.е. реальный `waContact::getPhoto()` контакта (у admin фото не загружено — вернулся дефолтный плейсхолдер ядра, не собственный SVG плагина). `custom`: иконка — ровно заданный URL (`/wa-data/f22-custom-marker.png`) без изменений. Обе ветки настройки возвращены в `default`/`''`, кэш очищен | ✅ |
| F-23 (05.09, доп. заход) | Редактор `summary_template` группы «Доставка» (`Открыть редактор` → вкладка «Предпросмотр») | В редактор дописан маркер `F23-PREVIEW-MARKER` вне всех `{if}` — вкладка «Предпросмотр» тут же отразила маркер (живой re-render из содержимого редактора, не кэш). После «Готово» → «Сохранить» тот же маркер дословно появился в свёрнутой карточке живого чекаута (`СДЭК ПВЗ … Доставим 11–12 сентября 2026 F23-PREVIEW-MARKER`) — предпросмотр и реальный рендер используют один и тот же код форматирования, расхождения нет. Шаблон возвращён к стандартному через «Сбросить к стандартному», сохранено, проверено — маркера на витрине больше нет | ✅ |
| F-25 (05.09, по перекрёстной ссылке) | Вставка `{$delivery_photos_html}` (фото пункта выдачи) в шаблон свёрнутого блока | Уже воспроизведено и задокументировано 29.08.2026 — [zen-photos-css-scope-broken.md](../bugs/zen-photos-css-scope-broken.md): переменная подставляется корректно (та же разметка ядра, `data-image-uri` на месте), но CSS-скоуп ядра завязан на предков `.wa-step-details-section .wa-details-rates-section`, которых нет в плоской разметке Zen-карточки — фото рендерятся 0×0px, видны только «голые» стрелки. Повторной живой репродукции в этом заходе не требовалось — вопрос «работает ли вставка картинок» уже имеет задокументированный отрицательный ответ | 🔴 (см. bugs/zen-photos-css-scope-broken.md) |
| F-26 (05.09, по перекрёстной ссылке) | XSS в полях групп `delivery`/`payment` (не только `customer`, который уже закрыт N-01) | Отдельный живой прогон не потребовался: `ZenSummaryEscaper::escape()` — единственная точка экранирования для **всех** групп без исключения (не привязана к тому, какая секция владеет полем), уже подтверждено структурно в F-27/28; сценарий 2 раздела «Экранирование данных покупателя» (20.08.2026) уже прогонял именно `delivery`-поле (`street` с HTML-нагрузкой) тем же кодом и получил экранированный текст. Новый живой тест дал бы результат по тому же коду, что и уже прогнанный — низкая предельная ценность повтора | ✅ (см. F-27/28 + сценарий 2 «Экранирование данных покупателя») |
| F-29 (05.09, доп. заход, полная поведенческая проверка) | Куку `prefill_zen_payment=expanded` выставили вручную (форсировали разворот), оформили этим состоянием реальный заказ (#100117), затем — новый товар в корзину, `GET /order/` | Группа «Оплата» на новом заказе отрендерилась **свёрнутой** со сводкой «Наличные», несмотря на то что прямо перед созданием заказа кука форсированно держала её развёрнутой — `resetState()`/`clearCookies()` действительно шлёт `Set-Cookie` с `expires=-1` на клиент (не только чистит серверный кэш), кука физически исчезла и следующая загрузка честно переоценила состояние заново. Раньше (D-09) было подтверждено только логом; здесь — прямое поведенческое наблюдение. Тестовые заказы #100116/#100117 удалены (мягко) | ✅ |
| F-10 (остальные 5 из 6 веток auth) | Реквизиты эха по остальным веткам жёсткой ошибки `auth` (чужой email/телефон покупателя, email/телефон админа, забаненный контакт, кончившийся товар, несовпадение типа покупателя) | Не переигрывалось в этом заходе — ветка 1 (невалидный email) уже показала регресс (см. F-09 выше), остальные пять используют тот же код-путь `syncDeliveryEcho()` без ветвления по типу ошибки auth; вероятность отличающегося результата низкая, но не измерено | ⬜ (см. причину) |

**Важное исправление (05.09.2026).** F-16 из строки выше был ложным срабатыванием — см. таблицу и
[storefront-settings-orphaned-by-url-promotion.md](../bugs/storefront-settings-orphaned-by-url-promotion.md).
Переигран корректно (через глобальную витрину `*`, которая реально управляет `wa-dev.loc`) — Zen
полностью отключается, баг закрыт. **Практическое следствие для всех оставшихся F-03..30, K9/K10/K11-конфигураций:**
любая настройка «только для витрины wa-dev.loc» должна применяться к `storefront_code='*'`, а не
к какому-либо коду вида `wa-dev.loc/...` — до тех пор, пока у `wa-dev.loc/*` нет собственной строки
`active=true`.

**Побочная находка вне плагина (устранено, не баг плагина).** При подготовке F-01 обнаружено, что
контакт 1 (демо-аккаунт `admin`) имел перезаписанные тестовыми гостевыми данными `firstname`/`lastname`
и первичный email (`shop_order_params`/`wa_contact` — последствие более раннего гостевого сценария
D-02/D-03, где форма `auth[...]` сохранила тестовые значения в профиль залогиненного admin вместо
отдельного гостя). Исправлено по прямому решению пользователя: ФИО → `Admin Admin`, email →
реальный `artem.shamberger@gmail.com`, дубль-запись email удалена. Задокументировано, root cause
самого механизма (почему форма сохранилась в профиль admin, а не осталась гостевой) не разбирался.

**Побочное наблюдение (не проверялось глубже).** `shop_prefill_settings` накопила за этот прогон
десятки дублирующихся строк `name='active'` на витрину/группу (последствие отсутствия уникального
индекса, issue-57, уже задокументировано ранее) — вплоть до 20+ строк на одну и ту же настройку.
Не мешало ни одному сценарию этого прогона напрямую, но затрудняет ручной SQL-аудит; не чищено.

### ФО-07. Эхо-кэши (G-01..G-08, G-10) — статус: 🟡 частично 05.09.2026

Прогон R, K1, `user:1`, живой браузер (тот же сессионный таб, что и ФО-01..06). Оплата «Наличные» уже
выбрана и свёрнута, доставка — «Самовывоз» с готовым ПВЗ. Смена региона (`Новосибирская область` →
`Московская область`) через `form_input` на select — один клик, три сценария сразу.

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| G-01 `SMOKE` | Сменить регион | Оплата осталась «Наличные» после пересчёта; лог: `Payment section restored from echo cache` | ✅ |
| G-05 | (тот же клик) Смена региона рвёт отпечаток адреса варианта доставки | Лог: `Delivery echo dropped: region changed`; группа доставки развернулась | ✅ |
| G-04 | Тип/вариант доставки сброшен сменой региона | Город пуст, кнопка «Выбрать доставку» без выбранного варианта — старый вариант чужого региона не подставлен | ✅ |
| G-02 (05.09) | Сменить тип доставки без смены региона (Самовывоз+ПВЗ NSK2 → Курьер, без выбора конкретного варианта, затем выбор конкретного варианта курьера) | Клик по «Курьер» (вариант ещё не выбран): лог `Delivery echo cleared: customer left shipping section empty` — корректно, это осознанный уход покупателя от прежнего выбора, а не короткозамкнутая гонка; группа осталась развёрнутой с пустым селектом, старая сводка ПВЗ нигде не показана (нет ложного сворачивания с чужими данными). После выбора конкретного варианта курьера и «Свернуть» — карточка свернулась с точной новой сводкой («Курьер • Бесплатно, Дзержинского/…, 8 сентября»), без остатков старого ПВЗ. Состояние восстановлено обратно на Самовывоз+ПВЗ NSK2 через тот же диалог выбора пункта | ✅ |
| G-03 (05.09, по накопленным наблюдениям всего прогона) | `fast_render` на каждой загрузке `/order/` (Shop-Script добавляет сентинел `fast_render` в errors на первом GET до `calculate`) | Отдельный целевой прогон не потребовался: за весь прогон R сделаны десятки свежих `GET /order/` (F-01, B-01, D-01 и др.), каждый — это ровно первый рендер с тем же сентинелом, и во всех случаях свёрнутые карточки `delivery`/`payment` показывали корректные данные с первого кадра, а не считались «пустыми» — `isFastRender()`/`getRegularErrors()` уже закрыты автотестом `CheckoutStateFastRenderTest.php` с регресс-щитом именно на эту путаницу | ✅ (по накопленным наблюдениям + автотест) |
| G-06 (05.09, по перекрёстной ссылке) | Короткое замыкание конвейера восстанавливает `shipping.variant_id` + `payment.id`; половина про `details.custom` — на этом стенде нет ни одного плагина доставки с кастомными полями (тот же блокер, что и раньше, сценарий 15 в bugs/shipping-payment-identity-lost-after-snapshot-removal.md) | `payment.id`-половина ✅ (F-09, `payment` восстанавливается верно). `shipping.variant_id`-половина — тот же код-путь `syncDeliveryEcho()`, что и в F-09, и там уже подтверждён РЕГРЕСС (не восстанавливается). `details.custom`-половина не тестируема на этом стенде | 🔴 (та же регрессия, что и F-09) / ⬜ (custom-часть недоступна) |
| G-07 (05.09, по итогам G-02) | Секция сама прислала пустое поле (покупатель снял выбор варианта) — эхо не должно восстанавливать | Подтверждено тем же кликом, что и G-02: после ухода с Самовывоза на Курьер без выбора варианта эхо корректно НЕ восстановило старый ПВЗ — лог явно говорит `customer left shipping section empty`, группа осталась пустой/развёрнутой, как и требует правило (это выбор покупателя, не гонка) | ✅ |
| G-08 (05.09, по автотесту) | `shipping[service_agreement]` в пространстве имён секции `shipping` не считается «своим полем» секции (регресс-щит) | `tests/SectionCheckerOwnershipVsDataTest.php`, блок 13 — целевая проверка ровно этого сценария (`service_agreement` от чужой формы `region`); прогнан заново перед записью этой строки — 166/166 ✅, регресс-щит держит | ✅ (автотест) |
| G-09 | Магазин с `details.custom` (дата/интервал доставки) | На этом стенде нет ни одного установленного плагина доставки с кастомными полями — проверить нечем (тот же блокер, что и в G-06/сценарии 15) | ⬜ (нет подходящего плагина на стенде) |
| G-10 (05.09) | Заказ создан → оба эхо-кэша (`shop/prefill_payment_echo`, `shop/prefill_delivery_echo`) очищены, следующему заказу не достаются | Код (`shopPrefillPluginOrderHooks::handleOrderActionCreate()`, строки 74-76): `clearPaymentEcho()`/`clearDeliveryEcho()` вызываются безусловно в том же хуке и в тот же момент, что и `zen_mode->resetState()` — тот факт, что после реальных заказов #100116/#100117 (см. F-29) новая сессия честно переоценивала состояние без остатков форсированного состояния, дополнительно подтверждает: тот же хук, тот же кадр выполнения | ✅ (код + те же реальные заказы, что и F-29) |

### ФО-14. Безопасность (N-01…N-07) — статус: ✅ пройдено 05.09.2026

Прогон R, K2.

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| N-01 | `<script>alert(1)</script>` в имени, `<img src=x onerror=alert(2)>` в фамилии → сводка Zen для группы `customer` | Оба payload'а экранированы: `&lt;script&gt;alert(1)&lt;/script&gt; &lt;img src=x onerror=alert(2)&gt;`, сырых тегов в HTML нет | ✅ |
| N-02 | Прямой доступ к чужому заказу через `apply-delivery`/`params-choice` | Уже закрыт сценарием D-11 (ФО-04, 28.08.2026, этот же прогон): оба эндпоинта — 403 | ✅ (см. D-11) |
| N-07 | Логи (`wa-log/prefill.plugin.log`, ~104k строк, весь прогон) на сырые токены/пароли/email | 0 совпадений `password`/`token=`/`prefill_guest_token`/`auth_token`; 0 email-адресов по regex за весь файл | ✅ |
| N-05 | CSRF на публичных эндпоинтах (`consent`, `apply-delivery`) | Известный принятый риск (issue-79), не тестировалось повторно — фиксируется как принятый риск, не как «пройдено». Старый debug endpoint `toggle-zen` удалён 05.09.2026 | ⚠️ принятый риск |
| N-03 | Случайный 64-hex токен гостя (`openssl rand -hex 32`) в куке `prefill_guest_token` | Промах безопасен: `/order/` не предзаполнился (email/имя из истории не найдены в разметке); подстрока токена не встречается ни в одной строке `shop_order_params.value` | ✅ |
| N-04 | `order_id` в `apply-delivery`: `1 OR 1=1`, `1; DROP TABLE shop_order;`, `UNION SELECT password FROM wa_contact`, мусорные строки с ведущими цифрами | Все запросы — 200, без ошибок; `shop_order` цел (99 строк), PHP-лог не вырос. Строки с ведущим числом (`106 AND SLEEP3`) явно кастуются в `int` PHP/`waModel` (`?i`) и резолвятся в реальный `order_id` — не SQL-инъекция, обычное приведение типа; в этом прогоне оба совпавших id (1, 106) случайно принадлежали тому же контакту, кросс-аккаунтный обход через каст не проверялся отдельно (уже закрыт структурно через ownership-гейт, см. D-11) | ✅ |
| N-06 | Аноним (curl без кук) на `?module=prefillPluginSettingsTemplatePreview` и `?module=prefillPluginSettingsTemplateEditor` | Оба — HTTP 200, но тело — стандартный логин-шелл Webasyst SPA (`Войти`/`password` в разметке), 0 маркеров реального редактора (`textarea`/`CodeMirror`/`summary_template`) — контент не отдаётся без сессии администратора | ✅ |

### ФО-08. Гео-синхронизация — не переигрывалось в этом заходе

13 сценариев (H1–H13) уже пройдены 26–27.08.2026, см. [TESTS.md](#интеграция-с-плагинами-выбора-города-g1-g1a--статус--пройдено-26082026) выше. В рамках прогона R повторный полный прогон этого блока не уместился по времени — фиксируется как перенесённый, а не как заново подтверждённый на билде 28.08. Расхождений с текущим кодом не искалось.

### ФО-09. Remember me (I-01, I-04, I-06) — статус: 🟡 частично 28.08.2026

Прогон R, K1/K14.

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| I-01 | `remember_me.active=on`, покупатель авторизован, реальное использование бэкенда/витрины в течение всей сессии (не отдельный тест — пассивное наблюдение за логом всего прогона) | `Auth token extended` регулярно пишется в лог на протяжении 28.08 (последние строки — 11:14:26), `expires_days: 90` | ✅ |
| I-04 | `expires` = 90 (реальное значение витрины `wa-dev.loc/*`) | Срок в логе `Auth token extended` совпадает — `expires_days: 90` | ✅ |
| I-06 | `order_without_auth=create_contact` (реальное значение витрины `wa-dev.loc/*`, `wa-config/apps/shop/checkout2.php:1174`), гость оформил заказ (#100110, временно включён `remember_me.on_order=true` для проверки) | Токен **не выдан** — ни `Auth token issued`, ни любая другая строка с `token`/`auth` в логе за это время. Сперва выглядело как возможный баг (I-02 ожидала выдачу), но `order_without_auth=create_contact` — это ровно сценарий I-06 («разовый контакт: токен не выдаётся»), а не I-02. Поведение корректно | ✅ |
| I-02 | Токен выдан гостю после заказа (`existing_contact`/`confirm_contact`) | Не проверено — текущая витрина `wa-dev.loc/*` работает в режиме `create_contact` (I-06), для I-02 нужна другая конфигурация `order_without_auth`, которой на этом стенде в активном виде нет | ⬜ (конфигурация недоступна на стенде без правки `order_without_auth`) |
| I-03, I-05, I-07, I-08 | `on_order=off`, `rememberme` выключен на домене, `existing_contact`/`confirm_contact`, авторизация не состоялась | Не прогонялось в этом заходе | ⬜ (нет времени в рамках сессии) |

Тестовые данные: заказ #100110 удалён (мягко); контакт **52** (`I09Guest Testov9`, разовый — режим `create_contact`) оставлен на стенде, тот же прецедент, что и с контактами 50/51 (нет кнопки удаления в карточке).

### ФО-11. Админка (K-01..K-14, кроме K-10/K-11) — статус: 🟡 частично 05.09.2026

Прогон R, K1/K5.

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| K-01 `SMOKE` | Открыть `?action=plugins#/prefill` | SPA грузится, все четыре вкладки (Основные, Дзен-режим, Дизайн, Отладка) и все шесть категорий настроек открываются | ✅ |
| K-03 (issue-78) | Переключить витрину `*` → `wa-dev.loc/*`, включить «Использовать индивидуальные настройки», сохранить с опцией «Только текущую витрину» | Дубля поля `[active]` нет: ровно одна строка `active` на `wa-dev.loc/*` (id=328) и глобальная строка `*` (id=1) не тронута, осталась той же — сохранение корректно ушло только для актуальной витрины | ✅ |
| K-04 | Диалог области сохранения (SaveScopeDialog) | Диалог появился сам (посещены 2 витрины за сессию SPA), корректно предложил «Только текущую» / «Все открытые витрины (2)», выбор «только текущую» сработал как заявлено (см. K-03) | ✅ |
| K-02 `SMOKE` (05.09, по накопленным наблюдениям) | Изменить одну настройку и сохранить — только она меняется в БД | Практически на каждом сценарии этого прогона (F-17, F-18, F-22, F-24 и др.) менялась ровно одна опознанная строка `shop_prefill_settings` и проверялась после сохранения — ни разу не зафиксировано побочного изменения соседней настройки | ✅ (по накопленным наблюдениям) |
| K-05 (05.09, по накопленным наблюдениям) | Сохранить для глобальной `*` — витрины без своих значений подхватили | Ровно этот механизм эксплуатировался весь прогон (`wa-dev.loc/*` не имеет своих строк, см. storefront-settings-orphaned-by-url-promotion.md) — каждое изменение на `*` (иконки, шаблоны, `hide_auth_header`, `delivery.enabled` и т.д.) немедленно отражалось на живом `wa-dev.loc/order/` | ✅ (по накопленным наблюдениям) |
| K-06 (05.09) | Группа-сирота: `custom_templates` с несуществующим id способа оплаты (`999999`, вставлено напрямую в БД) | Вкладка «Оплата» → «Шаблоны для конкретных методов» отрендерила ровно 6 реальных методов (#16-23), седьмой (сиротский) не показан вообще — `OrphanedGroupsFilter`/логика построения списка фильтрует его молча; форма не упала, PHP-лог не вырос (10555 строк до и после); автотест `OrphanedGroupsFilterTest.php` — OK. Тестовые строки удалены после проверки | ✅ |
| K-07, K-08 | Редактор шаблонов: подсказка HTML-поля, предпросмотр совпадает с витриной | Уже закрыто 20.08.2026, раздел «Экранирование данных покупателя», сценарии 7 и 8 | ✅ (см. выше) |
| K-09 (05.09) | Диалог контактов (кнопка «Поддержка» в шапке настроек) | Диалог «Мы на связи» открылся, оба адреса подставлены верно из `data-*` атрибутов шаблона: `support@appark.com` / `contact@appark.ru` | ✅ |
| K-12 (05.09) | Все 8 бэкенд-экшенов настроек без прав `shop` | Структурно: все 8 классов (`Settings`, `SettingsStorefront`, `SettingsTemplateEditor` — через `shopPrefillPluginSettingsBaseAction`; `SettingsGetCss`, `SettingsTemplatePreview`, `SettingsReadLogs`, `SettingsSaveLogLevel`, `SettingsClearLog` — через `shopPrefillPluginSettingsBaseController`) наследуют один из двух базовых классов с финализированным `execute()`, проверяющим `isAdmin('shop')` до вызова `handle()` — обойти по отдельному экшену нельзя (issue-54). Живьём для анонима уже подтверждено N-06 (login-шелл вместо контента); полноценный «авторизован, но без прав shop»-контакт на этом стенде не заводился | ✅ (структурно + N-06 для анонима) |
| K-13 (05.09) | Мусор в числовых полях: `remember_me.expires` = `"not-a-number"` | Код: `(int)($settings['expires'] ?? 0)` → `(int)"not-a-number"` = `0` → `rememberMe()`: `$expires_days > 0 ? ... : self::DEFAULT_TTL` — фоллбэк на дефолт, тот же паттерн защиты, что уже живьём проверен для `my_delivery_variants_limit` (E-04). Живой лог `Auth token extended` не поймался в окне проверки (нужен именно сценарий с `auth_token`-кукой, не обычная админ-сессия) — закрыто по чтению кода и по аналогии с уже подтверждённым паттерном, не по живому логу. Настройка возвращена в `90` | ✅ (структурно + аналогия с E-04) |
| K-14 (05.09) | XSS в текстовых настройках: `custom_css` = `body{color:red}</style><script>window.__k14xss=1</script>`, `button_classes` уже прогонялся с обычной строкой (E-06) | `custom_css` пишется в отдельный физический `.css`-файл (`Content-Type: text/css`, подтверждено curl) — payload виден в файле дословно, но `window.__k14xss` на живой странице `/order/` остался `0`: браузер разбирает `<link rel="stylesheet">` строго как CSS, значение никогда не проходит через `innerHTML`. `toggle_button_classes`/`button_classes` в шаблонах выводятся через Smarty-модификатор `escape:'html'` (`ToggleButton.html`) — HTML-теги в значении стали бы текстом, не разметкой; JS-сторона (`myDeliveryVariantsButtonClasses`) идёт только в `.className =`, не в `innerHTML`. Значение и сгенерированный файл `frontend__.css` возвращены в исходное (файл при пустом `custom_css` не должен существовать вовсе — таково поведение `saveFile('')` → `deleteFile()` — старый файл с payload удалён вручную, поскольку писался напрямую через SQL, минуя штатный путь сохранения) | ✅ |
| K-10, K-11 | Вкладка Debug: смена уровня лога, чтение/очистка лога из админки | Не прогонялось — требует отдельного прохода с `debug=on` (протокол прогона держит debug off с ФО-06, см. J-08); частично то же самое уже проверено через прямые вызовы `/prefill/logs`, `/prefill/clear-storage` и подобные в других разделах (ФО-01, ФО-04), но не через саму вкладку админки | ⬬ (см. причину) |

**Побочное наблюдение (не баг, уже задокументировано выше).** Первое включение «индивидуальных настроек» для витрины материализует **весь** дерево настроек как отдельные строки (~70 строк на снимок), а не только изменённый лист — ожидаемо для семантики «использовать общие / использовать свои», но раздувает `shop_prefill_settings` быстрее, чем можно было бы ожидать от одного клика. Выключение обратно (снова «Использовать общие») не удаляет эти строки — `active` для витрины просто становится `false`, 70 строк-снимков остаются организационным мусором (тот же паттерн накопления, что уже отмечен в находке про дубли `name='active'`).

### ФО-10. Ассеты, CSS, производительность (J-01..J-11) — статус: ✅ пройдено 05.09.2026

Прогон R, K1. Счётчик SQL — через `mysql.general_log` (см. методику ФО-01/ФО-04 выше).

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| J-01 `SMOKE` | Каталог: главная и страница товара | 0 ссылок на `plugins/prefill` в HTML обеих страниц | ✅ |
| J-09 | Счётчик SQL: загрузка `/order/` в тёплой сессии (`user:1`, маркер уже совпадает) | 0 обращений к `shop_order_params`/`shop_customer`/`shop_prefill*`; единственный совпавший запрос в логе — счётчик `shop_order.state_id` ядра Shop-Script, не плагина | ✅ |
| J-10 | Счётчик SQL: 3 `calculate` подряд (клики «+» на количестве товара) | 0 обращений к `shop_order_params`/`shop_customer`/`shop_prefill*` за все три пересчёта | ✅ |
| J-02 (05.09, по накопленным наблюдениям) | `/order/` при разных конфигурациях (K4-подобная — F-17, K9-подобная — F-16), кнопка «Изменить» | Ассеты и рабочая кнопка «Изменить» подтверждены на десятках загрузок `/order/` за весь прогон при самых разных конфигурациях зена, включая полностью выключенный (F-16) и с одной выключенной группой (F-17) | ✅ (по накопленным наблюдениям) |
| J-03 (05.09, дедукция) | Все группы дзена выключены по отдельности → `zenmode.css` не подключается | Код: `hasAnyGroupEnabled()` (гейт CSS) перебирает `isGroupEnabled($group)` для каждой группы — то же самое условие, что и при `zen.active=false` (F-16, уже живьём подтверждено: 0 подключений `zenmode.css`), и структурно доказано отличным от `isActive()` в F-17 (одна выключенная группа не гасит CSS для соседних, значит гейт именно `hasAnyGroupEnabled()`, а не мастер-тумблер). Все три группы выключенными по отдельности эквивалентны `hasAnyGroupEnabled()===false` — тот же код-путь, что уже пройден | ✅ (дедукция из F-16+F-17, тот же код-путь) |
| J-04 | Смена `accent_color`/`accent_color_dark` | Уже пройдено в ФО-01, A-05 (28.08.2026) — разные значения на разных витринах не путаются | ✅ (см. A-05) |
| J-05 (05.09) | `custom_css` дописывается к таблице плагина, не заменяет | Уже реализовано и проверено 20.08.2026 (issue-76, override-модель); повторно подтверждено сегодня побочно в K-14 — сгенерированный `frontend_*.css` содержит ТОЛЬКО пользовательский текст (`getDefaultContent()` — отдельный, не тронутый источник `css/frontend.css`, подключаемый всегда первым) | ✅ (issue-76 + K-14) |
| J-06 | Cache busting через `update_time` в URL | Уже реализовано и проверено 19.08.2026 (issue-82, для debug и prod) | ✅ (см. issue-82) |
| J-07 (05.09) | `StaleFilePruner` убирает осиротевшие файлы при удалении витрины из routing | Живой прогон (удаление реальной точки входа из `routing.php`) сочтён слишком инвазивным для этого прохода — закрыто автотестом `tests/StaleFilePrunerTest.php`, прогнан заново перед записью строки — OK | ✅ (автотест) |
| J-08 | debug on/off → несжатые модули / `prefill.frontend.min.js` | Уже закрыто в ФО-06 под тем же номером (issue-87) | ✅ (см. выше) |
| J-11 (05.09, по перекрёстной ссылке) | Тема без jQuery — плагин пишет в лог и молча выключается, чекаут не ломается | Уже закрыто 28.08.2026 (issue-89 + issue-80§1): инлайновый скрипт и точка входа фронтенда обёрнуты в `typeof $!=="undefined"`, no-op при отсутствии jQuery подтверждён `node -e`; живой репро на реальной теме без jQuery не делался ни тогда, ни сейчас (риск и так низкий, см. issue-89) | ✅ (см. issue-89/issue-80) |

### ФО-12. Отладка (L-01, L-04) — статус: 🟡 частично 28.08.2026

Прогон R, K16 (`debug=false`, действует с ФО-06).

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| L-01 | `debug=false`, запрос от **авторизованного admin** (не анонима) к `/prefill/clear-storage` | `{"status":"fail","errors":"Access denied"}`, HTTP 200; в DOM `/order/` нет ни одного элемента `[class*="debug-panel"]`/`[class*="prefill-debug"]` — панель не рендерится даже админу | ✅ |
| L-04 `SMOKE` | Аноним дёргает существовавшие на дату прогона debug endpoints | Уже закрыт в L2.4 этого же прогона: все 5 вернули `{"status":"fail","errors":"Access denied"}`. `toggle-zen` позднее удалён 05.09.2026 | ✅ (см. L2.4) |
| L-06 | Уровень лога `warning` (умолчание) → debug-строки не пишутся | **Не проверяемо на этом стенде в чистом виде**: в `shop_prefill_settings` есть строка `storefront_code='-'`, `groups=["logging"]`, `name=level`, `value=debug` — та самая orphan-строка, задокументированная ранее как факт без объяснения ([settings-storefront-code-dash-row.md](../../bugs/settings-storefront-code-dash-row.md)). Похоже, именно она держит логирование на уровне `debug` весь прогон (лог полон `[DEBUG]`-строк с 19.08 по 28.08) — гипотеза, не подтверждена глубже | ⬜ (заблокировано той же orphan-строкой) |
| L-02, L-03, L-05 | `debug on + debug_panel off/on`, `force-prefill`/`reset-and-refill` от админа | Не прогонялось в этом заходе (требует отдельного debug=on прохода) | ⬜ (нет времени в рамках сессии) |

### ФО-13. Локализация (M-01, M-02, M-04, M-03) — статус: ✅ пройдено 05.09.2026

Прогон R. Статический анализ вместо визуального обхода (дешевле, тот же результат для «ключ вместо
текста»): извлечены все вызовы `_wp('...')` из `lib/`, `templates/`, `js/` и сверены с обоими `.po`.

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| M-01, M-02 (статически) | 173 уникальных ключа `_wp()`, использованных в коде, сверены с `ru_RU.po` и `en_US.po` | 0 отсутствующих ключей в обоих файлах; 0 пустых `msgstr` в обоих файлах — ни одной заглушки | ✅ (статически; визуальный обход витрины/админки на `en_US` не делался) |
| M-03 | Ручной grep `js/` на кириллицу вне `_wp()`/`$_()` (дополняет статическую сверку по ключам, которая слепа к хардкоду мимо `_wp()`) | 14 срабатываний, все в `js/prefill.debug.js` (debug-панель админа) + 1 уже известное (`logger.debug`, не пользовательский текст, L0.4). Найден новый факт — [debug-panel-hardcoded-russian-strings.md](../bugs/debug-panel-hardcoded-russian-strings.md); customer-facing JS (`js/modules/*`, `js/prefill.frontend.js`) хардкода не содержит | ✅ (сценарий закрыт, найден баг — см. файл) |
| M-04 | `.mo` актуальны относительно `.po` | `mtime` `.mo`/`.po` совпадает для обеих локалей (27.08 10:11) — последняя компиляция синхронна, `.po` не менялись после | ✅ |

### L5. Релизная приёмка (R-01, R-08, R-09) — статус: 🟡 частично 28.08.2026

| # | Сценарий | Наблюдение | Итог |
|---|---|---|---|
| R-01 | `php wa.php compress` | Уже закрыт в L0.6 этого же прогона — без ошибок валидации | ✅ (см. L0.6) |
| R-08 | PHP-лог за весь прогон (`errors.log`, 10540 строк, `mtime` 27.08 11:07 — новых строк с этого момента не появилось) | Единственный fatal error в файле — `OrphanedGroupsFilter::filter(): Argument must be array, object given`, датирован **24.08**, до старта этого прогона; уже задокументирован и исправлен ([issue-80](../codereview/issue-80-frontend-minor-findings.md), `->fetchAll()`). Сегодняшние сохранения настроек (K-03/K-04, тот же код-путь `saveSettings()`) прошли без единой новой строки в этом логе — регресс не воспроизвёлся | ✅ |
| R-09 | `wa-log/prefill.plugin.log` за весь прогон (~104k строк) | 0 строк `[ERROR]`, 0 строк `[WARNING]` за всю историю файла | ✅ |
| R-02..R-07 | Состав архива, версии, установка на чистую БД, обновление, деактивация, удаление | Не прогонялось в этом заходе | ⬜ (нет времени в рамках сессии) |

### L4. Совместимость — не переигрывалось в этом заходе

S-03/S-04 уже пройдены 26.08.2026 (см. TEST-PLAN.md §8), остальные пункты (S-01, S-02, S-05..S-08)
сам документ помечает как неопределённые/backlog вне зависимости от прогона — донастраивать стенд
под них не входило в бюджет этой R-сессии.

## Debug-панель — модернизация 05.09.2026

### Статическая проверка

| Сценарий | Результат | Статус |
| --- | --- | --- |
| PHP lint всех файлов `lib/` | Синтаксических ошибок нет | ✅ |
| `node --check js/prefill.debug.js` | Синтаксис корректен | ✅ |
| Кириллица и `innerHTML` в debug JS | Совпадений нет | ✅ |
| Удалённые `ToggleZen`, `sections_*_status`, `BEFORE/AFTER`, `called_hooks` | Исполняемых ссылок нет | ✅ |
| ru_RU/en_US PO | Скомпилировано `php wa.php locale shop/plugins/prefill`, по 411 сообщений | ✅ |

### Browser validation, Google Chrome

| Сценарий | Результат | Статус |
| --- | --- | --- |
| Глобальный `debug=false` | После reload панели в DOM нет; `refresh-debug`, `debug-source` и три изменяющих endpoint возвращают `Access denied` | ✅ |
| Гость без источника, начальная загрузка | Русская панель показала guest, fallback на `*`, отсутствие применённого источника; admin-мутации скрыты | ✅ |
| Явная загрузка источника | Результат подписан как диагностическое чтение, форма не изменилась | ✅ |
| Ручной refresh | Снимок обновился, существующая история запросов сохранилась | ✅ |
| Выбор типа доставки | В историю добавился новый `calculate` с отдельным `request_id`, Prefill/echo/Zen/render не смешались с прежним запросом | ✅ |
| Повторная проверка после исправления дублей | Начальные события отображаются один раз | ✅ |
| Светлая/тёмная тема панели | В изолированном browser-preview смена класса и фона Shop-Script автоматически переключила `data-color-scheme`: фон/текст `#fff/#20242c` ↔ `#16191f/#eef1f6` | ✅ |

Не проверялись в этом проходе: en_US в браузере, admin-команды, create, конкурентные AJAX-ответы,
авторизованный источник и полная матрица граничных Zen-состояний. Сообщения `Errors` в DevTools
создаёт штатный `shop/js/frontend/order/form.js` при неполной форме; исключений из `prefill.debug.js`
в проверенном потоке не обнаружено.

## Правила ведения

- Проверка без правила из `RULES.md` — повод либо добавить правило, либо выкинуть проверку.
- Прогнали браузерный блок — меняем статус и ставим дату. Не прогнали — оставляем ⬜, а не молчим.
- Нашли баг вживую — заводим файл в `bugs/`, сюда добавляем строку с воспроизведением.
