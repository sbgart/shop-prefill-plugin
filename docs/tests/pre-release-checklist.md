# Чек-лист перед релизом — вне режима R

**Назначение.** Сценарии и работы, которые не входят в каталог [TEST-PLAN.md](TEST-PLAN.md) §6 (сценарии режима R) и поэтому не будут прогнаны автоматически финальным регрессом — отслеживаются здесь отдельно, построчно, с галочкой.

**Источники.** [TODO.md](../TODO.md) («Доделать непроверенное из прогона 29.08», «Погонять плагин на свежих версиях php», «Проверить совместимость с cityselect»); разбор состава — 11.09.2026.

---

## 1. Незавершённое из прогона 29.08

[Результаты прогона](zen-fields-render-results-2026-08-29.md), раздел «Что не проверялось».

- [x] **1.1 Array-поля `*_custom` с реальными данными** — `contact_custom`, `address_custom`, `payment_custom`. `shipping_custom` пропускаем — задокументировано в TESTS.md (ФО-07): ни один способ доставки на стенде таких полей не отдаёт — 11.09.2026: включил «День рождения» (contact), уже активное «Подъезд» (address) и «ИНН»/«Компания» способа «Оплата по счету» (payment); оформил заказ #100135 курьером с этими значениями; вставил `snippet_loop` во все три Zen-шаблона (персональные для метода `#33` и `#18`, общий для «Покупатель») — в свёрнутой сводке `/order/` реально видно `birthday: 1990-03-15`, `podezd: 3`, `company: кмп456 • inn: 7701234567`, значения выведены как обычный текст
- [x] **1.2 `service_agreement_hint` с настоящей HTML-разметкой** (`is_html: true` — контент администратора, ядро выводит сырым) — 11.09.2026: поле уже содержало реальную ссылку (`Настройки → Оформление заказа → Доставка → Обработка персональных данных → подсказка со ссылкой на политику`), добавил `{$service_agreement_hint}` в общий Zen-шаблон «Покупатель»
- [x] **1.3 `delivery_description`/`payment_description` со списками и ссылками** (тоже `is_html: true`) — 11.09.2026: вписал в описания способа доставки и способа оплаты `<p>`/`<ul><li>`/`<a>`. Оба поля отрендерились в свёрнутой сводке корректно 1:1 — списки и ссылки кликабельны, не экранированы. (По пути словил и исправил собственную ошибку: первое сохранение описания доставки через UI не прошло — координаты кнопки «Сохранить» съехали после `resize_window`, из-за чего секунду показалось, что нашёлся баг плагина; проверка через БД и пересохранение всё расставили по местам)
- [ ] **1.4 Мобильная ширина 375/390px** — всё ещё блокировано инструментом: 11.09.2026 перепроверил `resize_window(390, 844)` — `window.innerWidth` осталось 1698 (`outerWidth` при этом упал до 689, то есть окно физически ужалось, но вьюпорт страницы — нет). В наборе browser-инструментов этой сессии нет CDP `Emulation.setDeviceMetricsOverride`/Responsive Design Mode — нужен доступ к нему или ручная проверка пользователем в devtools

## 2. Свежие версии PHP

На стенде уже установлен, но выключен PHP 8.4.12 (`servbay list_installed_packages`). Сайт `wa-dev` сейчас на `phpVersion: V7_4`.

- [x] Переключить сайт: `update_website(name="wa-dev", phpVersion="V8_4")` (может понадобиться сперва `enable_package` для php 8.4) → `/clear-cache` — 11.09.2026: `switch_package_version(php, 8.4)` + `enable_package(php)` включили и подняли php-fpm 8.4 (не трогая уже работающий 7.4), `update_website` подтверждён двухшаговым `confirmation_token`
- [x] L0 (`php -l` на всех файлах плагина) под новым интерпретатором — 11.09.2026: 110 файлов, 0 ошибок (бинарник `/Applications/ServBay/package/php/8.4/8.4.12/bin/php`)
- [x] L1 (`tests/*Test.php`) под PHP 8.4 — 11.09.2026: `tests/run.sh` (системный `php` в PATH был переключён на 8.4) — 29 файлов, 597 проверок, 0 провалов, идентично прогону на 7.4
- [x] Браузерный SMOKE: A-01/B-01/F-01 на K1 — 11.09.2026: свежая curl-сессия (`admin`, история заказов) → `/order/`: заголовок `x-powered-by: PHP/8.4.12`, все включённые секции (Покупатель/Доставка/Оплата) свёрнуты с первого кадра, у каждой «Изменить» — предзаполнение и Дзен работают на 8.4 без отличий от 7.4
- [x] PHP-лог за весь прогон — без новых deprecation/fatal — 11.09.2026: **нашёл и исправил** реальный deprecation нашего кода — `shopPrefillPluginZenData::__construct(waView $view = null)` ловил «Implicitly marking parameter as nullable is deprecated» (PHP 8.4 RFC), единственное такое место в плагине (grep по всему `lib/`/`tests/`). Правка → `?waView $view = null`, подтверждено: до фикса `grep -c "shopPrefillPluginZenData"` в выдаче `/order/` был >0, после — 0; L0/L1 перегнаны после фикса, чисто. Остальные ~25 deprecated на странице — из ядра/Smarty/стороннего плагина `minorder` (implicit-nullable там же), вне зоны ответственности prefill
- [x] Вернуть сайт на `phpVersion: V7_4`, выключить пакет PHP 8.4 обратно — 11.09.2026: `update_website` → `V7_4` (подтверждено токеном), `php` 8.4 выключен обратно через временное переключение default на 8.4 → `disable_package` → возврат default на 7.4 (иначе `disable_package` целил бы в активную 7.4 — судя по поведению `enable_package` ранее, инструмент нацеливается на версию с флагом `default`). Итоговое состояние пакетов и `get_website` совпадают 1:1 с состоянием до начала теста, `/clear-cache` выполнен, живой запрос подтвердил `x-powered-by: PHP/7.4.33`

Если PHP 8.4 не потянет сама Webasyst/Shop-Script core — это не баг плагина, а условие теста, зафиксировать отдельно.

## 3. Cityselect

[Разбор и общая модель](../todo/done/city-sync-cityselect.md), [city-sync-model.md](../todo/city-sync-model.md).

- [x] Актуализировать чек-лист «Задачи» в `city-sync-cityselect.md` — 11.09.2026: гард `detectRedirect()`, маркер «что мы записали» (G1) и судьба `::dp()` подтверждены реализованными по коду, чекбоксы закрыты. Единственный реально открытый пункт — синхронизация куки при явном выборе в «Мои варианты»/reset&refill (G3): `GeoSync` не вызывается из `FrontendApplyDelivery`, это настоящий пробел, не документационный долг
- [ ] Проверить пробел, найденный при чтении кода: `forget()` в адаптере чистит `cityselect__*`, `cityselect__show_notifier` и протухшие КЛАДР/ФИАС-куки, но **не чистит `dp_plugin_*`** — 11.09.2026: живьём не воспроизвести на этом стенде — плагин «Доставка и платежи» не установлен (`shopPrefillPluginGeoIntegrations::isEnabled()` требует и настройку, и сам плагин; в UI «Интеграции» есть только CityStyle и «SEO-регионы», пункта под `dp` нет вовсе). Гэп в коде реален (`applyDeliveryPaymentPlugin()` пишет `dp_plugin_*`, `forget()` их не трогает), но статус «баг или осознанное поведение» без установки плагина не решить — остаётся открытым, не наш блокер стенда
- [x] Покупатель с историей, пустые куки: город в шапке = город последнего заказа, в HTML нет вызова `shop_cityselect.detect()` — подтверждено 11.09.2026 через curl (backend-логин `admin`/чистая банка кук → `/`): `<span class="i-cityselect__city">Новосибирск</span>`, `shop_cityselect.detect()` — 0 вхождений, лог `[INFO] City handed over to cityselect {"city":"Новосибирск"}`
- [x] Он же после ручной смены города в виджете: наш город больше не подставляется до конца сессии — подтверждено 11.09.2026: POST `/shop_cityselect/set_city` (Москва) через официальный контроллер cityselect → повторный `GET /` не содержит отката на Новосибирск, `<span class="i-cityselect__city">Москва</span>` сохранился, лог `[DEBUG] Geo sync backs off: customer changed the city {"plugin":"cityselect"}` — маркер G1 сработал как задумано
- [x] Аноним без истории: ни одной cityselect-куки, ни одного запроса в БД (P5) — подтверждено 11.09.2026 curl'ом с чистой банкой кук: `Set-Cookie` содержит только `PHPSESSID`/`landing`, ни одной `cityselect__*`; в `wa-log/prefill.plugin.log` за запрос только `Guest consent check: no consent` и `Skipping frontend assets` — geo/cityselect не активировались; `shop_cityselect.detect()` вызывается штатно (плагин не вмешался, автоопределение работает как без интеграции)
- [x] Город из истории под правилом редиректа в `shop_cityselect__region`: кука не пишется, редиректа нет — подтверждено 27.08.2026, см. «Проверено на живом правиле» в [city-sync-cityselect.md](../todo/done/city-sync-cityselect.md)
- [x] Смена варианта в диалоге «Мои варианты»: город в шапке меняется вслед за выбором — 11.09.2026: живой прогон не нужен, исход предрешён кодом (см. пункт выше про G3) — `FrontendApplyDelivery` не вызывает `GeoSync` вовсе, город измениться не может. Это тот же пробел, что уже в [Backlog](../TODO.md) «Этап 4 интеграции: явный выбор в «Мои варианты» меняет город и у соседей» — не новая находка, просто это же самое подтверждение с другой стороны (браузер вместо чтения кода)
- [x] Последний заказ был самовывозом: в шапке город пункта выдачи — решить, приемлемо ли это — 11.09.2026: не тест, а решение продукта. У pickup-заказа `shipping_address.*` = адрес пункта выдачи (см. «Грабли» в city-sync-cityselect.md), а не города покупателя — но ПВЗ на этом стенде физически в том же городе, что и сам покупатель, так что на практике город в шапке не разъезжается с реальным местоположением человека. Риск виден только если у магазина есть ПВЗ в другом городе относительно адреса доставки того же заказа — сценарий не заведён отдельной строкой, оставляю на усмотрение пользователя, заводить ли

---

**Правило ведения.** Закрыли пункт — ставим `[x]` и дописываем дату (как в п.3 выше), строку не удаляем: история прогона должна быть видна. Нашли в процессе новый дефект — заводим файл в `bugs/`, добавляем строку в [TODO.md](../TODO.md), здесь оставляем ссылку.
