# Прогон 1.0.0 — 2026-08-28

**Режим:** R (релизный, [TEST-PLAN.md](../TEST-PLAN.md) §1) — первый полный прогон по этому документу
**Сборка:** без git (репозиторий не инициализирован), рабочее дерево на 28.08.2026
**Стенд:** Webasyst (текущая dev-инсталляция), Shop-Script, PHP 7.4, тема `default`

Бандлы (`js/prefill.frontend.min.js`, `css/frontend.min.css`) пересобраны перед стартом. Глобальный debug Webasyst выключен с ФО-06 и до конца L3 (`wa-config/config.php`, `/clear-cache` выполнен) — включает отдельную проверку прод-бандла (issue-87, J-08 в TESTS.md).

## Итог

L0 ✅ · L1 ✅ 15/15 · L2 ✅ 5/5 · L3 🟡 частично (**11** из 14 ФО закрыты на 100%: ФО-01, ФО-02, ФО-04, ФО-06, ФО-07, ФО-08, ФО-10, ФО-11, ФО-12, ФО-13, ФО-14) · L4 ⬜ · L5 ⬜

**Прогон продолжен 05.09.2026 после смены машины** (см. «Смена машины 05.09.2026» ниже) — состояние
файлов синхронизировано вручную, до этого прогон продолжался автономно 28–30.08.2026 (включая
отдельный точечный прогон на Docker-стенде минимальной версии, `runs/1.0.0-2026-08-30-min-version.md`,
не заменяет этот прогон).

9 новых багов найдено за прогон (список — «Провалы и находки» ниже; из них F-16 впоследствии
закрыт 05.09 как ложное срабатывание — реальных багов 8; из них ещё один найден 05.09 при
доигрывании ФО-06 — F-19, `zen.icon_display` default/plugin неотличимы) плюс 1 реопен уже
закрытого 🔴-бага (`shipping-payment-identity-lost-after-snapshot-removal.md` — сценарий «сломал
email → починил» для delivery снова воспроизводится, 05.09, F-09/F-10), плюс 1 посторонний инцидент
с данными демо-аккаунта (28.08; повторное появление 05.09 переквалифицировано — вероятный откат БД
к старому бэкапу при смене машины, не независимый рецидив, см. «Смена машины» ниже и файл бага).
Не начаты: L4 (совместимость), L5 (релизная приёмка, частично).

## L0 — статика

| # | Результат |
|---|---|
| L0.1 (синтаксис PHP 7.4) | ✅ |
| L0.2 (нет синтаксиса PHP8+) | ✅ |
| L0.3 (ядро не тронуто) | не применимо — `wa-dev` не git-репозиторий |
| L0.4 (хардкод RU) | 2 срабатывания, оба уже известны и разобраны в TEST-PLAN.md §2 (aria-label в ContactsDialog.html — открытая задача локализации; logger.debug — не пользовательский текст) |
| L0.5 (роуты `/?`) | ✅ |
| L0.6 (сборка архива) | ✅, без ошибок валидации |

## L1 — автотесты

15 файлов, 0 провалов.

## L2 — curl-проверки

| # | Результат |
|---|---|
| L2.1 (гость не создаёт следов) | ✅ только `PHPSESSID` (ставит «SEO-регионы», задокументированная особенность стенда) + `landing` |
| L2.2 (ассеты не на каталоге) | ✅ 0 |
| L2.4 (debug-эндпоинты закрыты) | ✅ HTTP 200 + `{"status":"fail","errors":"Access denied"}` на все 5 |
| L2.5 (чужая история) | ✅ 403/403 |

## L3 — браузерный регресс

| ФО | Статус |
|---|---|
| ФО-01 Активация и область действия | ✅ A-01…A-07, детали в [TESTS.md](../TESTS.md#фо-01-активация-и-область-действия-a-01a-07--статус--пройдено-28082026) |
| ФО-02 Предзаполнение: источники и ленивость | ✅ пройдено 05.09.2026 — B-01,02,05,06,07,10 (28.08) + B-08,09,11,12 (05.09) ✅; B-03,04 покрыты автотестами, не переиграны живьём |
| ФО-03 Приоритет ввода покупателя | 🟡 частично — C-07 (регресс issue-65) ✅, C-08/п.4.7 (`cityselect` не конфликтует с приоритетом ввода, 05.09) ✅ — закрывают оба открытых хвоста runbook issue-63; C-01..06 не переигрывались (низкий риск, тот же гейт ownership) |
| ФО-04 Гость и согласие | ✅ D-01…D-12 все пройдены (D-04 закрыт через N-03) |
| ФО-05 «Мои варианты» | 🟡 частично — E-01..06,08,09 ✅ (E-08, 05.09: временно отключён СДЭК — форма не ломается, недоступный вариант пропадает и из формы, и из диалога «Мои варианты»); E-07 только часть про побочный эффект на auth/payment/comment ✅, остальное (is_minimal_mode, UI 1.3, темы) не проверено |
| ФО-06 Дзен-режим | ✅ пройдено (измерено полностью 05.09, заголовок в TESTS.md исправлен — «кроме F-26» было устаревшим) — J-08 ✅, F-01 🔴 (customer не сворачивается, механизм уточнён 05.09 — гонка + кука-защёлка), F-02 ✅, F-04 ✅ (по логам), F-05 ✅, F-06 ✅ (структурно + живьём), F-07 ✅, F-15 ✅ (структурно), F-16 ✅ (05.09, переигран корректно — исходное 28.08 было ложным), F-18 ✅ (обе стороны, живьём + файл CSS), F-19 🔴 (новый баг — `icon_display` default/plugin неотличимы), F-20 ✅ (структурно + 1 живое значение), F-11/F-12 ✅ (переиспользована проверка из payment-zen-card-vanishes-on-shortcircuit.md, до старта прогона), F-13/F-14 ✅ (по автотесту, не живьём), F-17 ✅ (живьём, отключённая группа не рендерит карточку, соседние работают), F-03 ✅ (побочно подтверждено через сессионную куку delivery), F-21 ✅ (см. payment-zen-card-vanishes-on-shortcircuit.md, до старта прогона), F-27/F-28 ✅ (структурно, `is_html`-список + N-01), F-30 ✅ (структурно, `light-dark()`), F-24 ✅ (живьём — маркер шаблона на всех payment-инстансах, сработал верный), F-08 ✅ (не показывает пустоту), F-09 🔴 РЕГРЕСС (delivery не восстанавливается после починки email; payment ✅), F-10 ✅ все 6 веток жёсткой ошибки шага `auth` измерены 05.09 (ветка 1/невалидный email — 🔴 регресс, тот же что F-09; `forbidden_error`, `admin_error`, `banned_error` — все три через `shopConfirmationChannel`, обе секции восстанавливаются из эха корректно; `cart_invalid` — другая секция ошибки (`cart`, не `auth`), эхо-механизм не задействуется; несовпадение типа покупателя — сессия физически не переписывается на время ошибки, восстанавливать нечего; экстраполяция «одна ветка → все ветки той же секции регрессируют одинаково» опровергнута — регрессирует только ветка 1, остальные 5 закрыты как не-регресс по трём разным причинам); F-22 ✅ (avatar/custom иконки customer живьём), F-23 ✅ (редактор↔предпросмотр↔витрина — один код форматирования), F-25 🔴 (см. zen-photos-css-scope-broken.md — уже задокументированный отрицательный результат), F-26 ✅ (по перекрёстной ссылке на единый escaper + сценарий 2 «Экранирование данных покупателя»), F-29 ✅ (полная поведенческая проверка — кука форсированного разворота не пережила `resetState()` после заказа) |
| ФО-07 Эхо-кэши | ✅ пройдено (доигран 06.09) — G-01,02,03,04,05,07,08,10 ✅ (оплата пережила смену региона, отпечаток доставки лопнул, чужой вариант не подставлен, смена типа доставки не даёт ложного сворачивания, `fast_render` не путает карточки с пустыми, service_agreement не считается своим полем, оба эхо-кэша чистятся после заказа); G-06/G-09 доиграны 06.09 — нашёлся плагин доставки с кастомными полями («Курьер», поле `desired_delivery` было выключено настройкой, включено на время теста): на типе доставки Самовывоз (составной `variant_id`) регресс F-09 подтверждён (shipping-половина 🔴), но на типе Курьер (простой `variant_id: 34.delivery`) регресс НЕ воспроизводится — `variant_id` и `details.custom` восстановились из эха оба разом, дважды подряд. payment-половина ✅ во всех случаях. Разбор зависимости от типа доставки — bugs/shipping-payment-identity-lost-after-snapshot-removal.md |
| ФО-08 Гео-синхронизация | ✅ пройдено (доигран 06.09) — полные 13 сценариев прошли 26–27.08.2026 (см. TESTS.md); сегодня прицельно переигран смоук-минимум H-01/H-02/H-03 на текущем состоянии стенда (после находок 05.09 про мёртвые настройки `wa-dev.loc/shop/*` — чтобы убедиться, что интеграция всё ещё читает настройки с витрины `*`, а не с мёртвого кода): вакуум → город из истории (Новосибирск) подставлен обоим плагинам с первого захода, ровно одна подстановка на плагин (`City handed over to cityselect/regions`), `shop_cityselect.detect()` в разметке нет, окна «Ваш город?» нет; ручная смена города покупателем (Казань) — `Geo sync backs off: customer changed the city`, выбор пережил полную перезагрузку. H-10 (конфликт cityselect с предзаполнением) закрыт раньше и независимо — это ровно сценарий C-08 (ФО-03, 05.09) |
| ФО-09 Remember me | 🟡 частично — I-01,04 ✅ (пассивно из лога всей сессии), I-06 ✅ (`create_contact` → токен не выдан, поначалу выглядело как баг I-02, но конфигурация витрины другая), I-03 ✅ (перекрёстная ссылка — `on_order=false` уже фон всего прогона), I-02/I-07 ✅ (05.09, живой гостевой заказ через `existing_contact` + новый email — токен выдан, авторизация после заказа сработала); I-05 🔴 новый баг — при `rememberme=false` на домене ни токен, ни ожидаемое предупреждение не логируются; I-08 ⬜ заблокирована структурно — подтверждение кода блокирует создание заказа целиком, комбинация «метка стоит, авторизация не удалась» этим путём не воспроизводится без реальной доставки письма |
| ФО-10 Ассеты/производительность | ✅ пройдено 05.09.2026 — J-01 ✅ (0 ссылок на плагин в каталоге), J-02 ✅ (по накопленным наблюдениям), J-03 ✅ (дедукция из F-16+F-17), J-04 ✅ (см. A-05), J-05 ✅ (issue-76 + K-14), J-06 ✅ (issue-82), J-07 ✅ (автотест StaleFilePrunerTest), J-08 ✅ (issue-87), J-09,10 ✅ (0 SQL-обращений к shop_order_params/shop_customer на тёплой сессии и на 3 calculate подряд), J-11 ✅ (issue-89/issue-80) |
| ФО-11 Админка | ✅ пройдено 05.09.2026 — K-01 ✅ (SPA грузится целиком), K-02 ✅ (по накопленным наблюдениям), K-03 ✅ (issue-78: дубля `[active]` нет), K-04 ✅ (SaveScopeDialog), K-05 ✅ (по накопленным наблюдениям — вся сессия строилась на этом фоллбэке), K-06 ✅ (группа-сирота фильтруется, форма не падает), K-07/K-08 ✅ (см. выше), K-09 ✅ (диалог контактов), K-12 ✅ (структурно — единый базовый класс на все 8 экшенов), K-13 ✅ (структурно, аналогия с E-04), K-14 ✅ (XSS в `custom_css`/`button_classes` инертен — CSS-контекст и `escape:'html'`), K-10 ✅ (уровень логирования реально фильтрует запись в лог), K-11 ✅ (чтение и очистка лога работают, живьём с бэкапом/восстановлением) |
| ФО-12 Отладка | ✅ пройдено 05.09.2026 — L-01 ✅ (панель не рендерится и эндпоинты отказывают даже админу при debug=off), L-02 ✅ (debug on + debug_panel off — панель не выводится), L-03 ✅ (панель + стек хуков + refresh-debug работают), L-04 ✅ (см. L2.4), L-05 ✅ (source действительно перечитывается, подтверждено `mysql.general_log`), L-06 ✅ (переигран через саму вкладку админки — orphan-строка `-` не блокирует, это и есть целевая строка) |
| ФО-13 Локализация | ✅ пройдено 05.09.2026 — M-01,02,04 (28.08) + M-03 (05.09, ручной grep JS) ✅; M-03 нашёл новый баг (см. находки) |
| ФО-14 Безопасность | ✅ пройдено 05.09.2026 — N-01,02,07 (28.08) + N-03,04,06 (05.09) ✅; N-05 — принятый риск (issue-79, сознательно не тестировалось повторно) |
| L4 Совместимость | ⬜ не переигрывалось — S-03/S-04 пройдены 26.08.2026, остальное вне бюджета/backlog по самому TEST-PLAN.md |
| L5 Релизная приёмка | 🟡 частично — R-01 ✅ (см. L0.6), R-08 ✅ (единственный fatal error в PHP-логе — известный и уже исправленный issue-80, датирован до старта прогона, не воспроизвёлся при today-сохранениях), R-09 ✅ (0 ERROR/WARNING за весь plugin-лог); R-02..07 отложены |

## Провалы и находки

| ID | Что | Файл бага |
|---|---|---|
| A-02/A-03 | Мастер-тумблер «active» — визуал расходится с реальным состоянием | [settings-active-toggle-visual-state-desync.md](../../bugs/done/settings-active-toggle-visual-state-desync.md) |
| — | `storefront_code = '-'` в `shop_prefill_settings` | [settings-storefront-code-dash-row.md](../../bugs/done/settings-storefront-code-dash-row.md) |
| ФО-05 | Кэш сводки Zen показывает данные предыдущей личности после смены гость↔авторизован в той же сессии | [zen-summary-cache-leaks-across-identity-change.md](../../bugs/done/zen-summary-cache-leaks-across-identity-change.md) |
| F-01 (уточнено 05.09) | Группа «Покупатель» (customer) не сворачивается в Zen-режиме на первом кадре, хотя данные заполнены; механизм — гонка на первом запросе новой сессии + кука-защёлка, делающая единичный сбой постоянным | [zen-customer-group-never-collapses-f01.md](../../bugs/done/zen-customer-group-never-collapses-f01.md) |
| F-16 (28.08, закрыто 05.09) | ~~`zen.active = false` не отключает Zen-режим~~ — ложное срабатывание, тестовая витрина не имела `active=true` и фоллбэчила на глобальную | [zen-active-off-not-respected.md](../../bugs/done/zen-active-off-not-respected.md) |
| — (29.08) | `$delivery_schedule` всегда пуст в Zen-сводке (не CSS, неверный источник данных, причина найдена) | [zen-delivery-schedule-source-missing.md](../../bugs/done/zen-delivery-schedule-source-missing.md) |
| — (29.08) | `$delivery_photos_html` невидим в Zen-карточке, 0×0px (CSS-скоуп ядра не совпадает) | [zen-photos-css-scope-broken.md](../../bugs/done/zen-photos-css-scope-broken.md) |
| — (30.08, Docker) | SaveScopeDialog не поддерживает тёмную тему | [save-scope-dialog-no-dark-theme.md](../../bugs/done/save-scope-dialog-no-dark-theme.md) |
| — (28.08) | Профиль контакта admin затёрт гостевыми данными чекаута; повторное появление 05.09 переквалифицировано в вероятный откат БД к бэкапу (не рецидив, см. файл бага) | [admin-contact-profile-overwritten-by-guest-checkout-data.md](../../bugs/done/admin-contact-profile-overwritten-by-guest-checkout-data.md) |
| M-03 (05.09) | Дебаг-панель: хардкод русского текста в JS (alert/confirm/innerHTML) | [debug-panel-hardcoded-russian-strings.md](../../bugs/done/debug-panel-hardcoded-russian-strings.md) |
| D-07 (05.09) | Витрина `wa-dev.loc/*` не имеет своих строк настроек — код `wa-dev.loc/shop/*` мёртв после переноса приложения на корень домена, обе правки тумблера `active` (28.08 и 05.09) редактировали мёртвую строку | [storefront-settings-orphaned-by-url-promotion.md](../../bugs/done/storefront-settings-orphaned-by-url-promotion.md) |
| F-19 (05.09) | Глобальная настройка «Иконки в свёрнутых блоках» (`zen.icon_display`): опции «По умолчанию» и «Логотипы плагинов» в коде неотличимы (обе — просто «показать»), реальный тип иконки решает отдельная per-group `icon_source` | [zen-icon-display-default-vs-plugin-has-no-effect.md](../../bugs/done/zen-icon-display-default-vs-plugin-has-no-effect.md) |
| F-09/F-10 (05.09, РЕОПЕН) | Уже исправленный 24.08 баг («сломал email → починил» уничтожает выбор доставки) воспроизведён заново: `delivery` не восстанавливается после починки email (`payment` не затронут); лог `Delivery echo cleared: customer left shipping section empty` — тот же класс ложного срабатывания эха, что раньше ломал только `payment` | [shipping-payment-identity-lost-after-snapshot-removal.md](../../bugs/done/shipping-payment-identity-lost-after-snapshot-removal.md) |
| K-11 (05.09) | Диалоги подтверждения (очистка лога, force-prefill) используют нативный `window.confirm()` — блокирует браузерные инструменты автоматизации, нарушает конвенцию проекта (диалоги через `<template>`) | [native-confirm-dialogs-not-templated.md](../../bugs/done/native-confirm-dialogs-not-templated.md) |
| I-05 (05.09) | При `rememberme=false` на домене витрины не появляется ни ожидаемый `[INFO] Auth token issued`, ни ожидаемый по коду `[WARNING] Cannot keep customer signed in...` — полная тишина, воспроизведено дважды подряд, причина не найдена | [remember-me-domain-disabled-warning-missing.md](../../bugs/done/remember-me-domain-disabled-warning-missing.md) |

## Смена машины 05.09.2026

Пользователь сменил Мак, эта сессия разговора продолжилась на новой машине. Обнаружено: файлы
документации (`TESTS.md`, `TODO.md`, багрепорты, второй run-отчёт) на диске оказались синхронизированы
и отражали продолжение прогона за 28–30.08.2026, сделанное другой сессией/машиной — на 4 ФО дальше, чем
внутренний чек-пойнт этого диалога. Git-репозиторий плагина (отдельный от `wa-dev`) — та же история,
`up to date with origin/main`. После явного вопроса пользователю прогон продолжен с синхронизированной
точки, а не переигран с начала. Отдельно: `wa-config/config.php: debug` на момент переключения оказался
`true` (документированный протокол — `false` до конца прогона) — возвращено в `false`, `/clear-cache`
выполнен.

**Уточнение (05.09, вечер):** первоначальная запись выше («БД — тот же общий стенд, состояние
согласовано с файлами, не рассинхронизировано») была ошибочной. При подготовке B-08 обнаружился
на первый взгляд «рецидив» инцидента с профилем admin (см. «Провалы и находки»); сверка с
git-историей показала, что это откат БД к бэкапу, сделанному до конца сессии 28–29.08 — `MAX(id)`
в `shop_prefill_settings` (325) и в `wa_contact` (51) ниже, чем то, что та сессия создавала (строки
326/327, контакт 52), хотя git-репозиторий с документацией об этих действиях уже полностью актуален.
То есть при переносе на новый Мак **код и БД оказались из разных моментов времени**: код — свежий
из GitHub, БД — снимок бэкапа около границы 28→29.08. Практический вывод на будущее: при смене
машины перед тем, как считать что-то «рецидивом», сверять граничные `id`/счётчики таблиц с
git-историей документации, а не полагаться на визуальное совпадение состояния стенда. Подробности
и восстановление — в файле бага (раздел «Уточнение»).

## Параллельная работа во время прогона (05.09, вечер)

В ходе тестирования ФО-11/ФО-12 обнаружились 3 новых git-коммита (`f0a4375`, `5a041da`, `9d55807`) —
модернизация debug-панели, сделанная параллельно этому прогону (судя по содержимому, тем же
пользователем через другой инструмент/сессию). Перед продолжением проверено: `git diff` коммитов
на `shopPrefillPluginZenMode`/`shopPrefillPluginCheckoutHooks` — правки чисто аддитивные
(инструментальные вызовы `shopPrefillPluginDebug::recordEvent()` для новой панели), ни одна ветка
существующей логики сворачивания/эхо-кэшей не изменена; `php -l` по всем файлам плагина и полный
набор автотестов — чисто. Находки ФО-06/ФО-07, сделанные до этого момента прогона, остаются в силе.

Побочно вскрылся незавершённый шаг той параллельной работы: `.po`-файлы обновлены (новые ключи
`debug.*`), `.mo` — нет, из-за чего новая debug-панель первое время показывала сырые ключи локали
вместо текста. Выполнен `/compile-plugin-mo`, что и позволило продолжить проверку панели по её
реальным подписям (K-10/K-11/L-02/03/05/06).

**Ещё раз, позже в тот же вечер (E-08, F-10).** `wa-config/config.php: debug` снова оказался `true`
(панель «Диагностика Prefill» появилась на живом `/order/` без моего участия) — тот же параллельный
процесс, судя по времени, тестирует свою же модернизацию. В этот раз debug **не трогался** — окно не
нужно было моим тестам, а вмешиваться в чужой активный прогон настройками debug рискованнее, чем в
прошлый раз (тогда я сам был единственным, кто его использовал). К концу сессии стоит свериться с
владельцем параллельной работы, в каком состоянии оставить `debug` окончательно.

## Не прогнано и почему

| ID | Причина |
|---|---|

## Что трогалось и возвращено

| Настройка / данные | Было | Вернули |
|---|---|---|
| `wa-config/config.php: debug` | `true` | переключено на `false` для ФО-06 (в т.ч. отдельная проверка прод-бандла, issue-87/J-08) и остаётся `false` до конца прогона R; возврат в исходное — по завершении всего прогона |
| Бандлы `js/prefill.frontend.min.js`, `css/frontend.min.css` | собраны 25.08/20.08 | пересобраны 28.08 из актуальных исходников |
| `wa_contact` id=1 (демо-admin): `firstname`/`lastname` | `Гость`/`Ревьюшный` (перезаписано более ранним гостевым сценарием D-02/D-03 этого же прогона) | `Admin`/`Admin` — по прямому решению пользователя (см. TESTS.md ФО-06) |
| `wa_contact_emails` контакта 1 (id=1, sort=0) | `review-guest-2608@example.com`, `status=unconfirmed` | `artem.shamberger@gmail.com`, `status=unknown`; дубль-запись (id=21, тот же email) удалена |
| `shop_prefill_settings`: тестовая строка `zen.active=0` для `wa-dev.loc/*` (F-16, 28.08) | override отсутствовал (дефолт `true`) | строка `id=326` удалена сразу после проверки |
| `shop_prefill_settings` id=102 (`zen.active` у `*`, 05.09, переигранный F-16) | `true` | выключалась на время проверки, возвращена в `true` |
| `wa_contact` id=1: `firstname`/`lastname` (05.09, повторно — см. «Смена машины») | `Гость`/`Ревьюшный` (откат БД к бэкапу до фикса 28.08) | `Admin`/`Admin` |
| `wa_contact_emails` контакта 1 (05.09, повторно) | `sort=0` → `review-guest-2608@example.com` | `sort=0` → `artem.shamberger@gmail.com` (id=21), гостевой email — `sort=1` |
| `shop_prefill_settings`: `sections.customer/delivery/payment/confirm` для `*` (05.09, B-08…B-12) | все `true` | по одному выключались на B-08/09/10/11, все выключались разом на B-12, после каждого — возвращены в `true` |
| `wa-config/config.php: debug` (05.09) | `true` (не по протоколу — обнаружено при смене машины) | `false`, `/clear-cache` выполнен |
| `shop_prefill_settings` id=97 (`consent_required` у `*`, 05.09, D-07/D-12) | `true` | выключалась дважды для тестов, возвращена в `true` |
| `shop_prefill_settings` id=64/id=12 (мёртвый код `wa-dev.loc/shop/*`, 05.09) | `consent_required=1`, `active=0` | правились по ошибке (код не читается живым сайтом, см. баг); `consent_required` возвращён в `1`, `active` оставлен в `1` (по историческому намерению фикса 28.08, без функционального эффекта) |
| Тестовые заказы #110, #111, #112, #113, #114, #115 (05.09, D-06/D-07/D-10/D-12) | — | созданы и мягко удалены (`state_id='deleted'`) сразу после проверки каждого; #112 — от смазанной первой попытки D-12 (до рестарта Мака), #113/#114 — от переигранной успешной попытки (после рестарта), #115 — D-10 |
| `shop_prefill_settings` id=96 (`guest.enabled` у `*`, 05.09, D-10) | `true` | выключалась на время D-10, возвращена в `true` |
| `shop_prefill_settings` id=74 (`my_delivery_variants` у `*`, 05.09, E-05) | `true` | выключалась на время E-05, возвращена в `true` |
| `shop_prefill_settings` id=98 (`my_delivery_variants_button_classes` у `*`, 05.09, E-06) | `""` | временно `e2e-test-btn-class`, возвращена в `""` |
| `shop_prefill_settings` id=105 (`zen.groups.customer.hide_auth_header` у `*`, 05.09, F-18) | `true` | выключалась на время проверки обеих сторон сценария, возвращена в `true`, кэш очищен, проверено повторно |
| `shop_prefill_settings` id=109 (`zen.groups.delivery.enabled` у `*`, 05.09, F-17) | `true` | выключалась на время проверки, возвращена в `true`, кэш очищен, повторно проверено — снова 3 карточки вместо 2 |
| `shop_prefill_settings` id 132/134/136/138/140/238 (`payment.custom_templates.{16,17,18,20,22,23}.active` у `*`, 05.09, F-24) и id 133/135/137/139/141/239 (`.template`) | `active=0`/`0`, `template=''` на всех шести | временно `active=true`, `template='F24-CUSTOM-TEMPLATE-MARKER'` на всех шести сразу (чтобы не искать заранее id текущего способа оплаты); после проверки все двенадцать строк возвращены (`0`/`''`), кэш очищен, повторно проверено — сводка снова «Наличные…» |
| Браузерная сессия `admin` (тип доставки, 05.09, F-05) | «Самовывоз» + ПВЗ «Вокзальная магистраль (NSK2)» | временно переключено на «Почта» без выбора варианта для проверки F-05, после проверки возвращено на «Самовывоз» + тот же ПВЗ |
| Куки `prefill_zen_customer/delivery/payment` в той же браузерной сессии (05.09, для чистой проверки F-01/F-05/F-06) | `expanded` (накоплено за сессию тестирования; `prefill_zen_delivery` повторно залипла после клика «Почта» в F-05 и очищена второй раз перед F-06) | очищены вручную (`document.cookie`) для получения незамутнённого состояния; после проверки все три группы легитимно свёрнуты своими же данными (без ручного форсирования) — куку восстанавливать не в что, это не настройка. Тестовые проверки на независимых `curl`-сессиях (гостевые cookie jar) — вне персистентного состояния, отдельной уборки не требуют |
| Сессия чекаута админа: `auth[data][email]` (05.09, F-08/F-09/F-10) | `artem.shamberger@gmail.com` | временно `broken.address.without.at` для репро ошибки шага `auth`; возвращён обратно тем же живым вводом в браузере сразу после проверки |
| Сессия чекаута админа: выбор доставки (05.09, F-08/F-09/F-10) | Самовывоз, ПВЗ «Новосибирск, Вокзальная магистраль (NSK2)» | сброшен до пустого выбора самим найденным регрессом (не намеренное действие) при починке email — руками восстановлен тот же тип+ПВЗ сразу после того, как находка была зафиксирована в логе и коде |
| `shop_prefill_settings` id=106 (`zen.groups.customer.icon_source` у `*`, 05.09, F-22) и id=107 (`.icon`) | `default`, `''` | временно `avatar`, затем `custom`+`/wa-data/f22-custom-marker.png` для проверки обеих веток; возвращены в `default`/`''`, кэш очищен |
| Шаблон `zen.groups.delivery.summary_template` у `*` (05.09, F-23) | стандартный шаблон группы «Доставка» | временно дописан маркер `F23-PREVIEW-MARKER` вне условий для проверки паритета редактор/предпросмотр/витрина; возвращён через «Сбросить к стандартному», сохранено |
| Куки `prefill_zen_delivery/customer` в браузерной сессии (05.09, F-22/F-23) | `expanded` (накоплено ранее) | очищены вручную для получения незамутнённого свёрнутого состояния, необходимого для проверки иконок/сводки |
| Кука `prefill_zen_payment` (05.09, F-29) | отсутствовала | временно выставлена вручную в `expanded` для проверки, что `resetState()` после заказа снимает именно клиентскую куку, а не только серверный кэш; заказ #100117 оформлен этим состоянием и сразу удалён |
| Тестовые заказы #100116, #100117 (05.09, F-29) | — | созданы для поведенческой проверки сброса Zen-состояния между заказами, оба мягко удалены (`state_id='deleted'`) сразу после проверки |
| `shop_prefill_settings`: строка-орфан `storefront_code='-'` (id=115, `logging.level`, 05.09, K-10) | `debug` | временно `warning` через саму вкладку админки для проверки применения уровня; возвращена в `debug` |
| `wa-config/config.php: debug` (05.09, K-10/L-02/L-03/L-05) | обнаружен уже выставленным в `true` параллельной сессией (модернизация debug-панели) | использовано окно для прогона K-10/K-11/L-02/03/05/06, затем возвращён в `false` по протоколу прогона, `/clear-cache` выполнен |
| `shop_prefill_settings` id=75 (`prefill.debug_panel` у `*`, 05.09, L-02) | `true` | выключалась на время L-02, возвращена в `true` |
| `wa-log/prefill.plugin.log` (05.09, K-11) | 120494 строк | перед проверкой кнопки «Очистить» сделан бэкап; после проверки (файл стал 0 байт) содержимое восстановлено из бэкапа побайтово, `wc -l` совпал |
| `mysql.general_log` (05.09, L-05) | выключен | временно включён для подсчёта SQL-обращений при «Загрузить источник»/«Применить Prefill», выключен сразу после |
| Локали `.mo` плагина (05.09, обнаружено при L-02/K-10, не тестовое действие) | `.po` обновлён параллельной сессией 5 Sep 19:00 (новые ключи `debug.*`), `.mo` не пересобран — панель показывала сырые ключи | выполнен `/compile-plugin-mo` (все 6 шагов, включая restart PHP-FPM) — не фикс поведения плагина, а незавершённый шаг чужой параллельной работы, нужный для дальнейшего тестирования панели |
| `shop_prefill_settings`: `on_order`/`active` для `wa-dev.loc/*` (05.09, I-02/07/08) | нет собственных строк (наследует от `*`, `on_order=false`) | временно вставлены строки `on_order=true`/`active=true` для `storefront_code=d2EtZGV2LmxvYy8q` (напрямую SQL — сохранение через UI-тумблер «Использовать индивидуальные настройки» ушло в orphan-строку `-`, см. находку в K-10 выше); удалены после теста |
| `wa-config/apps/shop/checkout2.php`: `order_without_auth` для `wa-dev.loc/*` (05.09, I-02/05/07/08) | `create_contact` | временно `existing_contact` через штатную форму настроек чекаута; возвращено на `create_contact` тем же путём, подтверждено `grep` |
| `wa-config/auth.php`: `rememberme` для `wa-dev.loc` (05.09, I-05) | `true` | временно `false` для проверки предупреждения; возвращено в `true`, `/clear-cache` + restart PHP-FPM выполнены дважды (до и после теста) |
| Контакт `PrefillI08Test` (id=58, 05.09, I-08) | не существовал | создан для проверки конфликта email с существующим контактом, заказом использован не был (подтверждение блокирует создание заказа) — удалён полностью после теста |
| Заказы #118-121 и контакты 59-62 (05.09, I-02/05/07) | не существовали | тестовые гостевые заказы через curl (существующий/новый email, `rememberme` on/off); оставлены на стенде — тот же прецедент, что и с заказом #10/#100110 и контактами 50-52 |
| `shop_product.status` товара 874 (05.09, F-10 ветка `cart_invalid`) | `1` | временно `0` для имитации «товар недоступен», возвращено в `1` сразу после проверки |
| Контакт `PrefillF10Banned` (id=63, 05.09, F-10 ветка `forbidden_error`) | не существовал | создан (`is_user=-1`) для проверки коллизии email с чужим контактом, заказ не создавался (только `calculate`) — удалён полностью после теста |
| Поле «Электронная почта» в форме чекаута `user:1` (05.09, F-10) | `artem.shamberger@gmail.com` | временно `prefill-f10-banned@example.com` для триггера `forbidden_error`, возвращено на исходное значение, проверено — сохранилось в `wa_contact_emails` без изменений |
| Город в виджете `cityselect` (05.09, C-08/issue-63 п.4.7, E-08) | Новосибирск | дважды временно переключён на Пермь для проверки конфликта региона; оба раза возвращён на Новосибирск, доставка (Самовывоз+ПВЗ NSK2) выбрана заново через диалог «Мои варианты» |
| `shop_plugin.status` СДЭК (id=43, 05.09, E-08) | `1` | временно `0` для имитации «вариант из истории недоступен», возвращено в `1` сразу после проверки |
| Количество товара в корзине `user:1` (05.09, E-08) | `1` | временно `2` (триггер `calculate` для проверки недоступного варианта после отключения СДЭК), возвращено в `1` |
| Контакты `PrefillF10Admin2` (`is_user=1`, `is_company=0`) и `PrefillF10Banned2` (`is_user=-1`, `is_company=0` — намеренно совпадает с `is_company` у `user:1`, иначе `shopConfirmationChannel::getContact()` фильтрует кандидата и уводит в `forbidden_error`) (05.09, F-10 ветки `admin_error`/`banned_error`) | не существовали | созданы для проверки коллизии email с чужим контактом, заказ не создавался (только `calculate`) — оба удалены полностью через приложение «Команда» после теста |
| Настройка чекаута «Кто может оформлять заказы» для `wa-dev.loc/*` (05.09, F-10 ветка «несовпадение типа покупателя») | «Все» | временно переключено на «Организации» при авторизованном `user:1` (физлицо) для проверки раннего `return` в `shopCheckoutAuthStep::process()`, `/clear-cache` выполнен; после проверки возвращено на «Все», `/clear-cache` выполнен повторно |
| Плагин доставки «Курьер» (`shop_plugin.id=34`), галочка «Запрашивать желаемый интервал доставки» (06.09, G-06/G-09) | выключено | временно включено, чтобы получить непустой `details.custom` (поле `desired_delivery`) для теста эхо-кэша на короткое замыкание `auth`; тип доставки в чекауте `user:1` временно переключён на «Курьер», после теста возвращён на «Самовывоз»+ПВЗ NSK2; галочка выключена обратно, `/clear-cache` выполнен дважды |

На момент этой записи (после ФО-14, 05.09.2026): `wa-log/prefill.plugin.log` — свыше 104k строк
(0 `[ERROR]`, 0 `[WARNING]` за весь файл, проверено 28.08 на тот момент истории), PHP-лог — не рос с
начала прогона; единственный fatal error в файле датирован 24.08, до старта R, и уже исправлен — issue-80.
