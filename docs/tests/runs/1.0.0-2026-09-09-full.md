# Прогон 1.0.0 — 09.09.2026 (R, частично — первый заход)

**Режим:** R (заявлен полный, по факту закрыта автоматизируемая часть + разведка ключевых 🔴-сценариев браузером; полного покрытия §6 не достигнуто за один заход — см. «Не прогнано и почему»)
**Сборка:** 86656dc + незакоммиченные правки (issue-91…101, см. `docs/TODO.md`)
**Стенд:** wa-dev.loc, PHP 7.4 (ServBay), тема `default`

⚠️ **Этот прогон не закрывает критерий выпуска §10.2** (все 🔴-области целиком) — зафиксировано честно, не выдаётся за полный R. Ниже — что реально проверено, что нет и почему.

Соглашение: находки фиксируются здесь и одной строкой в `docs/TODO.md`, разбор каждой — отдельным файлом позже, не в рамках этого прогона.

Позиции логов на старте: `prefill.plugin.log` = 159828 строк, `errors.log` (PHP) = 11373 строк.
БД на старте: `MAX(shop_order.id)=130`, `MAX(wa_contact.id)=68`.

## Итог

L0 ✅ 6/6 · L1 ✅ 21/21 · L2 ✅ 3/5, 🟡 2 (не регрессы) · L3 🟡 частично (SMOKE ФО-01/02/03/06/14 закрыты, остальное ⬜) · L4 ⬜ · L5 частично (только R-01/R-02) · **debug off впервые прогнан живым браузером на прод-бандле — закрывает известный слепой участок из TODO.md**

## L0 — статика

| # | Проверка | Итог |
|---|---|---|
| L0.1 | `php -l` всех файлов | ✅ OK |
| L0.2 | Нет синтаксиса PHP 8+ | ✅ OK |
| L0.3 | Ядро не тронуто | 🟡 не применимо буквально — `/Users/artem/Project/wa-dev` не git-репозиторий, git есть только у самого плагина (`wa-apps/shop/plugins/prefill/.git`, последний коммит `86656dc`). Проверено по правилу CLAUDE.md «код плагина строго внутри своего каталога» — правок вне плагина в этой сессии не было |
| L0.4 | Хардкод русского текста | ✅ OK — единственное срабатывание `ParamsChoiceManager.js:145` (debug-лог, не пользовательский текст), совпадает с ранее принятым в TEST-PLAN §L0 |
| L0.5 | Роуты оканчиваются на `/?` | ✅ OK |
| L0.6 | `php wa.php compress` | ✅ (= R-01) собирается без ошибок валидации, 144 файла / 1056.93 KB → архив 250.36 KB |

## L1 — автотесты (R-02 частично: состав архива не мёртвый — не перепроверялось отдельно)

21 файл, 0 провалов (было 15 на 28.08 — выросло за счёт `ZenLatchIdentityTest`, `ZenSummaryCacheIdentityTest`, `ZenCustomerGateSourceTest`, `GeoSyncDecisionTest`, `GeoIntegrationsAvailabilityTest`, `StaleFilePrunerTest` и др., добавленных с тех пор).

## L2 — curl-проверки

| # | Проверка | Итог |
|---|---|---|
| L2.1 | Гость не создаёт следов | ✅ только `PHPSESSID` (ставит SEO-регионы, см. TESTS.md) и `landing` (ядро) — `prefill_*` нет |
| L2.2 | Ассеты не висят на каталоге | 🟡 на момент проверки debug ещё был **on** — главная и карточка товара отдавали `prefill.debug.css`/`prefill.debug.js`. **Не регресс A1**: по коду и комментарию (`FrontendHooks::handleFrontendHead`) debug-панель сознательно рендерится на всех страницах магазина, в отличие от `frontend/order`-ассетов, которые остаются checkout-only (`initializeFrontendAssets()`). После переключения debug **off** и чистки кэша — перепроверено, 0 совпадений на главной (см. L3/J-01) |
| L2.3 | Ассеты есть на чекауте | ✅ покрыто J-02 в браузере (см. ниже) |
| L2.4 | Debug-эндпоинты закрыты анониму | 🟡 команда не выполняется (`{"status":"fail","errors":"Access denied"}`), но HTTP-код **200**, не 403 — расходится с паттерном L2.5 (`params-choice`/`apply-delivery` дают честный 403). Функционально безопасно, стилистическая нестыковка — записано в TODO как 🟢 |
| L2.5 | Гость не лезет в чужую историю | ✅ оба эндпоинта — 403 |

## L3 — браузерный регресс (частично)

Прогон шёл **с выключенным глобальным debug** (`wa-config/config.php: 'debug' => false`, восстановлено в конце) — впервые проверен путь прод-бандла `prefill.frontend.min.js?1.0.0`, на который до сих пор не было ни одного браузерного прогона (issue-87, TODO.md).

| ID | Сценарий | Конф. | Итог |
|---|---|---|---|
| A-01/B-01/F-01 `SMOKE` | K1, авторизованный (`admin`) с историей, свежая загрузка `/order/` на прод-бандле | K1, debug off | ✅ все три группы (Покупатель/Доставка/Оплата) свёрнуты с первого кадра, данные из истории, `console` без ошибок плагина (одна `console.error` от **core** `form.js` — не расследовалась, вне плагина) |
| F-02 `SMOKE` | Клик «Изменить» → разворот с полями | K1 | ✅ форма разворачивается, поля редактируемы |
| F-26 `SMOKE` | XSS в поле «Имя» (`<img src=x onerror=window.__xss=1>`) | K1 | ✅ `window.__xss` не установлен, тег виден как литеральный текст, двойного экранирования нет |
| F-07 | Сломать email → серверная ошибка, блокировка кнопок | K1 | 🟡 частично — введённый заведомо невалидный формат email ловится **клиентской** валидацией core до отправки на сервер, `data-blocked-by` не появляется. Это не тот же путь, что описан в сценарии (нужен semantically-invalid, но формат-валидный email/телефон, вызывающий ошибку именно от `calculate()`). Требует более точного триггера — не переигрывался за неимением времени в этом заходе |
| J-01/J-02 `SMOKE` | Каталог / `/order/` при debug off | K1, K16(off) | ✅ 0 упоминаний `plugins/prefill` на главной, полный набор ассетов на чекауте, прод-бандл грузится |
| K-01 `SMOKE` | Admin SPA `?action=plugins#/prefill` | K1 | ✅ загружается, все 4 вкладки (Основные/Дзен-режим/Дизайн/Отладка) кликабельны |
| D-01 (curl) | Чистая банка кук, каталог + карточка + `/order/` без истории | K2 | ✅ ни одной `prefill_*` куки до захода на `/order/`; после — только сессионные `prefill_zen_*=expanded` (латчи, не следы данных) |
| F-04 (curl) | Гость без истории — группы не сворачиваются | K2 | ✅ `data-nothing-to-summarize` × 3 в ответе `/order/`, ни одна группа не свёрнута |
| D-02 (браузер) | Гость оформляет первый заказ, `consent_required=on`, чекбокс согласия отмечен | K2 | ✅ заказ `#131` создан, `shop_order_params`: `prefill_guest_dd47196731a5df6087c97c52dc998e907da379ae783d698c`, `CHAR_LENGTH(name)=62` — ровно критерий из TEST-PLAN, `value=1`. Лог: `Guest order link saved`, `Guest token extended`, `Guest consent check: consent present` |
| D-03 (браузер) | Тот же гость возвращается (та же вкладка/токен) с новым товаром | K2 | ✅ покупатель/доставка(ПВЗ)/оплата(Наличные) предзаполнены из заказа `#131`; лог — `Source already applied in this session, skipping DB lookup` (маркер сессии сработал, повторного лукапа не было — согласуется с B-02) |

Тестовые данные восстановлены: поле «Имя»/«Email» контакта `admin` возвращены к исходным значениям; заказ `#131` и контакт `#69` (`TestGuest Runner`, созданы гостевым сценарием D-02/D-03) удалены через бэкенд после проверки — заказ помечен «Удалён» с комментарием, контакт удалён навсегда (не сотрудник, тестовые данные). Товар, оставшийся в корзине `admin` изначально — не трогался специально, был в корзине уже до начала прогона.

### 🔴 Значимая находка: переоткрыт `admin-contact-profile-overwritten-by-guest-checkout-data.md`

При подготовке к следующему сценарию (логаут admin → гостевой чекаут D-02/D-03 → логин admin обратно, всё в одной вкладке/`PHPSESSID`) чекаут-форма `admin` после повторного логина показала **данные гостя** (`TestGuest Runner`, телефон, доставка «Бесплатная доставка курьером» — тип, который в гостевом сценарии не выбирался вовсе) вместо истории `admin` (Новосибирск/ПВЗ, которая была видна в этом же прогоне на A-01/F-01 SMOKE чуть раньше). Воспроизвелось повторно после перезагрузки и после пересоздания корзины — не разовый рендер-глюк.

Это третье срабатывание того же класса, что и закрытый 07.09.2026 «разовый глюк» — но впервые с точной последовательностью повтора. Лог подтверждает: `user:1` (настоящий admin), `Prefill was evaluated but no params were filled` — то есть **плагин тут ни при чём**, эхо-кэш P9 лишь зафиксировал уже чужие данные, лежавшие в core-сессии `shop/checkout` до участия плагина. Сам `wa_contact` в БД в этот раз не пострадал (только сессия/рендер). Полный разбор, находки и гипотеза — в файле бага; глубоко не копал, как договаривались.

## Раунд 3 (прерван лимитом сессии инструмента)

После находки выше сессия `admin` вручную восстановлена через UI (ФИО, телефон, e-mail → реальные значения admin; доставка → ПВЗ Новосибирск, как в истории; оплата уже была верной — «Наличные»). Восстановление подтверждено повторной загрузкой `/order/` — данные остались корректными, не откатились к гостевым.

Начата проверка **G-01** (ФО-07, эхо-кэш P9: смена региона → выбор оплаты переживает пересчёт) — дошли до открытия селектора «Регион», но браузерный инструмент упёрся в лимит запросов сессии (`rate_limit_error`, 5h-окно исчерпано) прямо на этом шаге. Клик по региону не завершён, смена региона не выполнялась, вывод по G-01 отсутствует. ФО-07 (G-01…G-10) остаётся полностью непройденной.

Состояние на паузе: корзина `admin` содержит 1 товар («Торшер трапеция», 7000₽), чекаут развёрнут (не тестовые данные, реальные — ничего страшного, если оставить). Тестовых заказов в этом раунде не создавалось.

## Не прогнано и почему

| ID / область | Причина |
|---|---|
| ФО-02 (B-02…B-12), ФО-03 (C-02…C-07), ФО-05, ФО-07 (G-02…G-10), ФО-08, ФО-09, ФО-10 (J-03…J-11), ФО-11 (K-02…K-14), ФО-12 (L-01…L-06), ФО-13, ФО-14 (N-01…N-07 кроме уже покрытых) | Не хватило времени в рамках одного захода — R по оценке TEST-PLAN §1 занимает 1-2 дня, в этом заходе закрыта в основном автоматизируемая часть (L0-L2) плюс SMOKE-разведка 🔴-областей. Требует отдельных заходов, по области или блоку |
| L4 (совместимость) | Не начиналось — требует смены темы/установки плагинов, не совместимо с «глубоко не лезем» в рамках одной сессии |
| L5 (кроме R-01) | R-04/R-05 (чистая установка / апгрейд) требуют отдельного стенда, не выполнялись |
| K1 vs K8 methodological review (TODO.md, п. «Пересмотреть исторические K1-сценарии») | Не начато — отдельная задача, требует сверки исторических прогонов |
| Матрица §4 (K3, K5-K17) | Не задействована ни разу в этом заходе — весь браузерный блок шёл на K1/K2 |

**Рекомендация:** не считать этот прогон закрывающим TODO.md-пункт «🔴 Прогнать релизный режим R целиком» — блокер остаётся открытым, но сузился: слепой участок «прод-бандл никогда не тестировался в браузере» закрыт (F-01/A-01/B-01 SMOKE на debug off прошли живьём). Следующий заход — целиком по ФО-02…ФО-14 сценариям §6, отдельными сессиями по 2-3 области.

## Что трогалось и возвращено

| Настройка / данные | Было | Сделали | Вернули |
|---|---|---|---|
| `wa-config/config.php: debug` | `true` | `false` (для прогона прод-бандла) | ✅ `true` |
| Контакт `admin`: имя | `Admin` | `<img src=x onerror=...>` (XSS-проверка) | ✅ `Admin` |
| Контакт `admin`: email | `artem.shamberger@gmail.com` | `not-an-email` (проверка ошибки email) | ✅ `artem.shamberger@gmail.com` |
| `wa-cache/*/apps/`, `wa-cache/*/config/` | — | очищен дважды (после смены debug, в конце) | ✅ |
| Заказ `#131` (гость `TestGuest Runner`, D-02/D-03) | не существовал | создан гостевым сценарием | ✅ помечен «Удалён» через бэкенд, причина в комментарии |
| Контакт `#69` (`TestGuest Runner`) | не существовал | создан заказом `#131` | ✅ удалён навсегда через бэкенд |
| Сессия чекаута `admin` (courier + чужие ФИО, см. находку выше) | — | заражена в ходе воспроизведения D-02/D-03-сценария | ⚠️ **не восстановлена вручную** — это и есть находка, трогать её означало бы затереть улику. Данные не персистентны (только сессия), самостоятельно сойдут на нет при следующем реальном оформлении заказа `admin` |

`shop_prefill_settings` — diff до/после всего прогона пуст, ничего не сохранялось через SPA.

## Логи

PHP-лог (`errors.log`): **чист**, 0 новых строк за весь прогон (оба раунда).
Лог плагина: 2037 новых строк суммарно, **0 `[ERROR]`**. Разбор по требованию.
БД на конец прогона: `MAX(shop_order.id)=131` (тестовый заказ `#131` создан и мягко удалён — id не переиспользуется, это ожидаемо), `MAX(wa_contact.id)=68` (тестовый контакт `#69` создан и удалён навсегда — id снова не занят).
