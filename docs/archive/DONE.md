# Архив закрытых задач — Prefill Plugin

Закрытые пункты из [TODO.md](../TODO.md), перенесённые сюда при уборке 11.09.2026, чтобы рабочий файл не разрастался. Порядок и формулировки не менялись — только перенос строк. Все связанные файлы (`codereview/`, `bugs/`, `todo/`, `plans/`) для закрытых задач лежат в своих `done/` подпапках.

**Искать здесь:** grep по названию файла или issue-номеру, либо просто Ctrl+F по тексту задачи — каждая строка самодостаточна и хранит дату и причину закрытия.

История — в релизах и коммитах.

---

## 🧪 Тесты перед релизом — закрытые прогоны

- [x] Тесты на старых версиях Webasyst — найти минимальную версию, указанную в конфиге как поддерживаемая (первая с новым дизайном чекаута)
- [x] Обновить Webasyst до последней версии и проверить, что плагин по-прежнему работает
- [x] Собрать несколько тестовых магазинов с разными вариантами корзин (доставка/оплата/кастомные поля) и прогнать сценарии предзаполнения через ИИ

---

## ✅ Сделано

### Баги

- [x] Прогнать [чек-лист вне режима R](../tests/pre-release-checklist.md) — прогнан и завершён 11.09.2026 по решению пользователя. Секция 1 (29.08) 3/4, секция 2 (PHP 8.4) 6/6, секция 3 (cityselect) 6/8 — оба оставшихся в файле `[ ]` (мобильная ширина 375/390px и `dp_plugin_*` в `forget()`) держатся не по недосмотру, а по объективным ограничениям этого захода: нет CDP `Emulation.setDeviceMetricsOverride` в наборе браузерных инструментов сессии, и плагин «Доставка и платежи» не установлен на стенде — подробности и точные шаги для следующего раза в самом файле

- [x] Прогнать тесты на 8 версии PHP — 11.09.2026, [чек-лист вне режима R](../tests/pre-release-checklist.md) п.2: сайт временно переключён на PHP 8.4 через ServBay, L0 (110 файлов, 0 ошибок), L1 (29 файлов, 597 проверок, 0 провалов), браузерный SMOKE A-01/B-01/F-01 — всё чисто, сайт возвращён на 7.4
- [x] 🟢 PHP 8.4: `shopPrefillPluginZenData::__construct(waView $view = null)` ловил deprecated «Implicitly marking parameter as nullable» — найдено и исправлено 11.09.2026 при том же прогоне: `?waView $view = null`, единственное такое место в плагине, живьём подтверждено на реальном стенде с временно переключённым на 8.4 сайтом

- [x] ~~🟠 Шире проверить фикс подсветки «Мои варианты»~~ [закрыто как дубликат](../bugs/done/params-choice-stale-highlight-after-type-switch.md) — 11.09.2026: перечень непройденных сценариев (`is_minimal_mode`-вёрстка, легаси UI 1.3, темы кроме `default`, побочный эффект на payment/auth/comment) дословно совпадает со сценарием E-07 в [TEST-PLAN.md §6](../tests/TEST-PLAN.md); он входит в релизный режим R (см. «Прогнать релизный режим R» выше) — отдельно не отслеживаем, чтобы не дублировать чек-лист
- [x] Восемь пробелов автотестов из [TEST-PLAN.md §5](../tests/TEST-PLAN.md#5-пробелы-автотестов--что-дописать) закрыты 11.09.2026: 🔴 `HelperDeepMergeTest` (P9, немутирующий `deepMergeArrays()`/`stripEmptyLeaves()`, 16 проверок), `GuestTokenFormatTest` (P6, формат токена/lookup id и семь видов мусорной куки, 19 проверок), `ZenSummaryCacheTest` (R4, `hasFreshData()`/`set()`/`get()`/`clear()` — функциональная часть, не покрытая `ZenSummaryCacheIdentityTest`, 18 проверок), `ZenFieldContractTest` (Z7, структурный замок между `is_html` в `ZenData::getAvailableFields()` и вручную продублированным списком в `ZenSummaryEscapeTest`, 51 проверка); 🟠 `SettingsSchemaTest` (схема `storefront.settings.php`: opt-in выключен по умолчанию, `my_delivery_variants_limit` в 1–10, `filter` соответствует типу `value`, 77 проверок), `StorefrontCollectionTest` (B3, `getByCode()`/`getTree()`/`toJson()`, плюс коллекционный аналог реального инцидента с общим `checkout_storefront_id` — первая добавленная витрина побеждает, 20 проверок), `CssManagerPathTest` (A1, cache busting по `update_time` и защита `getFilePath()` от path traversal на шести вредоносных кодах, 23 проверки), `LocaleCompletenessTest` (все литеральные ключи `_wp()` из `lib/` и `templates/`, оба синтаксиса, есть в обоих `.po` без пустых `msgstr`, 9 агрегированных проверок). Плюс инфраструктура: `tests/run.sh` (A11, суммарный код возврата и строка «N файлов, M проверок, K провалов») и мутационная проверка каждого из восьми файлов (A12) — на каждый внесён точечный регресс в исходнике, подтверждено падение теста, откат через `git checkout --`. Итог: 29 файлов, 597 проверок, 0 провалов. Детали и число проверок по каждому файлу — [tests/TESTS.md](../tests/TESTS.md); в списке пробелов остались только 🟢 `LogReaderTest`/`RoutingContractTest` (TEST-PLAN.md §5) — вне текущего релизного блокера
- [x] 🟢 ~~`use_storage` ядра гасится после смерти сессии~~ [закрыто как неприменимо](../bugs/done/use-storage-restoration-suppressed.md) — бэклог-пункт с 08.09.2026 (найден при разборе f01), закрыт 11.09.2026 живыми замерами: 3 curl-прогона на чистых банках кук плюс контрольный с полностью выключенным prefill. Для гостя **без истории заказов** флаг гасит плагин «SEO-регионы» (автоопределение города на хук `routing`, срабатывает на любой странице сайта до чекаута, воспроизведено и без единой строчки нашего кода) — к prefill отношения не имеет. Для покупателя/гостя **с историей** флаг действительно гасит наша запись, но цена невысокая (после долгого простоя виден последний заказ, а не пустая форма) — чинить не стоит. Старые ссылки на «случай 11» — [issue-65](../codereview/done/issue-65-prefill-overrides-current-input.md) и [план по f01](../plans/done/zen-customer-gate-source-alignment.md), отвергнутый вариант 3
- [x] 🟢 Debug-эндпоинты (`clear-storage`/`force-prefill`/`reset-and-refill`) отдают анониму HTTP 200 с телом `{"status":"fail","errors":"Access denied"}` вместо честного 403 — найдено 09.09.2026 при прогоне R ([отчёт](../tests/runs/1.0.0-2026-09-09-full.md#l2--curl-проверки), L2.4). Исправлено 11.09.2026: `wa()->getResponse()->setStatus(403)` добавлен в общий `shopPrefillPluginFrontendDebugBaseController::execute()` (чинит разом все четыре debug-контроллера, включая `Logs`), по образцу `apply-delivery`
- [x] ~~🔴 **Профиль контакта admin получает гостевые данные чекаута**~~ [закрыто — баг ядра, не плагина](../bugs/done/admin-contact-profile-overwritten-by-guest-checkout-data.md) — переоткрыто 09.09.2026 (третье срабатывание, впервые с точными шагами повтора), причина найдена и подтверждена чтением кода ядра 11.09.2026: `shop/checkout` в сессии не привязан к личности и не чистится логаутом (`shopFrontendOrder.actions.php`, ключ удаляется только при создании заказа), гард на чужой ввод в `shopCheckoutAuthStep` гасит только пустые значения — непустые гостевые проходят насквозь (`array_filter()` без колбэка), а `shopOrder::saveCustomer()` при оформлении заказа пишет их прямо в `wa_contact`/`wa_contact_emails` авторизованного контакта. Плагин ничего не пишет — собственный лог подтверждает `no params were filled`, P9-эхо лишь зеркалит уже отравленную ядром сессию. Снято с блокеров релиза плагина — чинить нечего в `wa-apps/shop/plugins/prefill`, правка принадлежит ядру (`wa-apps/shop/lib/`)
- [x] 🟠 ~~[Первый клик «Подтвердить заказ» после починки email не оформляет заказ]~~ [закрыто — баг ядра, не плагина](../bugs/done/zen-double-submit-after-email-fix.md) — воспроизведено и разобрано инструментально 08.09.2026 (трассировка нативных событий мыши): клик физически не долетает до кнопки — `Form.prototype.update` ядра синхронно вешает `is-locked` на форму в обработчике `change` между `mousedown` и `mouseup`, из-за смены hit-target браузер генерирует `click` на обёртке формы, не на кнопке. Класса `is-locked` в плагине нет ни строки (grep по js/css/php/html), Zen ни при чём — заглушка карточек не может рассинхронизироваться структурно: CSS сворачивания генерируется тем же ответом сервера, что и скрываемая секция. Второй клик срабатывает штатно. Гостевой путь отдельно не проверялся и не нужен: причина в порядке нативных событий мыши, одинакова для гостя и покупателя, ветки `auth` не касается. Открыт один внешний шаг вне TODO — сообщить о баге в трекер Shop-Script/Webasyst, требует решения пользователя, отправлять ли
- [x] 🔴 [`$delivery_schedule` всегда пуст в Zen-сводке — читается не тот источник данных](../bugs/done/zen-delivery-schedule-source-missing.md) — исправлено 08.09.2026: источник переключён на `vars.details.shipping_rate`, попутно рендерер догнал разметку ядра (`additional`, `title` с часовым поясом); подтверждено вживую на СДЭК ПВЗ
- [x] 🔴 [`$delivery_photos_html` и `$delivery_schedule` — вёрстка ядра не работает в Zen-карточке](../bugs/done/zen-photos-css-scope-broken.md) — найдено 29.08.2026, исправлено 08.09.2026: оба поля переведены на собственные классы `.prefill-zen-photos`/`.prefill-zen-schedule` и собственный CSS (миниатюра была 561×0 px, стала 77×58; день расписания 42 px в две строки → ~23 px в две колонки). Классы ядра не дописывали: их CSS приложенческий и с захардкоженными цветами, а совпадение имён подставляет карточку под правила темы (у темы `default` уже есть своё правило на `.wa-date`)
- [x] 🔴 **Разобрать результаты прогона по [zen-fields-render-test-plan.md](../tests/zen-fields-render-test-plan.md)** (29.08.2026) — закрыто 08.09.2026: оба бага, на которые указывала задача, исправлены и подтверждены вживую (два пункта выше — источник данных и вёрстка). Непроверенное из того же захода вместе с ней не закрывается, вынесено отдельной строкой в тесты выше
- [x] ~~🟠 **Эхо-кэши `payment`/`delivery` не привязаны к личности**~~ [закрыто как неприменимо](../codereview/done/issue-90-echo-cache-identity-scope.md) — найдено и закрыто 07.09.2026 при разборе [кэша сводки Zen](../plans/done/zen-summary-cache-identity-scope.md). Штампа владельца в эхо действительно нет, но лечение «как у Zen-кэша» здесь не работает: логин в чекауте — не отправка формы, а `location.reload()` (`form.js:3632`), на перезагрузке ядро кормит `processAll()` прямо из `shop/checkout`, секции гостя приезжают непустыми, и эхо **перезаписывается** теми же данными под новой личностью раньше, чем ветка восстановления получит управление. Переносит данные через смену личности `shop/checkout` ядра, эхо лишь зеркалит его; плагин добавляет один шаг — переживает `html: 'only'`, которым ядро само стирает `payment`. ФИО/email/телефон/улица в эхо не попадают. Остаточный риск (общий компьютер, первый ушёл не разлогинившись) принят осознанно; четыре отвергнутых варианта и условия переоткрытия — в файле
- [x] ~~🔴 **РЕГРЕСС**~~ [Ошибка на шаге `auth` уничтожает выбор доставки](../bugs/done/shipping-payment-identity-lost-after-snapshot-removal.md) — **закрыто 07.09.2026 как невоспроизводимое.** Сам баг (ядро рвёт конвейер на ошибке `auth` и стирает выбор) реален и закрыт эхо-кэшем ещё 24.08. Запись о регрессе 05.09 сегодня не подтвердилась: 4 цикла «сломал email → починил» на том же типе доставки с составным `variant_id` (Самовывоз+ПВЗ `43.NSK2:136:270`), обоими способами ввода (`form.update()` и живая клавиатура) — выбор восстанавливается каждый раз, в логе `Delivery section restored from echo cache`. Код механизма с 24.08 не менялся (правка 06.09 в `SectionChecker` только добавила диагностические методы, критерий `isSectionMechanicallyClean()` не тронут). **Причина наблюдения 05.09 осталась неизвестной** — гипотеза про транзитное окно ПВЗ (тип выбран, `variant_id` ещё пуст) разобрана и отвергнута: ветка стирания в этом окне работает верно, там у покупателя и на экране нет выбора, защищать нечего. Поэтому критерий не трогали. Страховка на случай рецидива — логирование состава секции в обеих ветках стирания (коммит `44f6e1a`): теперь в логе видно, что именно пришло в `order.shipping`/`order.payment` и что выброшено. Заодно вживую подтверждено, что **D1 закрыт**: `data-blocked-by` ставится сервером на все переключатели обеих групп, «Изменить»/«Свернуть» дают диалог «Сначала нужно исправить ошибку… Ваши данные сохранены и вернутся сами», разворота в пустоту и осиротевшей кнопки «Свернуть» нет. **09.09.2026: все 15 сценариев таблицы «Проверять после фикса» закрыты.** 7 (смена типа доставки без выбора варианта — `Delivery echo cleared`, оплата пережила по P9), 9 (та же смена + чужая ошибка auth — группа развёрнута без ложной сводки, эхо было уже пусто), 12 (пустой `firstname` — `delayed_errors` не короткое замыкание, ни один эхо-путь не сработал, всё осталось как было), 13 (гость с пустой банкой кук — эхо не пишет в сессию, когда восстанавливать нечего), 14 (оформленный заказ #100129 не протёк в следующий чекаут той же сессии — `clearPaymentEcho()`/`clearDeliveryEcho()` в `orderActionCreate()`) прогнаны вживую одним связным заходом на `wa-dev.loc/*`. 8 (`fast_render` на первой отрисовке) закрыт по совокупности наблюдений прогона R (TESTS.md, G-03) и автотесту `CheckoutStateFastRenderTest.php`, отдельно не переигрывался. Заодно поправлена стухшая строка 15 в самой таблице (была `⬜`, хотя TODO уже отмечал её закрытой 06.09.2026 плагином «Курьер»)

- [x] [Диалог выбора области сохранения (SaveScopeDialog) не поддерживает тёмную тему](../bugs/done/save-scope-dialog-no-dark-theme.md) — исправлено 07.09.2026: перенесён фикс с `.prefill-confirm-dialog` (переопределение фона/цвета/тени под `html[data-theme="dark"]`); проверено в браузере на бэкенде в тёмной теме
- [x] 🔴 [Кука-защёлка Zen переживает смену PHP-сессии и глушит все три группы после гостевого визита](../bugs/done/zen-collapse-latch-outlives-php-session.md) — найдено 08.09.2026 при перепроверке f01 в браузере, **исправлено в тот же день** по [плану](../plans/done/zen-collapse-latch-identity-scope.md): защёлки привязаны к личности отдельной кукой-владельцем `prefill_zen_owner`, чужие снимаются перед первым же чтением. Штамп внутри значения защёлки отвергнут при разборе кода — её пишет и `ZenModeToggle.js` на клике «Изменить», и штамп потребовал бы прокидывать отпечаток в шаблоны и JS; отдельная кука не трогает ни формат защёлок, ни клиент. Протокол куки вынесен из `ZenMode` в `shopPrefillPluginZenLatch` — координатор при этом лишился `request`/`response`, они жили только там. Проверено четырьмя сценариями curl-ом и живым браузерным прогоном (репро, Z4 настоящим кликом «Изменить», логаут, оформление заказа со сбросом), `tests/ZenLatchIdentityTest.php` (26 проверок), все тесты плагина зелёные
- [x] 🔴 [Группа «Покупатель» (customer) не сворачивается в Zen-режиме на первом кадре](../bugs/done/zen-customer-group-never-collapses-f01.md) — найдено 28.08.2026 при прогоне R (ФО-06, F-01), корень найден и **исправлено 08.09.2026** по [плану](../plans/done/zen-customer-gate-source-alignment.md): гейт группы `customer` спрашивал `order.auth.data.*` из сессии чекаута, куда для авторизованного покупателя эти данные не пишутся by design, а сводка строилась из `$state` (`vars.auth.fields`) — источники разошлись. Дефект оказался регрессом от 20.08.2026: проверка данных для Z2 введена тогда и починила гостя, но сломала авторизованного (до неё группа сворачивалась всегда, включая гостя с пустой формой — обратный дефект). Гейт `customer` переведён на те же геттеры, что строят сводку, под гардом `isStepSkipped('auth')`; `delivery`/`payment` не тронуты. Проверено дважды — curl-ом на холодной банке и в живом браузере (три сценария: авторизованный на первом кадре, гость-негатив с `data-nothing-to-summarize`, Z4 не схлопывается посреди ввода), `tests/ZenCustomerGateSourceTest.php` (13 проверок), все 18 тестов плагина зелёные
- [x] 🟠 [Кэш сводки Zen показывает данные предыдущей личности после смены гость↔авторизован в той же сессии](../bugs/done/zen-summary-cache-leaks-across-identity-change.md) — найдено 28.08.2026 при прогоне R (ФО-05); **исправлено 07.09.2026** по [плану](../plans/done/zen-summary-cache-identity-scope.md): в записи кэша появился штамп владельца (отпечаток источника), как уже сделано в `GeoStorage`. Заодно выяснилось, что утечка шире исходной формулировки — заказ оформлять не требуется, достаточно свернуть карточку гостем и войти в той же сессии. Проверено в браузере двумя прогонами (репро + негативный, что кэш продолжает работать своей личности), автотест `tests/ZenSummaryCacheIdentityTest.php`
- [x] 🟠 [Настройки витрины теряются при любом изменении её адреса](../bugs/done/storefront-settings-orphaned-by-url-promotion.md) — найдено 05.09.2026 при подготовке D-07 как частный случай («витрина `wa-dev.loc/*` живёт только фоллбэком»), механизм установлен и исправлено 07.09.2026: код витрины считался как `base64(domain/url)`, а адрес маршрута ядро переписывает молча — «сделать главной страницей» (`siteMapSetMainPage` не шлёт ни одного события), переименование URL раздела или домена. Идентичность переведена на `checkout_storefront_id` — тот же ключ, по которому ядро хранит конфиг чекаута; для маршрутов без него (заведены до checkout2, id задним числом не выдаётся) оставлен фоллбэк на прежнюю схему. Баг воспроизведён штатными кликами в интерфейсе до фикса и проверен после — переименование URL и «сделать главной» с возвратом настройки не теряют. План — [storefront-identity-by-checkout-id.md](../plans/done/storefront-identity-by-checkout-id.md). Миграции не потребовалось — плагин не выпущен
- [x] ~~[В shop_prefill_settings есть строки с storefront_code = '-']~~ [закрыто — не баг](../bugs/done/settings-storefront-code-dash-row.md) — 07.09.2026: `-` это штатный код общеплагинных настроек, объявленный в `shopPrefillPluginSettingProvider` (`private const CODE = '-'`), а `lib/config/settings.php` содержит ровно те четыре ключа, что и наблюдались в базе. Все три «независимых воспроизведения» были сохранением общеплагинных настроек, а не сбоем. Тот же корень, что у закрытого 06.09 [settings-active-toggle-visual-state-desync.md](../bugs/done/settings-active-toggle-visual-state-desync.md)
- [x] [Глобальная настройка «Иконки в свёрнутых блоках» (`zen.icon_display`): «По умолчанию» и «Логотипы плагинов» неотличимы](../bugs/done/zen-icon-display-default-vs-plugin-has-no-effect.md) — найдено 05.09.2026 при прогоне R (ФО-06, F-19), исправлено 06.09.2026: не чинили различение `default`/`plugin`, а убрали саму глобальную настройку и добавили `none` в per-group `icon_source` delivery/payment (у customer такая опция уже была) — теперь у всех трёх групп один самодостаточный селект вместо глобального гейта поверх трёх независимых. Заодно переименована соседняя настройка «Размер иконок» → «Размер иконок в свёрнутых блоках» (без `icon_display` рядом теряла смысл). 18 автотестов зелёные, вживую проверено на payment (`icon_source=none` → свёрнутый блок без иконки), значение вернул к исходному после проверки. Миграции для живых инсталляций не потребовалось — плагин ещё не продавался
- [x] 🟢 [Кнопка «Свернуть» на пустой секции auth: клик гонял recalculate вхолостую, без единого сообщения](../bugs/done/zen-collapse-noop-when-section-empty.md) — найдено и исправлено 06.09.2026: сервер помечает кнопку `data-nothing-to-summarize`, JS после пересчёта показывает отдельный диалог. Поведение сервера не менялось — пустая группа по-прежнему не сворачивается (Z2), защёлка Z4 не тронута. Приоритет понижен до 🟢: на боевых витринах у секции покупателя почти всегда есть обязательное поле, и там уже сегодня срабатывает штатный диалог валидации; молчание было только при полностью необязательной секции. В файле — два отвергнутых варианта с замерами (прятать кнопку нельзя: при защёлке это единственный путь к свёрнутому блоку; снятие защёлки не чинит [f01](../bugs/done/zen-customer-group-never-collapses-f01.md) и ломает Z4)
- [x] [Дебаг-панель: хардкод русского текста в JS (alert/confirm/innerHTML)](../bugs/done/debug-panel-hardcoded-russian-strings.md) — исправлено 05.09.2026: строки локализованы для ru_RU/en_US, JS не содержит кириллицы и не собирает HTML; русский интерфейс проверен в Chrome
- [x] 🟠 [Диалоги подтверждения (сброс CSS, очистка лога, кнопки плавающей панели) используют нативный `window.confirm()`, не шаблон плагина](../bugs/done/native-confirm-dialogs-not-templated.md) — исправлено 06.09.2026: настройки (`prefillConfirm()` на `$.waDialog` + `<template>`, как `SaveScopeDialog`) и плавающая панель (нативный `<dialog>`/`showModal()`, т.к. рендерится без гарантии jQuery) переведены на шаблонные диалоги; заодно добавлена поддержка тёмной темы бэкенда для нового диалога настроек (ядровый `$.waDialog` её не поддерживает). Проверено в браузере: light/dark тема бэкенда и плавающая панель на витрине, вкладка не подвисает, отмена/крестик/Esc не выполняют действие
- [x] ~~[При `rememberme=false` на домене не появляется ни токен, ни предупреждение в логе]~~ [закрыто как ложное срабатывание](../bugs/done/remember-me-domain-disabled-warning-missing.md) — 06.09.2026: `[WARNING]` реально писался (заказы #120/#121), просто в другой файл — `shopPrefillPluginLog::warning()` всегда пишет в `prefill.plugin.error.log`, а не в `prefill.plugin.log`, который проверяли. TESTS.md I-05 переведён в ✅

- [x] ~~🔴 `zen.active = false` не отключает Zen-режим~~ [закрыто как ложное срабатывание](../bugs/done/zen-active-off-not-respected.md) — 05.09.2026: тестовая витрина `wa-dev.loc/*` не имела собственного `active=true`, поэтому фоллбэчила на глобальную `*`, и override `zen.active=0` физически не мог сработать (код не читал эту витрину). Проверено заново на глобальной `*` — `zen.active=false` полностью отключает Zen-режим (нет `zenmode.css`, нет `prefill-zen-*` классов), как и требуется

- [x] [Настройки плагина не рендерятся на классическом бэкенде (UI 1.3): `$switcher.waSwitch is not a function`](../bugs/done/settings-waswitch-missing-legacy-ui.md) — найдено и закрыто 30.08.2026 как неприменимое: плагин поддерживает только дизайн-систему Webasyst 2.0, UI 1.3 не заявлен. После включения 2.0 на Docker-стенде `wa.js`/`waSwitch` доступны, настройки открываются штатно
- [x] [Апостроф в переводе ломает Smarty-шаблон на минимально заявленной версии Webasyst](../bugs/done/smarty-quote-break-old-webasyst.md) — исправлено 30.08.2026: прямой апостроф `'` в EN-переводах `hint.prefill.guest.enabled` и `hint.styles.accent_color_dark` заменён на типографский `’` (U+2019) — обе строки подставляются локаль-прификтером внутри одинарных кавычек Smarty-атрибута, прямой апостроф разрывал парсинг на `webasyst-framework v2.9.0`. Проверено вживую на Docker-стенде под минимальную версию (`~/Project/shop10-docker`): страница настроек плагина открывается, исправленный текст подтверждён в живом DOM. РУ-переводы и остальные 51 из 53 мест с локаль-плейсхолдером в одинарных кавычках апострофов не содержат — не тронуты
- [x] 🟠 **Этап 1 интеграции с плагинами выбора города** — 26.08.2026. Город из последнего заказа передаётся CitySelect и «SEO-регионам» их же публичными функциями до запуска определения по IP. Проверено в браузере на `user:1` (последний заказ — Новосибирск): город в шапке и в корзине совпал, скрипт `shop_cityselect.detect()` из разметки исчез, окно «Ваш город?» не показано, ручная смена города покупателем даёт `Geo sync backs off`. Регресс с выключенным тумблером: ноль гео-активности, поведение стоковое. Автотест `tests/GeoSyncDecisionTest.php`, 18 проверок. Этапы 2-4 — в Backlog

- [x] 🟠 **Этап 2 интеграции: предложение перехода на витрину своего города** — 27.08.2026. Там, где город покупателя привязан к другой витрине, `shopPrefillPluginGeoAdapterRegions::propose()` кладёт его в слот «результат определения» «SEO-регионов» (`detected_location`) — их окно показывается и называет наш город вместо догадки по IP, а переход выполняет их `changeCity()` и только по клику покупателя (G4). У CitySelect `propose()` осознанно `false`: их нотифаер по «Да» не редиректит, а показывает город из куки, которую в этой ветке мы не пишем. Заодно чинится `prefill_geo` — кука переставляется своими атрибутами после переноса между поддоменами (соседи переносят её без `httponly`, а `regions` ещё и без срока жизни); `prefill_guest_token`/`prefill_consent` в починке не нуждались — они и так переставляются на каждом рендере. Проверено вживую: город на чужой витрине (Казань → `shop-1/*`) → предложение записано, текущая витрина не подменена, повтор в сессии не дублируется → клик `?shop_plugins_regions[proposed]=1` (тот же параметр, что шлёт их кнопка) → реальный переход `changeCity()`, `wa-dev.loc/` → `wa-dev.loc/shop-1/`, город на месте. Правки целиком внутри `lib/classes/geosync/`, путь предзаполнения не тронут; 15 существующих автотестов зелёные

- [x] 🟢 **Второй поддомен на стенде и живая проверка починки `prefill_geo` (п. 2.3)** — 27.08.2026. `shop-1.wa-dev.loc` заведён как настоящий отдельный домен (через `update_website` ServBay MCP — см. [CLAUDE.md](../../../../../../CLAUDE.md#несколько-витрин-на-стенде) фреймворка), Казань перепривязана на него в `shop_regions_city.storefront`. Повторён сценарий этапа 2 уже с реальным `env`-переносом `changeCity()`: сессия перенеслась (`PHPSESSID` на новом домене), `prefill_geo` невидима из JS до и после перехода, повторная загрузка ничего не меняет. Побочная находка: на самом первом запросе после перехода (несущем `env_key`) покупатель на один хоп виден нашему коду анонимом — `controller_before.*` у prefill срабатывает раньше, чем `regions` в `frontend_head` восстанавливает `session_id()`. Безвредно для этапа 2, но важно для этапа 5. Подробности — в [city-sync-regions.md](../todo/done/city-sync-regions.md#пункт-23-починка-prefill_geo-при-переносе-между-поддоменами--проверено-27082026-на-настоящем-поддомене)

- [x] 🟠 **«SEO-регионы» установлены на dev-инсталляцию и адаптер проверен вживую** — 26.08.2026. Стенд, приём с `<title>` для чтения текущего города плагина, разбор его проверки лицензии и таблица восьми проверенных сценариев — в [city-sync-regions.md](../todo/done/city-sync-regions.md). Подтверждены оба стоп-условия этапа 1: город чужой витрины (Казань → `shop-1/*`) и город не из списка плагина (Омск) — не вмешиваемся, редиректа нет

- [x] 🟠 **Тумблер интеграции без установленного плагина ничего не включает** — 26.08.2026. Единственный источник правды — `shopPrefillPluginGeoIntegrations`: доступность плагина перекрывает хранимое значение и на чтении настроек, и на записи, и в адаптерах. Тумблер отсутствующего плагина не рендерится, а его `true` гасится в `0` при первом же сохранении. Умолчание у всех трёх интеграций — выключено. Автотест `tests/GeoIntegrationsAvailabilityTest.php`, 15 проверок

- [x] [Диалог «Мои варианты»: подсветка не появляется сразу после смены «Типы доставки»](../bugs/done/params-choice-stale-highlight-after-type-switch.md) — 23.08.2026, вместо точечного поля `variant_id` подсветка теперь строится из полного снимка формы (`Form.getFormData()` ядра) вместо сессии — закрывает не только исходный симптом, но и параллельно найденный (короткое замыкание валидации вымывает `details.shipping_address.street`, а сравнение идёт по всему адресу). 2 сценария подтверждены в браузере через `is-active`; более широкая проверка — отдельным пунктом в «🧪 Тесты перед релизом» выше
- [x] [Zen сворачивал группы, которые ядро не обработало из-за ошибки на шаге выше](../bugs/done/zen-collapse-on-upstream-checkout-error.md) — решение по сессии + кэш сводки, 19.08.2026; автотест 132 проверки. Сценарии 1, 1a, 2, 4, 4a, 4b, 6, 8 пройдены в браузере, 3 и 7 закрыты разбором, 5 вынесен отдельно (issue-75) и подтверждён вживую 09.09.2026. ⚠️ 24.08.2026 сценарии 1/1a/2 переоткрывались регрессией снятия снапшота и в тот же день повторно закрыты эхо-кэшем — см. [shipping-payment-identity-lost-after-snapshot-removal.md](../bugs/done/shipping-payment-identity-lost-after-snapshot-removal.md). При повторной проверке найден смежный, отдельный дефект той же карточки — [payment-zen-card-vanishes-on-shortcircuit.md](../bugs/done/payment-zen-card-vanishes-on-shortcircuit.md) — закрыт, см. «✅ Сделано» ниже
- [x] [session_is_alive не прокидывается в AJAX-рендере чекаута — риск перезаписи localStorage](../bugs/done/session-is-alive-storage-overwrite.md) — закрыта 24.08.2026 как неприменимая: сам дефект core подтверждён вживую (curl + браузер, PHP notice `Undefined index: session_is_alive` в HTML-ответе `calculateAction()`), но путь эксплуатации из issue — нет. Смена региона/способа доставки идёт посекционным `Section.update({reload:true})`, а не через `Form.prototype.reload()` — единственный метод, который пересобирает форму целиком и заново гоняет уязвимый инлайн-скрипт. `Form.prototype.reload()` независимо подтверждён мёртвым кодом в [issue-65](../codereview/done/issue-65-prefill-overrides-current-input.md) (ревизия 4) и перепроверен здесь тем же grep'ом и живым кликом в браузере (тип доставки Курьер → Самовывоз не пересоздаёт `Form`). Латентный баг core остаётся, фикса в плагине не требует — условие переоткрытия описано в файле

### Issue ревью, закрытые в v1.0

- [x] 🟢 [Мёртвый код полного ревью, 11 методов](../codereview/done/issue-97-dead-code-full-review.md) — удалено 11.09.2026: 10 методов без вызывающих (в т.ч. `renderGroupSummary()`, `clearEffectiveStorefrontCache()` — отдельно проверено, что это не забытый вызов, а защита от сценария, структурно невозможного в PHP-FPM) плюс сломанный шим `attachDialogCloseHandler()`. `hasDataForSection()` оставлен под резерв issue-84 §2. `php -l` чист, все 21 тест зелёные
- [x] 🟢 [Мелочи полного ревью, шесть штук](../codereview/done/issue-98-minor-findings-pass-4.md) — все закрыты 11.09.2026, план и статус по каждому пункту в [issue-98-minor-findings-fix-plan.md](../plans/done/issue-98-minor-findings-fix-plan.md): ✅ диагностика `checkoutBeforeAuth` при выключенной панели исправлена; ✅ индекс по `storefront_code` добавлен (был full scan на каждое чтение настроек); ❌ `rememberMe()` без `cookie_domain` ядра закрыто как ложная находка — у ядра самого нет такого значения ни в одном известном пути, копировать нечего; ✅ `prefill/logs` подключён к общей CSRF/method-проверке (единообразно с тремя соседними debug-эндпоинтами, не пересекается с issue-79 (другой набор публичных эндпоинтов, закрыт отдельно — см. ниже)); ✅ диалоги фронтенда переведены на `<template>` (вёрстка сверена до/после через `git stash`, DOM побайтово идентичен); ❌ потолок по истории заказов в `collectUniqueDeliveryOrderIds()` отклонён — воспроизводил бы баг, убранный в issue-68 (потеря редкого варианта доставки)
- [x] 🟠 [Кука `prefill_zen_owner` отдаёт `contact_id` клиентскому JS](../codereview/done/issue-99-zen-owner-cookie-leaks-contact-id.md) — исправлено 09.09.2026, в день находки: кука получила `secure`/`httponly` (единственная серверная кука плагина, у которой их не было), хеш владельца посолен install-специфичным `identity_hash` (`wa('shop')->getConfig()->getIdentityHash()`) — несолёный `substr(md5('user:'.$id), 0, 12)` разбирался перебором 2 млн вариантов за 0.54 с. Проверено `tests/ZenLatchIdentityTest.php` (26 проверок) и живым curl-прогоном на стенде (`secure; HttpOnly` в заголовке)
- [x] 🟠 [`AGENTS.md` описывал вырезанную архитектуру](../codereview/done/issue-101-agents-md-describes-removed-architecture.md) — исправлено 09.09.2026, в день находки: файл перегенерирован из `CLAUDE.md` (расходятся только шапка-адресат и хвост Cowork), убраны `prefill.on_entry`, `prefill_guest_hash`, классы `FillParamsStorage`/`GuestHashStorage`, «тестов нет» при 19 зелёных. Заодно в самом `CLAUDE.md` убран снятый `shop/prefill_snapshot` из описания Data Flow
- [x] 🟢 [Мелочи повторного полного прохода, пять штук](../codereview/done/issue-100-minor-findings-pass-5.md) — исправлено 09.09.2026, в день находки: гард на повторную запись защёлки тем же значением в `ZenLatch::remember()` (удаление в `forget()`/`clearAll()` оставлено безусловным — на нём держатся тесты); `ConsentStorage::hasConsent()` стала чистым чтением, продление вынесено в `renewConsentIfGranted()` — единственная точка вызова, `frontend_head`; докблок `ZenLatch::reconcile()` больше не приписывает межхуковую однократность гарду поля экземпляра — она держится на побочном эффекте `waResponse::setCookie()` над `$_COOKIE`; `aria-label="Закрыть"` и пример `'Сокольники'` переведены в локаль; у `_w('day off')` появился комментарий про намеренно чужой домен локали. Живой прогон: второй `GET /order/` — 0 `Set-Cookie` от защёлок (было 3), `GET /order/` после `grant` — 1 `Set-Cookie: prefill_consent` (было 2)
- [x] 🟠 [`summary_template` пишется по праву `shop:settings`, а не `isAdmin('shop')`](../codereview/done/issue-96-summary-template-writable-by-settings-right.md) — исправлено 09.09.2026, в день находки: путь записи (`?module=plugins&action=save`, минуя закрытые isAdmin-экшены плагина) получил собственный `isAdmin('shop')`-гейт на опасных ветках (`summary_template`/`custom_templates`), не трогая остальные поля формы под правом `settings`; вход на запись впервые прогоняется через схему (`filterKnown()`), неизвестные ключи POST больше не оседают в БД. B4 переформулирован — называет оба реальных гейта, не один придуманный. Проверено тестом (13 проверок) и вживую POST-сценарием с контактом на одном `shop:settings`: шаблон не сохраняется, соседние поля той же формы — сохраняются. Состояние «до фикса» вживую не воспроизводилось (проверки на пути записи не было вовсе — видно из диффа); почему первая попытка сценария дала ложный результат — в файле issue
- [x] 🟠 [Уборщик ассетов удаляет пер-витринный `frontend_{code}.css`](../codereview/done/issue-94-pruner-deletes-storefront-css.md) — исправлено 09.09.2026, в день находки: `StaleFilePruner` получил обязательную glob-маску своих файлов (`variables_*.css` для `css/`, `*.js` для `js/`) и подставляет её прямо в `glob()` — чужих файлов в общем каталоге он больше не видит. Раньше пер-витринный файл переопределений жил рядом со сгенерированными и по TTL устаревал всегда: покупатель, пришедший в момент смены CSS-переменных, получал `<link>` на только что удалённый файл. Автотест — случай 7 в [StaleFilePrunerTest.php](../../tests/StaleFilePrunerTest.php)
- [x] 🟢 **Способ доставки `sd` («Пункт выдачи заказов», id 37) не отдавал тариф (08.09.2026)** — не воспроизвелось 09.09.2026 при живом прогоне фикса issue-91: в «Самовывозе» пункт виден в списке рядом с СДЭК, выбирается, `getRates()`/`calculate()` возвращает `rate=0`, адрес и фото приходят в `custom_data.pickup`. Причина расхождения 08.09 не установлена и не переисследовалась — способ сам восстановился (тем же прогоном проверено `$delivery_photos_html` на реальных фото ПВЗ, не подстановкой)
- [x] 🟠 [Признак «сработал checkout-хук» не доживает до `frontend_head`](../codereview/done/issue-93-checkout-detector-flag-lost-between-hooks.md) — исправлено 09.09.2026, в день находки: флаг переведён в статику детектора и переживает пересоздание объекта плагина между `checkout_before_auth` и `frontend_head`. До этого признак 1 существовал только в тестах, а в бою работал один маршрут `frontend/order`: тема с формой заказа вне этого маршрута получила бы свёрнутый чекаут без кнопки «Изменить» (нарушение Z3). Добавлены два теста: отметка одного экземпляра видна другому, и сброшенная отметка не протекает на каталог (щит от возврата issue-64)
- [x] 🟠 [Миниатюры фото ПВЗ не строятся — в свёрнутую карточку уезжает оригинал](../codereview/done/issue-91-thumb-helper-constructed-with-view.md) — исправлено 09.09.2026, в день находки: хелпер берётся штатным `wa()->getView()->getHelper()->shop` (ядро его кэширует) и резолвится один раз на галерею, а не конструируется с `waView` на каждое фото. Заодно закрыта не описанная в issue ветка — на несуществующем файле `imgUrl()` возвращает корень витрины, а не пустую строку, и старый guard пропустил бы `<img src="/">`. Откат на оригинал больше не беззвучен (`error`/`debug` в лог). Проверено на витрине: `<img src>` = `*.100x75.jpg`, natural 100×75 при отрисовке 77×58, файлы созданы рядом с оригиналами
- [x] 🟢 [Документация разошлась с кодом после перехода на собственную разметку фото/расписания ПВЗ](../codereview/done/issue-92-minor-findings-pass-3.md) — исправлено 09.09.2026, в день находки: описание `delivery_photos_html` переписано под фактическое поведение (ru/en + `/compile-plugin-mo`), комментарий у `delivery_schedule` ссылается на `renderPickupSchedule()`, критерии обоих полей в [zen-fields-render-test-plan.md](../tests/zen-fields-render-test-plan.md) переписаны под собственную разметку плагина. Сверх разбора нашлось четвёртое расхождение — пример значения «Галерея с лайтбоксом (HTML)» в том же справочнике переменных. Тексты проверены в редакторе шаблона в браузере
- [x] 🟢 [Shipping-билдер пишет кастомные поля доставки в чужую секцию details](../codereview/done/issue-60-cross-section-write-details-custom.md) — закрыта 24.08.2026: второй, независимый гейт `canPrefillSection('details')` в `prepareShippingSectionParams()` (не перенос в `prepareDetailsSectionParams()` — сломал бы `applyDeliveryAddress()`, где запись в `details.custom` осознанно идёт без чекера). Автотест [ShippingCustomDetailsOwnershipGateTest.php](../../tests/ShippingCustomDetailsOwnershipGateTest.php), 7 проверок. Саму ветку обхода живьём воспроизвести не удалось — `form.js` шлёт `html` всех шести секций одним POST, ownership `shipping`/`details` не рассинхронизируется в штатном сценарии; гейт защищает от кастомной темы или прямого запроса к `calculate`
- [x] 🟢 [Единица предзаполнения мельче связанной группы полей](../codereview/done/issue-65-prefill-overrides-current-input.md) — 22.08.2026, реализовано иначе, чем планировалось: владение сведено к `html`, снапшот снят. Закрыта и закоммичена 23.08.2026
- [x] 🟢 [Регресс после снятия снапшота: способ оплаты сбрасывается при каждой смене варианта доставки](../plans/done/payment-section-echo-cache.md) — эхо-кэш секции payment (правило P9), реализовано и закрыто 23.08.2026: все 8 этапов, 156 юнит-проверок, 5/5 браузерных сценариев подтверждены логом
- [x] 🟢 [Снапшот затирается текущим состоянием в ветке «нечего предзаполнять»](../codereview/done/issue-72-snapshot-overwritten-by-empty-branch.md) — закрыта 23.08.2026 как неприменимая: снапшот снят 22.08.2026, кода, который описывает issue, больше нет
- [x] 🟠 [Предзаполнение собирает несогласованный заказ из разных источников](../codereview/done/issue-84-prefill-inconsistent-across-groups.md) — закрыта 23.08.2026: п.1 закрыт планом [delivery-variant-identity.md](../plans/done/delivery-variant-identity.md) — `shipping_type_id` (собственный параметр плагина) удалён целиком, вариант доставки (`shipping_id`+`shipping_rate_id`) стал единственной идентичностью везде: в предзаполнении, гейте «Мои варианты», дедупликации, минимуме группы дзен-режима. 5 этапов, все проверены в браузере реальными заказами. Побочно открыл диалог «Мои варианты» истории, которую раньше отсекал гейт по нашему параметру (60 заказов из 85 на тестовой базе). П.2 не относится к тому же фиксу — вынесен в issue-86, закрыта следующей строкой
- [x] 🟠 [Секции группы delivery заполняются независимо от полноты источника](../codereview/done/issue-86-delivery-group-source-completeness.md) — вынесена 23.08.2026 из issue-84 п.2, закрыта в тот же день как неприменимая: раскол по полноте источника не даёт сломанный заказ — гасится `shopCheckoutRegionStep` (город обязателен, `can_continue: false` блокирует дальнейшие шаги) и `shopCheckoutShippingStep` (пересчитывает `variant_id` против живого региона на каждом расчёте, несовпадающий — молча сбрасывает). Смежный сценарий — сторонний плагин `cityselect` пишет `order.region` в сессию мимо `html`, prefill перезаписывает историей заказов — не баг, а осознанное решение продукта (см. «Синхронизация города с cityselect» в Backlog выше); гейт по сравнению «уже заполненных» данных воспроизвёл бы отменённый подход issue-59/65 (путать наличие данных с владением)
- [x] [Прод-бандл `prefill.frontend.min.js` отстал от исходников и несёт удалённый код](../codereview/done/issue-87-stale-prod-js-bundle.md) — найдена и закрыта в тот же день, 28.08.2026, как неприменимая: `/release-pack` безусловно пересобирает бандл на шаге 3 перед архивацией, независимо от `git diff`/`mtime`, так что отставание рабочего дерева до паковки не долетает до реального архива. Пункт про непроверенный в браузере релизный JS-путь (debug выключен) не зависел от staleness и остался отдельной строкой в «🧪 Тесты перед релизом» выше
- [x] 🟠 [`getShippingLogoUrl()` роняет PHP-notice на странице оформления заказа](../codereview/done/issue-88-shipping-logo-undefined-index.md) — исправлено 28.08.2026: `?:` → `??` при чтении `logo`/`img` из тарифа стороннего плагина доставки (те же ключи не гарантированы, в отличие от соседнего `getPaymentLogoUrl()`, который уже был на `??`). Поведение fallback не изменилось, проверено `php -r` на 4 кейсах. Регресс подтверждён в браузере гостевым чекаутом (штатная доставка/оплата рендерятся, новых строк в логе PHP нет); сам триггер (сторонний плагин без `logo`/`img`) на стенде не воспроизвести — такого плагина нет
- [x] 🟢 [Инлайновый `<script>` сигнала о недоступной доставке не защищён гардом jQuery](../codereview/done/issue-89-inline-script-no-jquery-guard.md) — исправлено 28.08.2026: обёрнуто в `if(typeof $!=="undefined")`, та же проверка, что и в точке входа фронтенда (issue-80 §1). `php -l` чист, no-op при отсутствии `$` подтверждён через `node -e`. Живой репро не делался — ветка требует одновременно куку `prefill_user_selected=1`, пустой `shipping_type`, не-`fast_render` и тему без jQuery, риск и так низкий
- [x] [В собственных эндпоинтах плагина уровень лога не инициализируется — debug/info теряются](../codereview/done/issue-81-log-level-never-set-in-plugin-endpoints.md) — 22.08.2026, `getLevel()` стал ленивым вместо зависимости от побочного эффекта `isActive()`; проверено в браузере на `force-prefill`
- [x] [Данные покупателя выводятся в шаблонах без экранирования](../codereview/done/issue-56-zen-summary-no-escaping.md)
- [x] [Мёртвый публичный эндпоинт fillCheckoutParams без проверки доступа](../codereview/done/issue-62-dead-unguarded-fill-checkout-endpoint.md)
- [x] [«Сохранить» отправляет настройки всех витрин, которые админ успел открыть](../codereview/done/issue-78-save-submits-all-viewed-storefronts.md)
- [x] [Одна константа на два вопроса: `html` как признак данных](../codereview/done/issue-59-html-key-marks-section-filled.md) — разделена на ownership/data 19.08.2026; автотест 132 проверки + браузерный блок пройдены
- [x] [Фатал при создании заказа вне фронтенда (storefront = null)](../codereview/done/issue-49-fatal-storefront-null-backend-order-create.md)
- [x] [TypeError в orderActionCreate при пустой checkout-сессии](../codereview/done/issue-50-type-error-null-checkout-params.md)
- [x] [Плагин молча выдаёт cookie авторизации auth_token на 90 дней](../codereview/done/issue-51-remember-me-auth-token-forced.md)
- [x] [Публичный эндпоинт consent: флуд лога](../codereview/done/issue-52-consent-endpoint-log-flood-csrf.md) — **только лог и ротация**; CSRF не сделан, вынесен в [issue-79](../codereview/done/issue-79-issue-52-csrf-half-done.md)
- [x] 🟠 [issue-79: CSRF на публичных эндпоинтах `consent` и `apply-delivery` так и не сделан](../codereview/done/issue-79-issue-52-csrf-half-done.md) — закрыто 11.09.2026: white-list-гейт `shopPrefillPluginCsrfGuard` на обоих контроллерах, проверено живым браузерным тестом и curl-сценариями, без регрессов
- [x] [Предзаполнение пишет пустые значения в сессию каждому посетителю](../codereview/done/issue-53-empty-prefill-writes-session.md)
- [x] [Часть бэкенд-эндпоинтов без проверки прав](../codereview/done/issue-54-backend-actions-no-rights-check.md)
- [x] [Гостю был доступен серверный путь выбора заказа из истории](../codereview/done/issue-55-guest-apply-delivery-ignores-order-id.md)
- [x] [setShippingVariantId сохраняет rate_id с точками целиком](../codereview/done/issue-66-shipping-variant-id-dot-split.md)
- [x] [CSS витрины: настройки от одной витрины, код файла от другой](../codereview/done/issue-58-storefront-css-settings-code-mismatch.md)
- [x] [Дубликат класса в lib/classes/fillparams: мина в автозагрузке](../codereview/done/issue-61-duplicate-class-file-autoload.md)
- [x] [Zen Mode скрывает секции CSS-ом, даже когда кнопка «Изменить» не выведена — чекаут без выхода](../codereview/done/issue-75-zen-collapse-without-toggle-button.md) — сценарий 5 (доставка выключена в настройках чекаута) подтверждён вживую 09.09.2026 на витрине `shop-1` (там доставка и оплата отключены штатно): `.wa-plugin-hook`/`<style id="prefill-zen-styles-delivery">` для delivery не генерируется вовсе, «сироты»-CSS без кнопки нет
- [x] [CSS и JS плагина подключаются на всех страницах витрины, а нужны только в корзине](../codereview/done/issue-64-assets-loaded-on-every-page.md) — детектор страницы чекаута, 20.08.2026; автотест + прогон витрины, попутно закрыт мёртвый Zen у гостя при выключенном гостевом предзаполнении
- [x] [Кнопка «Да, очистить» невидима в тёмной теме: белый текст на белом акценте](../codereview/done/issue-85-confirm-button-invisible-in-dark-theme.md) — цвет текста переключается вместе со схемой одной CSS-переменной, 20.08.2026; обе схемы проверены в браузере
- [x] [isSameDeliveryOption: null у кандидата совпадает с чем угодно, варианты схлопываются](../codereview/done/issue-67-same-delivery-option-null-match.md)
- [x] [Все заказы покупателя гидратируются, чтобы показать пять карточек (N+1, нет лимита)](../codereview/done/issue-68-params-choice-collection-n-plus-1.md) — лимит меряется в вариантах, а не в заказах: история добирается страницами до нужного числа разных вариантов, 15 запросов → 4; заодно закрыта [issue-17](../codereview/done/issue-17-n-plus-1-comment-contact-per-order.md)
- [x] [Поиск источника предзаполнения выполняется на каждом запросе](../codereview/done/issue-63-guest-hash-lookup-full-scan.md) — реализовано и **проверено в браузере** 18.08.2026, полный runbook — [tests/issue-63-browser-test-runbook.md](../tests/issue-63-browser-test-runbook.md) (хвосты 4.5/4.7 закрыты — выше в этом разделе)
- [x] [Русский текст зашит в JS и в дефолтных настройках — виден покупателю на en-витрине](../codereview/done/issue-69-hardcoded-russian-strings-frontend.md) — реализовано и **проверено в браузере** 18.08.2026; при проверке найден отдельный баг [issue-82](../codereview/done/issue-82-frontend-js-no-cache-busting.md)
- [x] [prefill.frontend(.min).js подключается без версии — браузер может годами отдавать старый JS из кэша](../codereview/done/issue-82-frontend-js-no-cache-busting.md) — убран трейлинг-`?`, ядро снова клеит версию; **проверено в браузере** 19.08.2026 в debug и prod
- [x] [Кастомный CSS витрины замещает frontend.css целиком — обновления стилей плагина не доедут](../codereview/done/issue-76-custom-css-replaces-plugin-stylesheet.md) — override-модель (frontend.css всегда + переопределения поверх), read-only справка с оригиналом в редакторе, 20.08.2026; **проверено в браузере**; попутно найден и исправлен смежный баг — `getPublicUrl()` отдавал URL с двойным ведущим `/`, из-за чего per-storefront CSS никогда не подключался на витрине
- [x] [Плагин навязывает display:inline !important элементам шапки авторизации всегда](../codereview/done/issue-77-auth-header-display-important-always.md) — правило перенесено из статического frontend.css в генерируемый variables_*.css, эмитится только при включённой hide_auth_header, `!important` сохранён, 20.08.2026; **проверено в браузере** on/off
- [x] [Абсолютный путь /wa-apps/shop/img/... ломает иконки на установках в подкаталоге](../codereview/done/issue-70-hardcoded-absolute-sprite-path.md) — путь собирается через `{$wa_url}`, заодно исправлена опечатка `?v` → `?v=`; 20.08.2026, **проверено в браузере**
- [x] [Сборник мелких находок ревью (null-витрина, secure-cookies, мусор в wa-data, ротация логов, custom_css в TEXT)](../codereview/done/issue-57-minor-robustness-findings.md) — 7 из 8 закрыты 21.08.2026; уникальный индекс на shop_prefill_settings отклонён (YAGNI + лимит ключа MyISAM/utf8mb3), обоснование в файле
- [x] 🟢 [Сборник мелких находок третьего прохода (jQuery без guard, ложный диалог, мусор в настройках, локали, ace)](../codereview/done/issue-80-frontend-minor-findings.md)


- [x] 🟢 [Мёртвый код уезжает в релизный архив, часть — «настройки, которые ничего не делают»](../codereview/done/issue-71-dead-code-in-release-archive.md) — 9 неиспользуемых классов/методов + незаполняемые контактные поля `FillParams`; блокирующего эффекта нет
- [x] 🟢 [getInstance() отдаёт объект от первого хука; per-instance кэши значат не то, что в комментариях](../codereview/done/issue-73-stale-plugin-singleton.md)
- [x] 🟢 [Сборник мелких находок второго прохода (загрязнение waView, типы в post, notice, логи, SameSite)](../codereview/done/issue-74-minor-findings-pass-2.md)

- [x] 🟠 [Свёрнутая карточка «Оплата» пропадает целиком при короткозамкнутом рендере](../bugs/done/payment-zen-card-vanishes-on-shortcircuit.md) — найдено и подтверждено в браузере 24.08.2026 при проверке фикса соседнего бага. Данные не теряются (после починки email карточка возвращается), но пока держится ошибка `auth`, весь блок «Оплата» пропадает со страницы — в отличие от «Доставки», её карточка эту ситуацию переживает
- [x] 🟠 [Дублирующееся поле `[active]` в шапке страницы настроек и во вкладке «Основные настройки»](bugs/duplicate-active-field-header-vs-tab.md) — найдено и подтверждено в браузере 24.08.2026 при проверке батчинга сохранения (issue-74#5). Безымянный тумблер в Header.html и подписанный тумблер вкладки «Основные настройки» шлют одно и то же поле формы; побеждает не тот, что переключил пользователь — тихая потеря выбора администратора без ошибки в UI
- [x] ~~[Мастер-тумблер «active» в шапке настроек: визуал расходится с реальным состоянием]~~ [закрыто — не баг, задуманное поведение](../bugs/done/settings-active-toggle-visual-state-desync.md) — 06.09.2026: тумблер шапки не витринный, а глобальный killswitch плагина (`storefront_code = '-'`, независим от селектора витрины); тумблер вкладки «Основные настройки» — отдельная настройка конкретной витрины. Два разных флага под именем `active` и одинаковой иконкой `switch`, а не рассинхронизация одного контрола — тот же корень, что и в duplicate-active-field-header-vs-tab.md выше
- [x] Довести до конца [browser runbook issue-63](../tests/issue-63-browser-test-runbook.md) — сценарий 4.5 закрыт 28.08.2026 как C-07 (не воспроизвёлся), сценарий 4.7 (`cityselect`) закрыт 05.09.2026 как C-08: смена города виджетом корректно дропает устаревшее эхо доставки (`Delivery echo dropped: region changed`), без конфликта с приоритетом ввода покупателя — см. [TESTS.md](../tests/TESTS.md#фо-03-приоритет-ввода-покупателя-c-07-c-08--статус--частично-05092026)

### Хвосты issue-51 (remember me)

- [x] Путь A: вход через штатную форму логина, с галочкой «Запомнить меня» и без неё — 21.08.2026, оба сценария прогнаны в браузере на тестовом контакте с известным паролем и подтверждены логом плагина: с галочкой `[DEBUG] Auth token extended` (expires_days: 90), без галочки — строка не появляется вовсе
- [x] Ограничения remember-me записаны в CHANGELOG (раздел Unreleased → Known limitations): кастомный `expires` — best-effort, ядро сбрасывает токен на 30 дней в `waAuth::updateAuth()`; фича зависит от тумблера `rememberme` домена
- [x] Тестовый мусор issue-51 убран с локалки: заказы 86 и 87 (отображались как #10086/#10087 из-за сдвига нумерации магазина), контакты 36 и 37
- [x] Мусор прогона issue-63 (18.08.2026) убран: заказы 88, 89, 90; контакты 40, 41, 42 («Гость Прогон63»)
- [x] 8 мёртвых строк `shop_order_params.name = 'prefill_guest_hash'` (заказы 32-47) удалены

### Фичи

- [x] [Zen Mode](../concept/ZEN-MODE.md)
- [x] [Кастомные шаблоны сводки оплаты](../todo/done/zen-mode-custom-payment-templates.md)
- [x] [Кастомные шаблоны сводки доставки (UI настроек)](../todo/done/zen-mode-custom-delivery-templates-ui.md)
- [x] [Посекционные флаги предзаполнения](../todo/done/per-section-prefill-flags.md)
- [x] [Очистка сохранённых данных предзаполнения](../todo/done/clear-prefill-data.md)
- [x] Выбор отображения иконок в Zen Mode: логотипы способов доставки/оплаты, дефолтные иконки или «Без иконок»
- [x] Добавить вывод всех полей по типу обработки перс. данных
- [x] Добавить возможность отключения предзаполнения для витрины (настройка `active`)
- [x] Настройка витрины «Сколько вариантов показывать» в диалоге «Мои варианты» — 1–10, по умолчанию 5
- [x] Если адресов нет — выводить заглушку
- [x] Секция details: предзаполнение кастомных полей доставки, не только street
- [x] Опция для администратора — доп. классы для кнопок «Мои варианты», «Свернуть», «Развернуть»
- [x] Дебаг-вкладка для чтения логов
- [x] Debug-режим плагина не зависит от глобального дебага Webasyst
- [x] Почта поддержки в шапке настроек

### Проверки и правки UI

- [x] Проверено 09.09.2026: вставка `<img>` в кастомный шаблон сводки свёрнутого блока работает на всех трёх уровнях — Ace-редактор (режим `smarty`) не искажает тег при вводе, `SettingsTemplatePreviewController` возвращает его как есть (`$view->fetch('string:'...)`, без экранирования — превью не Zen-поле, а собственная разметка администратора), на живом чекауте `<img>` рендерится и масштабируется атрибутом `width` как обычный HTML. Прогнано на шаблоне группы `delivery`, картинка — существующий файл товара
- [x] 🟠 **Совместимость с `bnpblocks`** («Свои блоки доставки», плагин студии bodysite.ru) — закрыто 09.09.2026 разбором кода, без живого прогона: лицензия на стенде оказалась уже истёкшей (карточка в маркетплейсе плагинов: «Plugin's license is expired»), из-за чего плагин пропадает из бэкенд-роутинга SPA и настроить блок штатно нельзя. Прочитан недостающий кусок — Smarty-шаблон хука `checkout_render_details` — оказался голым `<script>` (jQuery `.hide()` по `.wa-delivery-price`/тексту «Срок доставки»), не видимой разметкой; плюс подтверждено чтением `waEvent::runPlugins()`, что даже пустой возврат хука оборачивается в пустой `.wa-plugin-hook`. Вывод: визуальной и функциональной коллизии с Zen-сворачиванием нет ни в одном из трёх хуков плагина — детали в [bnpblocks-compat-test-plan.md](../tests/bnpblocks-compat-test-plan.md#проверено-09092026). Лицензию не продлевали и `checkLicense()` не патчили (см. преамбулу файла плана)
- [x] Предзаполнение галочки «terms» в секции confirm и проверка предзаполнения секции по этой галочке
- [x] В диалоге «Мои варианты доставки» показываются только активные инстансы доставок (отключённые администратором — скрываются)
- [x] Проверено: в диалоге «Мои варианты доставки» показываются последние N различных вариантов (N — настройка витрины, по умолчанию 5)
- [x] Подпись к выбору витрины в настройках: зелёный маркер и общие настройки (`hint.storefront_selector_status`)
- [x] Переименование «Статус для витрины» в «Индивидуальные настройки для этой витрины» / «Общие настройки для всех витрин», подсказка подробнее и выделена цветом
- [x] Эмодзи активной витрины в админке — сделали, но остался баг: не работает для общих правил (см. Backlog при необходимости)
- [x] Проверка доступности переменных на каждом шаге чекаута
- [x] Изменены примеры переменных для шаблонов
- [x] Локализация вычищена от лишних переводов, пройдена повторно
- [x] Локализация проверена на предмет кривых ИИ-строк
- [x] Тёмная тема: иконки корзины больше не чёрные
- [x] Починено сворачивание блока авторизации без имени и фамилии
- [x] Починено сообщение «первая версия плагина» (прыгающая кнопка закрытия) и диалог поддержки в тёмной теме
- [x] Диалог выбора адреса разворачивается во всю высоту
- [x] Диалог выбора адреса — тёмная тема проверена
- [x] Диалог предупреждения о проблемах заполнения — тёмная тема доведена до конца (цвет шрифтов, кнопка закрыть)
- [x] Вкладка «Дебаг»: события поддерживают тёмную тему
- [x] Проведены тесты с плагином, создающим доп. блоки доставки помимо курьера/самовывоза/почты
- [x] Проведено ревью на поиск крупных проблем
- [x] Рефакторинг `prefill.settings.js` — HTML собирался в JS ($el.html()/конкатенация строк), вынесено в Smarty-шаблоны: [сделано 11.09.2026](../plans/done/issue-98-minor-findings-fix-plan.md#7-prefillsettingsjs--тот-же-паттерн-актуален) — шесть `<template>` в `templates/actions/settings/blocks/JsTemplates.html` (лог-запись, load-more x2, превью-состояния x2, тултип переменной), `prefillEscapeHtml` удалён полностью, вёрстка сверена до/после через `git stash`

### Найдено при уборке 11.09.2026 — закрытые задачи без строки в TODO.md

Правило внизу TODO.md требует строку на каждый файл `codereview/`, `bugs/`, `todo/`, `plans/`; эти три её не имели. Разобраны и закрыты здесь же, задним числом.

- [x] 🟠 [Синхронизация города → cityselect (city-sync-cityselect.md)](../todo/done/city-sync-cityselect.md) — реализовано 26.08.2026 в рамках Этапа 1 интеграции с плагинами выбора города (см. выше), проверено в браузере. Специфика cityselect как адаптера, общие правила — в [city-sync-model.md](../todo/city-sync-model.md)
- [x] Общая деактивация предзаполнения (`global-prefill-disable.md`) — дубль-черновик уже отгруженной настройки «Добавить возможность отключения предзаполнения для витрины (`active`)» из «Фичи» выше; отдельного смысла не несёт, оставлен как исторический черновик
- [x] [Актуализация дебаг-панели Prefill](../plans/done/debug-panel-modernization.md) — реализовано 05.09.2026: AJAX-трейс переведён на `<template class="js-prefill-debug-carrier">` в render-хуках вместо перехвата `fetch`/XHR; статические проверки и гостевой сценарий пройдены в Chrome
