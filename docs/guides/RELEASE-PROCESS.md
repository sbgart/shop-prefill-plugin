# Процесс создания релиза плагина Prefill

Этот документ описывает пошаговый процесс подготовки и публикации новой версии плагина в магазине Webasyst.

> **⭐ РЕКОМЕНДУЕТСЯ:** Используйте встроенную команду `php wa.php compress` для создания архива!  
> Она автоматически проверяет код, исключает ненужные файлы и создаёт правильную структуру.

> **⚠️ ВАЖНО:** Магазин Webasyst принимает **только** архивы `.tar.gz`.  
> Архив `.tar` будет отклонён с ошибкой про недопустимое расширение.

---

## 📋 Подготовка к релизу

### 1. Обновление версии

**Файл:** `lib/config/plugin.php`

```php
return [
    'version' => '1.X.X', // <- Обновить версию
    // ...
];
```

### 2. Обновление документации

#### CHANGELOG.md

Добавить описание изменений новой версии:

```markdown
## [1.X.X] - YYYY-MM-DD

### Добавлено

- Новая функциональность

### Улучшено

- Оптимизации

### Исправлено

- Баги
```

#### README.md

Обновить версию внизу файла:

```markdown
**Версия:** 1.X.X (см. `lib/config/plugin.php`) | **Webasyst Shop-Script plugin**
```

### 3. Перекомпиляция локализации

```bash
cd /Users/user/Project/wa-dev
XDEBUG_MODE=off php wa.php locale shop/plugins/prefill
```

Проверить вывод:

```
Result: SUCCESS. Words for en_US locale:
Total: XXX
Updated: XXX

Result: SUCCESS. Words for ru_RU locale:
Total: XXX
Updated: XXX
```

---

## 📦 Создание архива для публикации

### ⭐ Метод 1: Встроенная команда Webasyst (рекомендуется)

```bash
cd /Users/user/Project/wa-dev

# Создать архив с автоматическими проверками
php wa.php compress shop/plugins/prefill -style false
```

**Что делает команда автоматически:**

1. ✅ **Проверяет синтаксис PHP** - все `.php` файлы
2. ✅ **Проверяет конфигурацию** - `lib/config/plugin.php`, routing, requirements
3. ✅ **Проверяет базу данных** - именование таблиц, структура
4. ✅ **Исключает файлы** - из `.gitignore` и `lib/config/exclude.php`
5. ✅ **Создаёт контрольные суммы** - `.files.md5` для верификации
6. ✅ **Правильный формат** - без проблем PaxHeader, длинных путей

**Архив создаётся:** `wa-apps/shop/plugins/prefill/prefill.tar.gz`

**Вывод команды:**

```
Check & compress shop/plugins/prefill with params:
	style	false
	skip	none
PHP version 7.4.33
--------------------------------------------------------------------------------
IGNORE XXX FILE(S)
--------------------------------------------------------------------------------
...список исключённых файлов...
--------------------------------------------------------------------------------
Start test item config.
	OK
Start checking system requirements
	PASSED
Start checking PHP syntax
PHP 7.4.33 file syntax check	PASSED
Start test item database section.
	Passed
Config check OK
Code style check disabled

time	XX ms
size	XX.XX KByte

NOTICE: Archive created without XXX file(s)
```

### Проверка архива

```bash
cd /Users/user/Project/wa-dev/wa-apps/shop/plugins/prefill

# Размер и количество файлов
ls -lh prefill.tar.gz
tar -tzf prefill.tar.gz | wc -l

# Проверить Dialog файлы (должно быть 5)
tar -tzf prefill.tar.gz | grep -i dialog

# Проверить formatters файлы (должно быть 3)
tar -tzf prefill.tar.gz | grep formatters

# Проверить отсутствие документации
tar -tzf prefill.tar.gz | grep -E '(\.md$|^prefill/docs/)'
# Должно быть пусто!

# Посмотреть первые 20 файлов
tar -tzf prefill.tar.gz | head -20
```

### Ожидаемый результат

```
Размер: ~92 KB
Файлов: ~117
Dialog файлов: 5
Formatter файлов: 3
MD файлов: 0 (ОБЯЗАТЕЛЬНО!)
docs/: 0 (ОБЯЗАТЕЛЬНО!)
.files.md5: есть ✓
```

### Что исключается из архива автоматически

- ✅ **`.gitignore` правила** - всё из вашего `.gitignore`
- ✅ **`lib/config/exclude.php`** - дополнительные исключения (docs, \*.md)
- ✅ **Git файлы** - `.git/`, `.gitignore`
- ✅ **Системные файлы** - `.DS_Store`, `Thumbs.db`
- ✅ **Temporary файлы** - `.bak`, `.temp`, `*.swp`

---

## 🚀 Публикация в магазине Webasyst

### 1. Подготовка материалов

**Файл для загрузки:**

```
/Users/user/Project/wa-dev/wa-apps/shop/plugins/prefill-X.X.X.tar.gz
```

**Описание для магазина:**

```
Плагин для установки гибких правил минимального заказа в Shop-Script.
Поддержка витрин, категорий, товаров с интеллектуальной валидацией и современным UI.

Ключевые возможности:
• Многоуровневая система правил (глобальные, витринные, категориальные, продуктовые)
• Настройка минимумов по сумме и количеству товаров
• Поддержка мультивалютности с автоматическим пересчётом
• Интеллектуальная валидация на всех этапах (корзина, оформление, заказ)
• Импорт/экспорт настроек
• Современный UI на базе Webasyst UI 2.0
• Кэширование и оптимизация производительности
```

**Системные требования:**

```
PHP: >= 7.4
Webasyst Framework: >= 2.x
Shop-Script: >= 9.0
MySQL: >= 5.7
```

### 2. Загрузка

1. Перейти на https://www.webasyst.ru/store/
2. Войти в аккаунт разработчика (vendor: 1059969)
3. Загрузить архив `prefill-X.X.X.tar.gz` (строго `.tar.gz`, не `.tar` и не `.zip`)
4. Заполнить описание и указать системные требования
5. Добавить скриншоты (рекомендуется 4-6 штук)
6. Опубликовать

### GitHub Releases (если используете)

- Прикрепляйте к релизу **именно** `prefill-X.X.X.tar.gz` как отдельный asset.
- Не используйте архивы “Source code (tar.gz/zip)” от GitHub для магазина — они содержат репозиторий, а не корректную структуру/исключения для Webasyst.

---

### 3. Рекомендуемые скриншоты

- Главная страница настроек плагина
- Настройки витрин с правилами
- Настройки категорий/товаров
- Пример валидации на фронтенде (корзина/оформление)
- Диалог импорта/экспорта
- Новые фичи текущей версии

---

## ✅ Чек-лист перед публикацией

### Обязательные пункты

- [ ] Версия обновлена в `lib/config/plugin.php`
- [ ] CHANGELOG.md актуализирован
- [ ] README.md актуализирован (версия внизу)
- [ ] Локализация перекомпилирована (`.po` → `.mo`)
- [ ] Архив создан через `php wa.php compress`
- [ ] Проверки пройдены (PHP syntax, config, db, requirements)
- [ ] Архив проверен (есть Dialog файлы, нет `.md` и `docs/`)
- [ ] Размер архива адекватный (~92 KB, ~117 файлов)

### Проверка после установки

После публикации в магазине протестировать на чистой системе:

- [ ] Плагин устанавливается без ошибок
- [ ] Настройки открываются корректно
- [ ] Основной функционал работает
- [ ] Валидация работает на фронтенде
- [ ] Импорт/экспорт функционирует
- [ ] Все тексты переведены

---

## 📝 Формат CHANGELOG

Используем стандарт [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/):

```markdown
## [X.X.X] - YYYY-MM-DD

### Добавлено (Added)

- Новая функциональность для пользователей

### Изменено (Changed)

- Изменения в существующей функциональности

### Устарело (Deprecated)

- Функции, которые скоро будут удалены

### Удалено (Removed)

- Удалённая функциональность

### Исправлено (Fixed)

- Исправленные баги

### Безопасность (Security)

- Исправления уязвимостей
```

---

## 🔄 Семантическое версионирование

Плагин использует [SemVer](https://semver.org/lang/ru/):

**Формат:** MAJOR.MINOR.PATCH

- **MAJOR** (X.0.0) - несовместимые изменения API
- **MINOR** (0.X.0) - новая функциональность с обратной совместимостью
- **PATCH** (0.0.X) - исправления багов с обратной совместимостью

**Примеры:**

- `1.0.0` → `1.0.1` - исправление бага
- `1.0.1` → `1.1.0` - новая фича
- `1.1.0` → `2.0.0` - breaking changes

---

## 🛠 Полезные команды

### Очистка кэша плагина

```bash
rm -rf /Users/user/Project/wa-dev/wa-cache/*/apps/shop_prefill/
```

### Просмотр содержимого архива

```bash
tar -tzf prefill-X.X.X.tar.gz | less
```

### Распаковка архива (для проверки)

```bash
tar -xzf prefill-X.X.X.tar.gz -C /tmp/
```

### Проверка размера по типам файлов

```bash
tar -tzf prefill-X.X.X.tar.gz | grep -E '\.(php|html|js|css)$' | wc -l
```

---

## 📂 Структура файлов в релизе

В архиве должны быть:

```
prefill/
├── css/              # Стили
├── img/              # Иконки и изображения
├── js/               # JavaScript
├── lib/              # PHP код
│   ├── actions/      # Контроллеры и экшены
│   ├── classes/      # Классы плагина
│   ├── config/       # Конфигурация
│   ├── models/       # Модели данных
│   └── updates/      # Миграции БД
├── locale/           # Переводы (.po и .mo)
└── templates/        # Smarty шаблоны
```

**НЕ должно быть (СТРОГО!):**

- **`*.md` файлов** - ВООБЩЕ НИКАКИХ (README.md, CHANGELOG.md, RELEASE-_.md, docs/\*\*/_.md)
- **`docs/` папки** - целиком исключена
- `.DS_Store`, `.git*` файлов
- `node_modules/`
- `*.log` файлов
- `PaxHeader/` (важно для macOS - используйте --no-xattrs и --no-mac-metadata)

---

## ⚠️ Важные замечания

1. **НИКАКИХ `.md` ФАЙЛОВ в архиве!** - Это критично! Все MD файлы только для репозитория
2. **Документация для пользователей** должна быть на странице плагина в магазине, а не в архиве
3. **CHANGELOG.md** и **README.md** остаются в репозитории, но **НЕ включаются** в релиз
4. **Служебные файлы** (`RELEASE-*.md`) - только для разработки, не для релиза
5. **Папка docs/** - только для внутреннего использования, не для релиза
6. **Локализация** должна быть перекомпилирована перед каждым релизом
7. **Архив** должен содержать **ТОЛЬКО** исполняемые файлы плагина (PHP, JS, CSS, шаблоны, локализация)

---

## 📊 История релизов

| Версия | Дата       | Размер | Файлов | Заметки                                                         |
| ------ | ---------- | ------ | ------ | --------------------------------------------------------------- |
| 1.4.1  | 2026-01-08 | 92 KB  | 117    | Исправление архивации (wa.php compress), обновлена документация |
| 1.4.0  | 2026-01-05 | 82 KB  | 155    | Система сворачиваемых блоков, оптимизации, requirements fix     |
| 1.3.2  | -          | -      | -      | Предыдущая версия                                               |

---

## 🔗 Полезные ссылки

- **Магазин Webasyst:** https://www.webasyst.ru/store/
- **Документация Webasyst:** https://www.webasyst.ru/developers/
- **Keep a Changelog:** https://keepachangelog.com/ru/1.0.0/
- **Semantic Versioning:** https://semver.org/lang/ru/

---

**Последнее обновление:** 8 января 2026  
**Версия документа:** 2.0
