# Шпаргалка: Создание релиза плагина

**Быстрый гайд для подготовки релиза плагина Minorder**

> **⭐ РЕКОМЕНДУЕТСЯ:** Используйте встроенную команду `php wa.php compress` для создания архива!  
> Она автоматически исключает ненужные файлы, проверяет код и создаёт правильную структуру.

> **⚠️ ВАЖНО:** Магазин Webasyst принимает **только** `.tar.gz`.  
> `.tar` будет отклонён как недопустимое расширение.

---

## 🚀 Быстрый процесс (копируй и выполняй)

### 1️⃣ Обновить версию

```bash
# Отредактировать lib/config/plugin.php
# 'version' => '1.X.X'
```

### 2️⃣ Обновить документацию

- **CHANGELOG.md** - добавить новую версию
- **README.md** - обновить версию внизу

### 3️⃣ Перекомпилировать локализацию

```bash
cd /Users/user/Project/wa-dev
XDEBUG_MODE=off php wa.php locale shop/plugins/prefill
```

### 4️⃣ Создать архив (с автоматическими проверками)

**⭐ РЕКОМЕНДУЕТСЯ: Встроенная команда Webasyst**

```bash
cd /Users/user/Project/wa-dev

# Создать архив с автоматическими проверками
php wa.php compress shop/plugins/prefill -style false
```

**Что делает команда автоматически:**

- ✅ Проверяет синтаксис PHP
- ✅ Проверяет конфигурацию плагина
- ✅ Проверяет структуру базы данных
- ✅ Проверяет требования (requirements)
- ✅ Исключает файлы из `.gitignore` и `lib/config/exclude.php`
- ✅ Создаёт контрольные суммы (`.files.md5`)
- ✅ Создаёт правильный архив без проблем с PaxHeader

**Архив создаётся:** `wa-apps/shop/plugins/prefill/prefill.tar.gz`

### 5️⃣ Проверить архив

```bash
cd /Users/user/Project/wa-dev/wa-apps/shop/plugins/prefill

# Размер и количество файлов
ls -lh prefill.tar.gz
tar -tzf prefill.tar.gz | wc -l
# Должно быть: ~92 KB, ~117 файлов

# Проверить критичные файлы
tar -tzf prefill.tar.gz | grep -i dialog
# Должно быть 5 Dialog файлов

tar -tzf prefill.tar.gz | grep formatters
# Должно быть 3 Formatter файла

# Проверить отсутствие документации
tar -tzf prefill.tar.gz | grep -E '(\.md$|^prefill/docs/)'
# Должно быть пусто!
```

### 6️⃣ Загрузить в магазин

- URL: https://www.webasyst.ru/store/
- Файл: `prefill-X.X.X.tar.gz` (строго `.tar.gz`, не `.tar` и не `.zip`)

---

## ✅ Чек-лист

```
[ ] lib/config/plugin.php - версия обновлена
[ ] CHANGELOG.md - актуализирован
[ ] README.md - версия обновлена
[ ] Локализация перекомпилирована
[ ] Архив создан через php wa.php compress
[ ] Проверки пройдены (PHP syntax, config, db, requirements)
[ ] Архив проверен: есть Dialog файлы, нет *.md и docs/
[ ] Архив ~92 KB, ~117 файлов
[ ] В архиве есть prefill/lib/shopPrefill.plugin.php
[ ] Загружен в магазин
```

---

## 📦 Что должно быть в архиве

✅ **Включено:**

- `css/`, `js/`, `img/`
- `lib/` (PHP код, конфиги, модели)
- `locale/` (`.po` и `.mo` файлы)
- `templates/` (Smarty шаблоны)

❌ **Исключено (СТРОГО!):**

- **ВСЕ `*.md` файлы** (README.md, CHANGELOG.md, RELEASE-_.md, docs/\*\*/_.md)
- Папка `docs/` (целиком!)
- Служебные файлы (`.DS_Store`, `.git*`)
- `node_modules/`
- `*.log` файлы

---

## 🔢 Версионирование (SemVer)

**Формат:** `MAJOR.MINOR.PATCH`

- **1.0.0 → 1.0.1** - исправление бага
- **1.0.1 → 1.1.0** - новая фича
- **1.1.0 → 2.0.0** - breaking changes

---

## 📝 Шаблон для CHANGELOG

```markdown
## [X.X.X] - YYYY-MM-DD

### Добавлено

- Новая функциональность

### Улучшено

- Оптимизации

### Исправлено

- Баги
```

---

## 🆘 Частые проблемы

**Ошибка компиляции локализации:**

```bash
# Отключить xdebug
XDEBUG_MODE=off php wa.php locale shop/plugins/prefill
```

**Архив слишком большой:**

```bash
# Проверить, что исключены MD и docs
cd /Users/user/Project/wa-dev/wa-apps/shop/plugins/prefill
tar -tzf prefill.tar.gz | grep -E '(\.md$|docs/)'
```

**Отсутствуют Dialog файлы после установки:**

**Причина:** Правило `*.log` в `.gitignore` ошибочно исключило файлы с `Dialog` в названии (так как они содержат подстроку "log").

**Решение:** Используйте `lib/config/exclude.php` вместо `.gitignore` для исключения файлов из архива:

```php
// lib/config/exclude.php
<?php
return [
    'docs',      // Документация
    '*.md',      // Markdown файлы
];
```

**Проверки не проходят:**

```bash
# Посмотреть полный вывод
cd /Users/user/Project/wa-dev
php wa.php compress shop/plugins/prefill -style false

# Найти конкретную ошибку в выводе
```

**Нужно пересоздать архив:**

```bash
cd /Users/user/Project/wa-dev/wa-apps/shop/plugins/prefill
rm -f prefill.tar.gz

cd /Users/user/Project/wa-dev
php wa.php compress shop/plugins/prefill -style false
```

---

## 📖 Полная документация

См. [RELEASE-PROCESS.md](RELEASE-PROCESS.md) для детального процесса.

---

---

---

**Версия:** 2.0 | **Дата:** 2026-01-08
