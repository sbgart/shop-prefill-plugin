# Runbook: браузерное тестирование issue-63

**Для кого:** исполняет Claude при выданном доступе к браузеру.
**Что проверяем:** [issue-63](../codereview/issue-63-guest-hash-lookup-full-scan.md) — источник предзаполнения читается не чаще одного раза на личность за сессию, полный перебор `shop_order_params` исчез, каталог к заказам не обращается.
**Когда:** Фаза 0 исполняется **до** реализации (снимает baseline). Фазы 1-4 — после.

## Среда и доступы

| Что | Значение |
|---|---|
| Бэкенд | `https://wa-dev.loc/webasyst/` |
| Логин / пароль | `admin` / подставляет браузер — **пароль не вводить, просто нажать кнопку входа** |
| Витрина (основная) | `https://wa-dev.loc/` — код витрины `wa-dev.loc/*`, base64 `d2EtZGV2LmxvYy8q` |
| Корзина + чекаут | `https://wa-dev.loc/order/` — **единственный рабочий**; `/cart/` и `/checkout/` падают (в теме нет шаблонов) |
| Успех заказа | `https://wa-dev.loc/checkout/success/` |
| Настройки плагина | `https://wa-dev.loc/webasyst/shop/?action=plugins#/prefill` |
| Выход админа | `https://wa-dev.loc/webasyst/?action=logout` |
| Товар для корзины | `https://wa-dev.loc/lyustra-tulpan-03/` (id 847) или `/zvonok-besprovodnoy-violin/` (838) |
| СУБД | MySQL **8.4.7**, клиент `/Applications/ServBay/bin/mysql`, БД `wa-dev`, root / `ServBay.dev` |

Хелпер для всех SQL ниже:

```bash
MY() { /Applications/ServBay/bin/mysql -h127.0.0.1 -P3306 -uroot -pServBay.dev wa-dev "$@" 2>&1 | grep -v "Using a password"; }
```

## Грабли, которые уже стоили времени

- Товар в корзину кладётся **только со страницы товара**; кнопка «В корзину» в списке на главной не срабатывает.
- Город **автозаполняется** после выбора региона — не вводить следом руками, получится «МоскваМосква».
- «Индекс» и «Подъезд» обязательны даже для самовывоза. Самовывоз открывает модалку выбора ПВЗ.
- Заказ оформляется полной перезагрузкой на `/checkout/success/`.
- Бэкенд и витрина — **одна сессия**: залогинен в бэкенде → на витрине не гость.
- Выход **покупателя** (`is_user = 0`) по URL не работает. Приём: `UPDATE wa_contact SET password = CONCAT(password,'x') WHERE id = <ID>;` → на следующем запросе токен рассинхронизируется и `clearAuth()` сделает гостя.
- Куки плагина **HttpOnly**, из JS не видны, `Set-Cookie` браузерные инструменты не отдают → факт выдачи куки проверять **по логу плагина на сервере**.
- В SPA настроек селектор витрины — web-компонент: менять через `form_input` по внутреннему `<select>`, значение = base64 кода витрины. После переключения старый DOM остаётся — брать `querySelectorAll(...).pop()`, не `querySelector`.
- Радиокнопки `order_without_auth` визуально скрыты (0×0) — кликать по `<label>`.

## Инструмент 1: точный подсчёт SQL за запрос

Проверено — работает на этой установке.

```bash
# включить
MY -e "SET GLOBAL log_output='TABLE'; SET GLOBAL general_log='ON'; TRUNCATE TABLE mysql.general_log;"

# ...сделать ровно одно действие в браузере (одна загрузка страницы)...

# что выполнялось по интересующим таблицам, с разбивкой по соединениям
MY -e "
SELECT thread_id, event_time, LEFT(CONVERT(argument USING utf8mb4), 160) AS q
FROM mysql.general_log
WHERE command_type='Query'
  AND argument REGEXP 'shop_order_params|shop_order|shop_customer|wa_contact'
ORDER BY event_time;"

# сводка: сколько запросов к заказам за это действие
MY -e "
SELECT COUNT(*) AS order_queries
FROM mysql.general_log
WHERE command_type='Query'
  AND argument REGEXP 'shop_order_params|shop_order |shop_customer';"

# выключить и прибрать (ОБЯЗАТЕЛЬНО в конце сессии)
MY -e "SET GLOBAL general_log='OFF'; SET GLOBAL log_output='FILE'; TRUNCATE TABLE mysql.general_log;"
```

Исходные значения на этой машине: `general_log = OFF`, `log_output = FILE`. Вернуть именно их.

Одна загрузка страницы = одно PHP-соединение = обычно один `thread_id`; при сомнении группировать по нему. Лог ловит **все** приложения, поэтому фильтр по именам таблиц обязателен.

## Инструмент 2: лог плагина

```bash
LOG=/Users/artem/Project/wa-dev/wa-log/prefill.plugin.log
POS=$(wc -l < $LOG)          # зафиксировать ДО сценария
# ...действие...
tail -n +$((POS+1)) $LOG      # только новое
```

Маркер `user:N` в строке = контакт N, отсутствие = гость. Факт выдачи/продления куки виден только здесь.

## Инструмент 3: EXPLAIN

```bash
MY -e "EXPLAIN SELECT order_id FROM shop_order_params WHERE name='prefill_guest_<48hex>' ORDER BY order_id DESC LIMIT 1\G"
```

Ожидание после фикса: `type: ref`, `key: name`, `filtered: 100.00`. Признак провала: `Using where` вместе с низким `filtered` — значит сравнение снова ушло в постфильтр.

## Фиксация исходного состояния (до всего)

```bash
MY -e "SELECT storefront_code, name, value FROM shop_prefill_settings
       WHERE name IN ('on_entry','guest_enabled','guest_consent_required');" | tee /tmp/prefill-settings-before.txt
MY -e "SELECT MAX(id) AS max_order FROM shop_order; SELECT MAX(id) AS max_contact FROM wa_contact;"
```

Известно на момент написания: у витрины `wa-dev.loc/*` `on_entry = 1`, у глобальной `*` и у `wa-dev.loc/shop-1/*` — `false`. После реализации настройки не станет вовсе.

---

## Фаза 0 — Baseline (исполнять ДО реализации)

Цель — зафиксировать «до» и доказать, что проблема реальна.

| # | Действие | Что снять | Ожидание (текущий код) |
|---|---|---|---|
| 0.1 | Выйти из бэкенда, открыть главную | подсчёт SQL | есть запрос `WHERE name='prefill_guest_hash' AND value=...` |
| 0.2 | Открыть карточку товара, категорию, поиск | подсчёт SQL | тот же запрос повторяется **на каждой** странице |
| 0.3 | `EXPLAIN` старого запроса | план | `Using where`, `filtered: 10.00` |
| 0.4 | Оформить гостевой заказ, вернуться на главную | подсчёт SQL | цепочка из 5-9 запросов на страницу |
| 0.5 | На `/order/` изменить количество товара (2-3 раза) | подсчёт SQL на каждый `calculate` | цепочка повторяется целиком каждый раз |

Записать числа в таблицу отчёта — они станут колонкой «Было».

---

## Фаза 1 — Каталог молчит

| # | Сценарий | Проверка | Критерий |
|---|---|---|---|
| 1.1 | Гость (из бэкенда вышли) проходит главную → категорию → карточку → поиск | подсчёт SQL | **0** запросов к `shop_order_params` / `shop_order` / `shop_customer` на всех страницах |
| 1.2 | Тот же проход | лог плагина | нет строк о загрузке заказа; строки об ассетах/куках есть |
| 1.3 | Настройки плагина в админке | визуально | переключателя `on_entry` нет; остальные настройки сохраняются без ошибок |
| 1.4 | `grep -rn "on_entry" lib/ templates/ locale/*/LC_MESSAGES/*.po` | код | пусто |
| 1.5 | Открыть `/order/` | форма | чекаут предзаполнен с первой же загрузки, без `on_entry` |

## Фаза 2 — Гость

| # | Сценарий | Проверка | Критерий |
|---|---|---|---|
| 2.1 | Новый посетитель (очистить куки), пройти каталог и открыть `/order/` | лог + SQL | куки `prefill_guest_token` нет, к заказам **0** запросов |
| 2.2 | Оформить первый заказ | лог + SQL | токен создан; в заказе строка `name='prefill_guest_<48hex>'`, `value='1'`; длина имени ≤ 64 |
| 2.3 | Проверить длину и формат | `MY -e "SELECT order_id, name, CHAR_LENGTH(name) L, value FROM shop_order_params WHERE name LIKE 'prefill_guest_%';"` | `L = 62`, значение `1`, hex-часть 48 символов |
| 2.4 | Вернуться на `/order/` в **новой** сессии | SQL | ровно **1** индексный lookup + гидратация, дальше 0 |
| 2.5 | Изменить количество товара 3 раза | SQL на каждый `calculate` | **0** обращений к источнику |
| 2.6 | `EXPLAIN` реального имени из п.2.3 | план | `ref` по `name`, `filtered: 100.00`, без `Using where` |
| 2.7 | Подменить куку на случайные 64 hex | форма + SQL | безопасный промах: предзаполнения нет, ошибок нет |
| 2.8 | Подменить куку на невалидное значение (`abc`, пустая, 65 символов) | форма | считается отсутствующей, поведение как у нового гостя |
| 2.9 | Оформить **второй** заказ | SQL | то же имя параметра; источником стал больший `order_id` |
| 2.10 | Отозвать согласие / очистить историю | БД | кука, снапшот, маркер удалены **и строки `prefill_guest_%` в заказах удалены** |
| 2.11 | Заказ из бэкенда при живой сессии витрины | БД + лог | куки админа заказ не получил, storefront-маркер не тронут |
| 2.12 | При `consent_required = 1` без согласия оформить заказ | БД | ни куки, ни параметра заказа |

## Фаза 3 — Авторизованный

Вход: открыть форму логина и **нажать кнопку** — пароль подставит браузер.

| # | Сценарий | Проверка | Критерий |
|---|---|---|---|
| 3.1 | Пользователь без заказов, свежая сессия, открыть `/order/` | SQL | ровно **1** запрос — `shop_customer` по PK |
| 3.2 | Он же, ещё несколько действий | SQL | **0** обращений |
| 3.3 | Пользователь с заказом, свежая сессия | SQL | 1 PK lookup + гидратация, дальше 0 |
| 3.4 | Полностью заполненный чекаут либо достаточный снапшот | SQL | к `shop_customer` и заказам не ходим вовсе |
| 3.5 | Логин → логаут → логин | лог | source key меняется, маркер прошлой личности не переиспользуется |
| 3.6 | Оформить заказ, продолжить в той же сессии | SQL + форма | маркер сброшен, подхватился новый `last_order_id` |
| 3.7 | Контакт без заказов, но со строкой `shop_customer` (`last_order_id IS NULL`) | SQL | пустота определена по `last_order_id`, а не по `number_of_orders` |

## Фаза 4 — Критические регрессии

Эти два теста проверяют ровно те места, где предыдущая редакция решения ломала соседнюю логику. Их пропускать нельзя.

| # | Сценарий | Критерий |
|---|---|---|
| 4.1 | **Снапшот при совпавшем маркере.** В живой сессии заполнить форму, затем стереть город, обновить страницу | Город **восстановился из снапшота**, при этом обращений к БД за источником **0**. Провал = маркер гейтит весь метод, а не только loader |
| 4.2 | **Гость без куки не поднимает сессию.** Полностью очистить куки, пройти 3 страницы каталога | В ответах **нет** `Set-Cookie: PHPSESSID`, `session_is_alive` остаётся `false`. Провал = маркер пишется для `guest:none` |
| 4.3 | Явный выбор варианта в «Моих вариантах» и apply delivery у авторизованного | Обходят маркер, применяют выбранный заказ |
| 4.4 | Reset & refill, force prefill (debug) | Источник действительно перечитывается |
| 4.5 | Ручной ввод не перетирается повторным prefill | Отдельно сверить с [issue-65](../codereview/issue-65-prefill-overrides-current-input.md) — в **свежей** сессии |
| 4.6 | Гость с token-cookie открывает checkout и напрямую запрашивает `params-choice` / `apply-delivery` | Ссылки «Мои варианты» нет; оба endpoint отказывают в доступе; история адресов не выводится |
| 4.7 | `cityselect`: выбрать город виджетом, затем открыть `/order/` | Регион из виджета не конфликтует с предзаполнением (см. бэклог TODO.md) |

## Форма отчёта

| Сценарий | Было (Фаза 0) | Стало | Вердикт |
|---|---:|---:|---|
| Каталог, гость без истории | | | |
| Каталог, гость с историей | | | |
| `/order/`, первый вход в сессии | | | |
| `/order/`, каждый `calculate` | | | |
| Авторизованный без заказов | | | |
| Авторизованный с заказом | | | |

## Уборка после сессии

```bash
# 1. общий лог запросов — вернуть исходное
MY -e "SET GLOBAL general_log='OFF'; SET GLOBAL log_output='FILE'; TRUNCATE TABLE mysql.general_log;"

# 2. тестовые заказы и контакты (по max id, снятым в начале)
MY -e "SELECT id, contact_id, create_datetime FROM shop_order WHERE id > <max_order_before>;"

# 3. настройки — сверить с /tmp/prefill-settings-before.txt и вернуть изменённые
MY -e "SELECT storefront_code, name, value FROM shop_prefill_settings
       WHERE name IN ('on_entry','guest_enabled','guest_consent_required');"
```

Отдельно вернуть, если менялись: `order_without_auth` в настройках чекаута, `rememberme` домена, пароль контакта, которому ломали хеш для разлогина.

Кэш после правок кода: `rm -rf wa-cache/*/apps/ wa-cache/*/config/`.
